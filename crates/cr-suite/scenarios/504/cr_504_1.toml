rule = "504.1"
section = 504
status = "deferred"
text = "First, the active player draws a card. This turn-based action doesn't use the stack."
notes = "Deferred: needs a real advance into the draw step so the draw TBA fires. Once wired, assert hand_count_at_least increased by one relative to the pre-draw baseline (the hand_count_at_least assertion kind is ready)."
