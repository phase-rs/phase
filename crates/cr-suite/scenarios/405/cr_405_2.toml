rule = "405.2"
section = 405
title = "Stack - CR 405.2"
status = "skeleton"
text = "The stack keeps track of the order that spells and/or abilities were added to it. Each time an object is put on the stack, it’s put on top of all objects already there."
