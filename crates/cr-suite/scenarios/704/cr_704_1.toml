rule = "704.1"
section = 704
title = "State-Based Actions — CR 704.1"
status = "skeleton"
text = "State-based actions are game actions that happen automatically whenever certain conditions (listed below) are met. State-based actions don’t use the stack."
