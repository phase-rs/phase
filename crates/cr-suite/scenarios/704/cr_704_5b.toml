rule = "704.5b"
section = 704
status = "deferred"
text = "If a player attempted to draw a card from a library with no cards in it since the last time state-based actions were checked, that player loses the game."
notes = "Deferred: needs a production draw-from-empty-library transition. The runner has no draw step yet; once a real draw against an empty library is reachable, assert game_over."
