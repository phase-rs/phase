rule = "704.7"
section = 704
title = "State-Based Actions — CR 704.7"
status = "skeleton"
text = "If multiple state-based actions would have the same result at the same time, a single replacement effect will replace all of them."
