rule = "111.1"
section = 111
title = "Tokens — CR 111.1"
status = "skeleton"
text = "Some effects put tokens onto the battlefield. A token is a marker used to represent any permanent that isn’t represented by a card."
