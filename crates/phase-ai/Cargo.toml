[package]
name = "phase-ai"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
engine = { path = "../engine" }
serde = { workspace = true }
serde_json = "1"
rand = "0.9"
tracing = "0.1"

[dev-dependencies]
