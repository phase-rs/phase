import { describe, it } from "vitest";

describe("DraftPodPage betweenGames", () => {
  describe("BO3-09: sideboard UI rendering", () => {
    it.todo(
      "renders sideboard UI when sideboardPrompt is set and not yet submitted",
    );
  });

  describe("BO3-10: waiting-for-opponent view", () => {
    it.todo("shows waiting-for-opponent view after sideboard submission");
    it.todo("displays submitted deck in read-only view");
  });
});
