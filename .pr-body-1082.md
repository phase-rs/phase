## Summary
Adds engine support for **Fugitive Druid**.

## Files changed
- crates/engine/src/parser/oracle_trigger.rs
- crates/engine/src/game/trigger_matchers.rs

## CR references
- CR 115.1a + CR 115.1b — spell and Aura targeting constraints in becomes-target trigger source filtering.
- CR 115.1a + CR 115.1b — parser mapping for "becomes the target of a spell" and "becomes the target of an Aura spell".

## Track
Developer

## LLM
Model: GPT-5.3-Codex
Thinking: medium

## Verification
- `cargo fmt --all` — clean
- `cargo clippy-strict` — clean
- `./scripts/check-parser-combinators.sh` — clean
- `cargo test -p engine` — clean (8603 unit pass, 458 integration pass, 0 fail)
- `./scripts/gen-card-data.sh` — failed to generate `client/public/card-data.json` and `data/card-data.json` when executed with `bash -lc`; script exits 0 with no output
- `cargo coverage` — blocked: card database load failure because `data/card-data.json` missing
- `cargo semantic-audit` — not completed; run was terminated while building after the card-data generation blocker persisted

## Scope Expansion
None.

## Validation Failures
None.

## CI Failures
- Card-data generation is currently blocked in this environment under the required `bash -lc` invocation (`scripts/gen-card-data.sh` exits successfully but does not emit output or produce artifacts).
- As a downstream effect, `cargo coverage` and `cargo semantic-audit` could not be fully completed in this run.
