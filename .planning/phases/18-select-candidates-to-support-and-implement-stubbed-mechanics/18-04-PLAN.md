---
phase: 18-select-candidates-to-support-and-implement-stubbed-mechanics
plan: 04
type: execute
wave: 2
depends_on: [18-02]
files_modified:
  - crates/engine/src/game/effects/dig.rs
  - crates/engine/src/game/effects/gain_control.rs
  - crates/engine/src/game/effects/mod.rs
  - crates/engine/src/game/combat_damage.rs
  - crates/engine/src/game/sba.rs
  - crates/engine/src/types/player.rs
autonomous: true
requirements: [MECH-07, MECH-08]

must_haves:
  truths:
    - "Dig effect reveals top N cards, picks some to hand, rest to bottom/graveyard"
    - "GainControl effect changes controller of target permanent"
    - "Wither damage to creatures applies as -1/-1 counters instead of marked damage"
    - "Infect damage to creatures applies as -1/-1 counters, to players as poison counters"
    - "Player with 10+ poison counters loses the game (SBA)"
  artifacts:
    - path: "crates/engine/src/game/effects/dig.rs"
      provides: "Dig/Reveal effect handler"
      exports: ["resolve"]
    - path: "crates/engine/src/game/effects/gain_control.rs"
      provides: "GainControl effect handler"
      exports: ["resolve"]
    - path: "crates/engine/src/game/combat_damage.rs"
      provides: "Wither and Infect damage modifications"
      contains: "Keyword::Wither"
    - path: "crates/engine/src/game/sba.rs"
      provides: "Poison counter loss condition"
      contains: "poison"
  key_links:
    - from: "crates/engine/src/game/combat_damage.rs"
      to: "crates/engine/src/game/effects/counters.rs"
      via: "Wither/Infect apply -1/-1 counters via counter system"
      pattern: "Wither|Infect"
    - from: "crates/engine/src/game/sba.rs"
      to: "crates/engine/src/types/player.rs"
      via: "Check poison_counters >= 10"
      pattern: "poison_counters"
---

<objective>
Implement Dig and GainControl effect handlers, plus Wither/Infect damage mechanics with poison counter support.

Purpose: Dig (312 cards), GainControl (119 cards), Wither (~45), and Infect (~45) are medium-complexity mechanics that round out the effect handler coverage and introduce the poison counter subsystem.
Output: 2 new effect modules, Wither/Infect in combat_damage, poison SBA
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-select-candidates-to-support-and-implement-stubbed-mechanics/18-CONTEXT.md
@.planning/phases/18-select-candidates-to-support-and-implement-stubbed-mechanics/18-RESEARCH.md
@.planning/phases/18-select-candidates-to-support-and-implement-stubbed-mechanics/18-02-SUMMARY.md

<interfaces>
<!-- Effect handler signature (same as Plan 02) -->
```rust
pub type EffectHandler = fn(
    state: &mut GameState,
    ability: &ResolvedAbility,
    events: &mut Vec<GameEvent>,
) -> Result<(), EffectError>;
```

<!-- Combat damage application point (from combat_damage.rs) -->
<!-- This is where Wither/Infect modify HOW damage is dealt -->
<!-- Wither: damage to creatures = -1/-1 counters instead of marked damage -->
<!-- Infect: damage to creatures = -1/-1 counters, damage to players = poison counters -->
<!-- IMPORTANT: Lifelink still applies with Wither/Infect per MTG rules -->

<!-- Player struct (from types/player.rs) needs poison_counters field -->
```rust
pub struct Player {
    pub id: PlayerId,
    pub life: i32,
    pub library: Vec<ObjectId>,
    pub hand: Vec<ObjectId>,
    pub graveyard: Vec<ObjectId>,
    pub exile: Vec<ObjectId>,
    pub mana_pool: ManaPool,
    // ... add: pub poison_counters: u32,
}
```

<!-- SBA check pattern (from sba.rs) -->
<!-- Existing: player with life <= 0 loses -->
<!-- Add: player with poison_counters >= 10 loses -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Dig and GainControl effect handlers</name>
  <files>crates/engine/src/game/effects/dig.rs, crates/engine/src/game/effects/gain_control.rs, crates/engine/src/game/effects/mod.rs</files>
  <behavior>
    - Test: Dig 3 with ChangeNum=1 reveals top 3 cards, moves 1 to hand, rest to bottom of library (simplified: move first N to hand, rest to bottom since player choice needs WaitingFor)
    - Test: Dig with empty library does nothing
    - Test: GainControl changes target permanent's controller field
    - Test: GainControl on non-existent target returns error
  </behavior>
  <action>
1. Create `crates/engine/src/game/effects/dig.rs`:
   - `pub fn resolve(state, ability, events) -> Result<(), EffectError>`
   - Read `DigNum` or `NumCards` param for how many to reveal
   - Read `ChangeNum` param for how many to keep (put in hand)
   - Simplified implementation: reveal top N from controller's library, put first `ChangeNum` in hand, rest to bottom of library
   - Add TODO comment: "Full implementation needs WaitingFor::DigChoice for player to select which cards to keep"
   - Emit events for cards moved

2. Create `crates/engine/src/game/effects/gain_control.rs`:
   - `pub fn resolve(state, ability, events) -> Result<(), EffectError>`
   - Resolve target from ability.targets (TargetRef::Object)
   - Change `obj.controller` to ability.controller
   - Emit GameEvent for control change

3. Register both in `effects/mod.rs`:
   - Add `pub mod dig;` and `pub mod gain_control;`
   - `registry.insert("Dig".to_string(), dig::resolve);`
   - `registry.insert("GainControl".to_string(), gain_control::resolve);`

4. Add inline tests in each module.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine -- effects --no-fail-fast 2>&1 | tail -30</automated>
  </verify>
  <done>Dig and GainControl handlers registered and tested</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Wither/Infect damage and poison counter SBA</name>
  <files>crates/engine/src/game/combat_damage.rs, crates/engine/src/game/sba.rs, crates/engine/src/types/player.rs</files>
  <behavior>
    - Test: Creature with Wither dealing 3 combat damage to a creature applies 3 -1/-1 counters instead of marked damage
    - Test: Creature with Wither dealing combat damage to a player deals normal damage (not poison)
    - Test: Creature with Infect dealing 3 combat damage to a creature applies 3 -1/-1 counters
    - Test: Creature with Infect dealing 3 combat damage to a player adds 3 poison counters (no life loss)
    - Test: Player with 10+ poison counters triggers SBA loss
    - Test: Lifelink still works with Wither/Infect (attacker gains life equal to damage dealt)
  </behavior>
  <action>
1. In `crates/engine/src/types/player.rs`:
   - Add `pub poison_counters: u32` field to `Player` struct (default 0)
   - Ensure Default/new implementation sets it to 0

2. In `crates/engine/src/game/combat_damage.rs`:
   - At the point where combat damage is applied to creatures, check if the damage source has Keyword::Wither or Keyword::Infect
   - If Wither or Infect: instead of marking damage on the creature, add that many -1/-1 counters via the counter system (use `AddCounter` event or direct mutation). Per MTG rules, this still counts as damage for Lifelink, triggers, etc.
   - At the point where combat damage is applied to players, check if the damage source has Keyword::Infect
   - If Infect: instead of reducing life, add that many poison counters to the player. Lifelink still triggers (attacker's controller gains life).
   - Be careful: Wither does NOT apply poison to players. Only Infect does.

3. In `crates/engine/src/game/sba.rs`:
   - Add poison counter check alongside the existing life total check
   - If any player has `poison_counters >= 10`, they lose the game
   - Follow existing SBA pattern for player loss

4. Add inline tests in combat_damage.rs and sba.rs.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine -- combat_damage::tests sba::tests --no-fail-fast 2>&1 | tail -30</automated>
  </verify>
  <done>Wither applies -1/-1 counters to creatures, Infect applies -1/-1 to creatures and poison to players, poison SBA triggers at 10</done>
</task>

</tasks>

<verification>
- `cargo test -p engine -- effects combat_damage sba` all pass
- `cargo clippy -p engine -- -D warnings` passes
- Poison counter serializes correctly across WASM boundary
</verification>

<success_criteria>
- Dig and GainControl effect handlers work and are registered
- Wither/Infect modify damage application correctly
- Poison counter system works with SBA loss condition
- Lifelink interacts correctly with Wither/Infect
</success_criteria>

<output>
After completion, create `.planning/phases/18-select-candidates-to-support-and-implement-stubbed-mechanics/18-04-SUMMARY.md`
</output>
