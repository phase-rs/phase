---
phase: 18-select-candidates-to-support-and-implement-stubbed-mechanics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/engine/src/game/test_helpers.rs
  - crates/engine/src/game/mod.rs
  - crates/engine/src/game/combat.rs
autonomous: true
requirements: [MECH-01, MECH-02]

must_haves:
  truths:
    - "Fear creatures can only be blocked by artifact or black creatures"
    - "Intimidate creatures can only be blocked by artifact or color-sharing creatures"
    - "Skulk creatures cannot be blocked by creatures with greater power"
    - "Horsemanship creatures can only be blocked by other horsemanship creatures"
    - "Reusable test helper loads Forge card definitions and spawns creatures in game state"
  artifacts:
    - path: "crates/engine/src/game/test_helpers.rs"
      provides: "Reusable test helper for loading Forge cards and spawning game objects"
      exports: ["load_card", "spawn_creature", "forge_db"]
    - path: "crates/engine/src/game/combat.rs"
      provides: "4 new evasion keyword checks in validate_blockers"
      contains: "Keyword::Fear"
  key_links:
    - from: "crates/engine/src/game/combat.rs"
      to: "crates/engine/src/types/keywords.rs"
      via: "has_keyword() checks for Fear, Intimidate, Skulk, Horsemanship"
      pattern: "has_keyword.*Keyword::(Fear|Intimidate|Skulk|Horsemanship)"
---

<objective>
Create test infrastructure and implement the 4 remaining combat evasion keywords (Fear, Intimidate, Skulk, Horsemanship) in validate_blockers().

Purpose: Establishes the reusable test helper that all subsequent plans depend on, and implements the simplest tier of mechanics (single-function boolean checks) to build momentum.
Output: test_helpers.rs module, 4 evasion keywords with unit tests
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-select-candidates-to-support-and-implement-stubbed-mechanics/18-CONTEXT.md
@.planning/phases/18-select-candidates-to-support-and-implement-stubbed-mechanics/18-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From crates/engine/src/game/combat.rs (validate_blockers signature):
```rust
pub fn validate_blockers(
    state: &GameState,
    assignments: &[(ObjectId, ObjectId)],
) -> Result<(), String>
```

Existing evasion pattern (Flying check at line 146-154):
```rust
if attacker.has_keyword(&Keyword::Flying)
    && !blocker.has_keyword(&Keyword::Flying)
    && !blocker.has_keyword(&Keyword::Reach)
{
    return Err(format!(...));
}
```

From crates/engine/src/types/keywords.rs:
```rust
pub enum Keyword {
    Fear, Intimidate, Skulk, Horsemanship,
    // ... 200+ variants already parsed
}
```

From crates/engine/src/game/game_object.rs:
```rust
impl GameObject {
    pub fn has_keyword(&self, kw: &Keyword) -> bool { ... }
}
```

From crates/engine/src/types/card_type.rs:
```rust
pub enum CoreType { Artifact, Creature, Enchantment, Land, Planeswalker, ... }
```

From crates/engine/src/types/identifiers.rs:
```rust
pub struct ObjectId(pub u64);
pub struct CardId(pub u64);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test helper module and add 4 evasion keywords to validate_blockers</name>
  <files>crates/engine/src/game/test_helpers.rs, crates/engine/src/game/mod.rs, crates/engine/src/game/combat.rs</files>
  <behavior>
    - Test: Fear creature cannot be blocked by a non-artifact, non-black creature -> Err
    - Test: Fear creature CAN be blocked by a black creature -> Ok
    - Test: Fear creature CAN be blocked by an artifact creature -> Ok
    - Test: Intimidate creature cannot be blocked by non-artifact creature that shares no color -> Err
    - Test: Intimidate creature CAN be blocked by creature sharing a color -> Ok
    - Test: Skulk creature (power 2) cannot be blocked by creature with power 3 -> Err
    - Test: Skulk creature (power 2) CAN be blocked by creature with power 2 -> Ok
    - Test: Skulk creature (power 2) CAN be blocked by creature with power 1 -> Ok
    - Test: Horsemanship creature cannot be blocked by non-horsemanship creature -> Err
    - Test: Horsemanship creature CAN be blocked by horsemanship creature -> Ok
  </behavior>
  <action>
1. Create `crates/engine/src/game/test_helpers.rs` with:
   - `OnceLock<Option<CardDatabase>>` for lazy Forge DB loading
   - `forge_db() -> Option<&'static CardDatabase>` that loads from `../../forge/forge-gui/res/cardsfolder/` if available
   - `load_card(name: &str) -> Option<&'static CardRules>` wrapper
   - `spawn_creature(state, name, owner) -> Option<ObjectId>` that creates a battlefield object from a Forge card definition, setting card_types, keywords, power/toughness, entered_battlefield_turn
   - All functions gracefully return None when Forge DB is unavailable (CI-safe)

2. Add `pub mod test_helpers;` to `crates/engine/src/game/mod.rs` (gated behind `#[cfg(test)]` or made public for integration tests -- make it always public since coverage report needs it)

3. In `crates/engine/src/game/combat.rs` `validate_blockers()`, after the Shadow check block (line ~171) and before the `blockers_per_attacker` push (line ~173), add 4 evasion checks following the exact patterns from RESEARCH.md Pattern 1:
   - **Fear** (MTG 702.36): Can only be blocked by artifact creatures or black creatures. Check `blocker.card_types.core_types.contains(&CoreType::Artifact)` and `blocker.color.contains(&ManaColor::Black)`.
   - **Intimidate** (MTG 702.13): Can only be blocked by artifact creatures or creatures sharing a color with the attacker. Check artifact OR `attacker.color.iter().any(|c| blocker.color.contains(c))`.
   - **Skulk** (MTG 702.120): Cannot be blocked by creatures with strictly greater power. Compare `blocker.power.unwrap_or(0) > attacker.power.unwrap_or(0)`.
   - **Horsemanship** (MTG 702.30): Can only be blocked by creatures with horsemanship. Same pattern as Flying but with Horsemanship keyword.

4. Add `use crate::types::mana::ManaColor;` import to combat.rs if not already present.

5. Add inline `#[cfg(test)]` tests in combat.rs for all 10 test cases listed in behavior. Use the existing test pattern in combat.rs (create_object, set keywords, call validate_blockers).
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine -- combat::tests --no-fail-fast 2>&1 | tail -30</automated>
  </verify>
  <done>All 4 evasion keywords enforce correct blocking restrictions, 10+ new tests pass, test_helpers module compiles and is importable</done>
</task>

</tasks>

<verification>
- `cargo test -p engine -- combat::tests` passes with all new evasion keyword tests
- `cargo clippy -p engine -- -D warnings` passes
- `cargo test -p engine -- test_helpers` compiles (even if Forge DB tests skip)
</verification>

<success_criteria>
- Fear, Intimidate, Skulk, Horsemanship blocking restrictions enforced in validate_blockers
- test_helpers.rs provides load_card() and spawn_creature() for subsequent plans
- All existing combat tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/18-select-candidates-to-support-and-implement-stubbed-mechanics/18-01-SUMMARY.md`
</output>
