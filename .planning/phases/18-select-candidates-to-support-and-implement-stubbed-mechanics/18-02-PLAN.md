---
phase: 18-select-candidates-to-support-and-implement-stubbed-mechanics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/engine/src/game/effects/mill.rs
  - crates/engine/src/game/effects/scry.rs
  - crates/engine/src/game/effects/mod.rs
  - crates/engine/src/game/effects/pump.rs
  - crates/engine/src/game/effects/deal_damage.rs
  - crates/engine/src/game/effects/destroy.rs
  - crates/engine/src/game/effects/change_zone.rs
autonomous: true
requirements: [MECH-03, MECH-04]

must_haves:
  truths:
    - "Mill effect moves top N cards from library to graveyard"
    - "Scry effect lets controller look at top N and reorder to top/bottom"
    - "PumpAll effect applies P/T modification to all matching creatures"
    - "DamageAll effect deals damage to all matching creatures and/or players"
    - "DestroyAll effect destroys all matching permanents"
  artifacts:
    - path: "crates/engine/src/game/effects/mill.rs"
      provides: "Mill effect handler"
      exports: ["resolve"]
    - path: "crates/engine/src/game/effects/scry.rs"
      provides: "Scry effect handler"
      exports: ["resolve"]
    - path: "crates/engine/src/game/effects/mod.rs"
      provides: "Registry with 7+ new effect handlers"
      contains: "Mill"
  key_links:
    - from: "crates/engine/src/game/effects/mod.rs"
      to: "crates/engine/src/game/effects/mill.rs"
      via: "registry.insert(\"Mill\", mill::resolve)"
      pattern: "registry.insert.*Mill"
    - from: "crates/engine/src/game/effects/mod.rs"
      to: "crates/engine/src/game/effects/scry.rs"
      via: "registry.insert(\"Scry\", scry::resolve)"
      pattern: "registry.insert.*Scry"
---

<objective>
Implement core effect handlers: Mill, Scry (new modules) and PumpAll, DamageAll, DestroyAll, ChangeZoneAll (extensions to existing modules). Register all in build_registry().

Purpose: These are the highest-frequency missing effect handlers (Mill: 153 cards, PumpAll: 399, Dig: 312, DamageAll: 170, DestroyAll: 154, ChangeZoneAll: 207). Implementing them dramatically increases card support.
Output: 2 new effect modules + 5 extended handlers registered in build_registry()
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-select-candidates-to-support-and-implement-stubbed-mechanics/18-CONTEXT.md
@.planning/phases/18-select-candidates-to-support-and-implement-stubbed-mechanics/18-RESEARCH.md

<interfaces>
<!-- Effect handler signature from effects/mod.rs -->
```rust
pub type EffectHandler = fn(
    state: &mut GameState,
    ability: &ResolvedAbility,
    events: &mut Vec<GameEvent>,
) -> Result<(), EffectError>;
```

<!-- ResolvedAbility structure -->
```rust
pub struct ResolvedAbility {
    pub api_type: String,
    pub params: HashMap<String, String>,
    pub targets: Vec<TargetRef>,
    pub source_id: ObjectId,
    pub controller: PlayerId,
    pub sub_ability: Option<Box<ResolvedAbility>>,
    pub svars: HashMap<String, String>,
}
```

<!-- Existing registration pattern -->
```rust
registry.insert("DealDamage".to_string(), deal_damage::resolve);
registry.insert("Destroy".to_string(), destroy::resolve);
registry.insert("Pump".to_string(), pump::resolve);
registry.insert("ChangeZone".to_string(), change_zone::resolve);
```

<!-- Zone movement helper -->
```rust
// From crates/engine/src/game/zones.rs:
pub fn move_to_zone(state: &mut GameState, obj_id: ObjectId, target_zone: Zone, events: &mut Vec<GameEvent>);
```

<!-- Common param patterns from card-data.json -->
- Mill: `NumCards` (count), targets player
- Scry: `ScryNum` (count), controller looks at own library
- PumpAll: `NumAtt`, `NumDef`, `Valid` (filter like "Creature.YouCtrl")
- DamageAll: `NumDmg`, `Valid` (filter), `ValidDesc`
- DestroyAll: `Valid` (filter), `NoRegen` (optional)
- ChangeZoneAll: `Origin`, `Destination`, `Valid` (filter)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Mill and Scry effect handlers</name>
  <files>crates/engine/src/game/effects/mill.rs, crates/engine/src/game/effects/scry.rs, crates/engine/src/game/effects/mod.rs</files>
  <behavior>
    - Test: Mill 3 moves top 3 cards from target player's library to their graveyard
    - Test: Mill with empty library does nothing (no panic)
    - Test: Mill with fewer cards than requested mills what's available
    - Test: Scry 2 with controller's library reorders (simplified: for now, move all scryed cards to bottom since we can't prompt for choice -- add a comment noting this is a simplified implementation until WaitingFor::ScryChoice is added)
    - Test: Scry with empty library does nothing
  </behavior>
  <action>
1. Create `crates/engine/src/game/effects/mill.rs`:
   - `pub fn resolve(state, ability, events) -> Result<(), EffectError>`
   - Read `NumCards` param (default 1)
   - Resolve target player from ability.targets (first TargetRef::Player, or default to opponent of controller)
   - Move top N cards from player's library to graveyard using `zones::move_to_zone()`
   - Emit appropriate GameEvent for each card milled
   - Handle edge cases: empty library, fewer cards than requested

2. Create `crates/engine/src/game/effects/scry.rs`:
   - `pub fn resolve(state, ability, events) -> Result<(), EffectError>`
   - Read `ScryNum` param (default 1)
   - For simplified implementation: look at top N cards, move all to bottom of library (a proper implementation would need WaitingFor::ScryChoice for the player to choose top/bottom ordering -- add TODO comment)
   - This is still useful: it triggers Scry-related events and moves cards, which is correct behavior for AI opponents

3. Register both in `effects/mod.rs`:
   - Add `pub mod mill;` and `pub mod scry;`
   - Add `registry.insert("Mill".to_string(), mill::resolve);`
   - Add `registry.insert("Scry".to_string(), scry::resolve);`

4. Update the `registry_has_15_entries` test to reflect new count (or change to `assert!(registry.len() >= 15)`).

5. Add inline tests in each new module.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine -- effects --no-fail-fast 2>&1 | tail -30</automated>
  </verify>
  <done>Mill and Scry handlers registered, unit tests pass, existing effect tests unbroken</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add PumpAll, DamageAll, DestroyAll, ChangeZoneAll to existing handlers</name>
  <files>crates/engine/src/game/effects/pump.rs, crates/engine/src/game/effects/deal_damage.rs, crates/engine/src/game/effects/destroy.rs, crates/engine/src/game/effects/change_zone.rs, crates/engine/src/game/effects/mod.rs</files>
  <behavior>
    - Test: PumpAll with Valid="Creature.YouCtrl" gives +1/+1 to all controller's creatures
    - Test: DamageAll with Valid="Creature" deals N damage to all creatures on battlefield
    - Test: DestroyAll with Valid="Creature" destroys all creatures
    - Test: ChangeZoneAll with Origin=Battlefield, Destination=Hand, Valid="Creature.OppCtrl" bounces all opponent creatures
  </behavior>
  <action>
1. In `effects/pump.rs`, add a `resolve_all` function (or handle within `resolve` by checking for `Valid` param indicating "All" mode):
   - Parse `Valid` filter string (e.g., "Creature.YouCtrl", "Creature.OppCtrl", "Creature") using a simple filter matcher
   - Apply NumAtt/NumDef to all matching creatures on battlefield
   - Create a shared filter helper: `fn matches_filter(obj: &GameObject, filter: &str, controller: PlayerId) -> bool` that handles common Forge filter patterns: "Creature", "Creature.YouCtrl", "Creature.OppCtrl", type checks
   - Put the shared filter helper in effects/mod.rs so all "All" variants can use it

2. In `effects/deal_damage.rs`, add `resolve_all` for DamageAll:
   - Parse `NumDmg` and `Valid` filter
   - Deal damage to all matching permanents (and optionally players if filter includes "Player")

3. In `effects/destroy.rs`, add `resolve_all` for DestroyAll:
   - Parse `Valid` filter
   - Destroy all matching permanents (check Indestructible)

4. In `effects/change_zone.rs`, add `resolve_all` for ChangeZoneAll:
   - Parse `Origin`, `Destination`, `Valid` filter
   - Move all matching objects from origin zone to destination zone

5. Register all four in `effects/mod.rs` build_registry():
   - `registry.insert("PumpAll".to_string(), pump::resolve_all);`
   - `registry.insert("DamageAll".to_string(), deal_damage::resolve_all);`
   - `registry.insert("DestroyAll".to_string(), destroy::resolve_all);`
   - `registry.insert("ChangeZoneAll".to_string(), change_zone::resolve_all);`

6. Add inline tests in each module's `#[cfg(test)]` block.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine -- effects --no-fail-fast 2>&1 | tail -30</automated>
  </verify>
  <done>PumpAll, DamageAll, DestroyAll, ChangeZoneAll registered and tested, filter helper works for common Forge patterns</done>
</task>

</tasks>

<verification>
- `cargo test -p engine -- effects` passes all new and existing tests
- `cargo clippy -p engine -- -D warnings` passes
- build_registry() returns 21+ entries (was 15, added Mill, Scry, PumpAll, DamageAll, DestroyAll, ChangeZoneAll = 21)
</verification>

<success_criteria>
- 6 new effect handlers registered in build_registry()
- Shared filter helper handles common Forge "Valid" patterns
- All unit tests pass, no regressions in existing effect tests
</success_criteria>

<output>
After completion, create `.planning/phases/18-select-candidates-to-support-and-implement-stubbed-mechanics/18-02-SUMMARY.md`
</output>
