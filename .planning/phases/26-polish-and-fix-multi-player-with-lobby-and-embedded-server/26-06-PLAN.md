---
phase: 26-polish-and-fix-multi-player-with-lobby-and-embedded-server
plan: 06
type: execute
wave: 3
depends_on: ["26-02", "26-03"]
files_modified:
  - client/src/components/multiplayer/ConcedeDialog.tsx
  - client/src/components/multiplayer/EmoteOverlay.tsx
  - client/src/components/chrome/GameMenu.tsx
  - client/src/components/hud/OpponentHud.tsx
  - client/src/pages/GamePage.tsx
  - client/src/adapter/ws-adapter.ts
autonomous: false
requirements: [MP-CONCEDE, MP-EMOTE, MP-TIMER-UI, MP-GAMEOVER, MP-OPPONENT-NAME]

must_haves:
  truths:
    - "Player can concede via game menu with confirmation dialog"
    - "Quick emotes (Good game, Nice play, Thinking, Hello, Oops) can be sent and received"
    - "Opponent display name shown near their life total"
    - "Game over screen shows winner, total turns, game duration, and Back to Lobby button"
    - "Conceding sends Concede message to server and triggers game over for both players"
    - "Emotes appear as temporary overlay near opponent/player area"
  artifacts:
    - path: "client/src/components/multiplayer/ConcedeDialog.tsx"
      provides: "Confirmation dialog for conceding"
      min_lines: 30
    - path: "client/src/components/multiplayer/EmoteOverlay.tsx"
      provides: "Emote buttons and received emote display"
      min_lines: 50
  key_links:
    - from: "client/src/components/chrome/GameMenu.tsx"
      to: "client/src/components/multiplayer/ConcedeDialog.tsx"
      via: "Concede button in game menu opens dialog"
      pattern: "ConcedeDialog|concede"
    - from: "client/src/adapter/ws-adapter.ts"
      to: "client/src/components/multiplayer/EmoteOverlay.tsx"
      via: "Emote server message forwarded to UI via WsAdapterEvent"
      pattern: "emoteReceived|Emote"
---

<objective>
Add in-game multiplayer UX features: concede with confirmation, MTGA-style quick emotes, opponent display name, per-turn timer UI, and enhanced game over screen with lobby return.

Purpose: These features make multiplayer games feel complete — players can communicate via emotes, gracefully concede, see their opponent's identity, and return to the lobby for rematches after a game ends.

Output: ConcedeDialog, EmoteOverlay components, opponent name in HUD, timer display, game over with lobby return.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-polish-and-fix-multi-player-with-lobby-and-embedded-server/26-CONTEXT.md
@.planning/phases/26-polish-and-fix-multi-player-with-lobby-and-embedded-server/26-RESEARCH.md
@.planning/phases/26-polish-and-fix-multi-player-with-lobby-and-embedded-server/26-01-SUMMARY.md
@.planning/phases/26-polish-and-fix-multi-player-with-lobby-and-embedded-server/26-02-SUMMARY.md
@.planning/phases/26-polish-and-fix-multi-player-with-lobby-and-embedded-server/26-03-SUMMARY.md

<interfaces>
<!-- Extended WsAdapterEvent from Plan 01 -->
From client/src/adapter/ws-adapter.ts (after Plan 01):
```typescript
export type WsAdapterEvent =
  | { type: "gameCreated"; gameCode: string }
  | { type: "waitingForOpponent" }
  | { type: "stateChanged"; state: GameState; events: GameEvent[] }
  // ... existing variants
  ;
```

<!-- Protocol messages from Plan 02 -->
Server messages for this plan:
```
ServerMessage::Conceded { player: PlayerId }
ServerMessage::Emote { from_player: PlayerId, emote: String }
ServerMessage::TimerUpdate { player: PlayerId, remaining_seconds: u32 }
```

Client messages for this plan:
```
ClientMessage::Concede
ClientMessage::Emote { emote: String }
```

<!-- GameMenu props -->
From client/src/components/chrome/GameMenu.tsx:
```typescript
interface GameMenuProps {
  gameId: string;
  isAiMode: boolean;
  showAiHand: boolean;
  onToggleAiHand: () => void;
  onSettingsClick: () => void;
}
```

<!-- OpponentHud -->
From client/src/components/hud/OpponentHud.tsx:
```typescript
export function OpponentHud(): JSX.Element;
// Currently shows opponent life total, avatar
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement concede, emotes, and opponent display name</name>
  <files>
    client/src/components/multiplayer/ConcedeDialog.tsx,
    client/src/components/multiplayer/EmoteOverlay.tsx,
    client/src/components/chrome/GameMenu.tsx,
    client/src/components/hud/OpponentHud.tsx,
    client/src/adapter/ws-adapter.ts
  </files>
  <action>
1. Extend `WsAdapterEvent` in `ws-adapter.ts` with new variants:
   ```typescript
   | { type: "emoteReceived"; fromPlayer: PlayerId; emote: string }
   | { type: "conceded"; player: PlayerId }
   | { type: "timerUpdate"; player: PlayerId; remainingSeconds: number }
   | { type: "opponentName"; name: string }
   ```

2. Handle new server messages in `ws-adapter.ts` `handleMessage`:
   - `"Conceded"`: emit `{ type: "conceded", player: data.player }`
   - `"Emote"`: emit `{ type: "emoteReceived", fromPlayer: data.from_player, emote: data.emote }`
   - `"TimerUpdate"`: emit `{ type: "timerUpdate", player: data.player, remainingSeconds: data.remaining_seconds }`

3. Add send methods to `WebSocketAdapter`:
   - `sendConcede()`: send `{ type: "Concede" }` to server
   - `sendEmote(emote: string)`: send `{ type: "Emote", data: { emote } }` to server
   - Expose these via a ref or store so components can call them

4. Create `client/src/components/multiplayer/ConcedeDialog.tsx`:
   - Props: `isOpen: boolean`, `onConfirm: () => void`, `onCancel: () => void`
   - Modal overlay with dark backdrop
   - Title: "Concede Game?"
   - Text: "Your opponent will be declared the winner."
   - Two buttons: "Concede" (red, calls onConfirm) and "Cancel" (gray, calls onCancel)
   - Framer-motion fade/scale entrance

5. Add "Concede" to `GameMenu.tsx`:
   - Add `isOnlineMode: boolean` and `onConcede: () => void` to GameMenu props
   - When `isOnlineMode`, show a "Concede" button in the game menu dropdown (after existing items)
   - Clicking opens ConcedeDialog
   - On confirm, call the adapter's sendConcede (via prop callback)

6. Create `client/src/components/multiplayer/EmoteOverlay.tsx`:
   - Emote set: ["Good game", "Nice play", "Thinking...", "Hello!", "Oops"]
   - Two parts:
     a. **Emote button bar** (bottom-left, near player HUD): 5 small circular buttons with emote text/icons. Clicking sends emote via adapter.
     b. **Received emote display**: When an emote is received, show a floating bubble near the opponent area that auto-fades after 3 seconds (framer-motion AnimatePresence)
   - State: `receivedEmote: { text: string; id: number } | null` with auto-clear timeout
   - Props: `onSendEmote: (emote: string) => void`, `receivedEmote: string | null`
   - Minimal, non-intrusive design — small text bubbles with semi-transparent background

7. Update `OpponentHud.tsx` to show opponent display name:
   - Accept optional `opponentName?: string` prop
   - Display name above or below the life total in a smaller font
   - Read from multiplayerStore or pass via props from GamePage

8. Store opponent display name:
   - Add `opponentDisplayName: string | null` to multiplayerStore
   - Set it when `GameStarted` is received (the server could include it — or derive from lobby data)
   - For now, since the server `GameStarted` message doesn't include opponent name yet: extend the server message in a backward-compatible way by reading from session display_names in the server. OR just store it client-side from the lobby game listing data (the host name is visible in the lobby).
   - Simplest approach: add `opponent_name: Option<String>` to the `GameStarted` server message in protocol.rs (Plan 02's protocol). If Plan 02 didn't add it, add it now — it's a minor backward-compatible addition (Optional field).
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check</automated>
  </verify>
  <done>
    - ConcedeDialog renders with confirm/cancel
    - GameMenu shows Concede in online mode
    - EmoteOverlay shows 5 emote buttons and displays received emotes
    - OpponentHud shows opponent display name
    - ws-adapter handles Concede, Emote, TimerUpdate server messages
    - ws-adapter can send Concede and Emote client messages
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire multiplayer UX into GamePage and enhance game over screen</name>
  <files>
    client/src/pages/GamePage.tsx,
    client/src/adapter/ws-adapter.ts
  </files>
  <action>
1. Wire concede into GamePage:
   - Add `showConcedeDialog` state
   - Pass `isOnlineMode` and `onConcede` to GameMenu
   - On concede confirm: call adapter's sendConcede, close dialog
   - Handle `conceded` WsAdapterEvent: if opponent conceded, update UI (game over will follow from server)

2. Wire emotes into GamePage:
   - Add `receivedEmote` state (auto-cleared after 3s via setTimeout)
   - Handle `emoteReceived` WsAdapterEvent: set receivedEmote
   - Add EmoteOverlay component to GamePage (only in online mode)
   - Pass sendEmote callback that calls adapter's sendEmote

3. Wire timer into GamePage:
   - Add `timerRemaining` state per player
   - Handle `timerUpdate` WsAdapterEvent: update timer state
   - Display timer near the active player's HUD area (simple countdown text)
   - When timer reaches 0, the server handles the timeout — client just displays

4. Wire opponent name:
   - Read `opponentDisplayName` from multiplayerStore
   - Pass to OpponentHud as prop

5. Enhance GameOverScreen for multiplayer:
   - Accept `isOnlineMode: boolean` prop
   - When online mode:
     - Show "Back to Lobby" button instead of/in addition to "Return to Menu"
     - "Back to Lobby" navigates to `/?view=lobby` or similar (sets menu view to lobby)
     - Show game duration (track game start time when GameStarted fires, compute elapsed)
     - Show total turn count from `gameState.turn_number`
   - Replace hardcoded `winner === 0` with dynamic check using activePlayerId from multiplayerStore
   - Victory/defeat determination: `winner === activePlayerId` (not hardcoded 0)

6. Track game duration:
   - In GameProvider or gameStore, record `gameStartedAt: number` (Date.now()) when GameStarted event fires
   - Pass to GameOverScreen for duration calculation

7. Handle WsAdapterEvents in GamePage's handleWsEvent callback:
   - Add cases for `conceded`, `emoteReceived`, `timerUpdate` events
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check && pnpm test -- --run</automated>
  </verify>
  <done>
    - Concede sends message and triggers game over
    - Emotes sent/received and displayed with auto-fade
    - Timer countdown shown when active
    - Game over screen shows correct winner for both players (not hardcoded player 0)
    - Game over shows turn count, duration, and Back to Lobby button
    - TypeScript compiles, tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify in-game multiplayer UX end-to-end</name>
  <action>Complete in-game multiplayer UX: concede with confirmation, quick emotes, opponent display name, timer display, enhanced game over with lobby return</action>
  <how-to-verify>
    1. Start phase-server: `cd /Users/matt/dev/forge.rs && cargo run -p phase-server`
    2. Start frontend: `cd /Users/matt/dev/forge.rs/client && pnpm dev`
    3. Open two browser tabs, host game from Tab 1, join from Tab 2
    4. Verify opponent name appears in HUD for both players
    5. Send an emote from Tab 1 — verify it appears briefly on Tab 2
    6. Send an emote from Tab 2 — verify it appears on Tab 1
    7. Open game menu on Tab 1 — verify "Concede" option is visible
    8. Click Concede on Tab 1 — verify confirmation dialog appears
    9. Confirm concede — verify both tabs show game over (Tab 2 as winner)
    10. Verify game over screen shows turn count and has "Back to Lobby" button
    11. Click "Back to Lobby" — verify return to lobby view
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>
</tasks>

<verification>
1. `cd /Users/matt/dev/forge.rs/client && pnpm run type-check` — compiles
2. `cd /Users/matt/dev/forge.rs/client && pnpm test -- --run` — tests pass
3. `cd /Users/matt/dev/forge.rs && cargo test --all` — all Rust tests pass
4. Manual: Full multiplayer game with concede, emotes, game over flow
</verification>

<success_criteria>
- Concede flow works: dialog, server message, game over for both players
- Emotes sent and received with temporary display
- Opponent name visible during gameplay
- Game over shows correct winner, turns, duration, and lobby return
- Timer countdown displays when timer is configured
</success_criteria>

<output>
After completion, create `.planning/phases/26-polish-and-fix-multi-player-with-lobby-and-embedded-server/26-06-SUMMARY.md`
</output>
