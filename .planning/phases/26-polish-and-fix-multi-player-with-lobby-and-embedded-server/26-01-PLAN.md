---
phase: 26-polish-and-fix-multi-player-with-lobby-and-embedded-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/adapter/__tests__/ws-adapter.test.ts
  - client/src/stores/__tests__/multiplayerStore.test.ts
  - client/src/adapter/ws-adapter.ts
  - client/src/stores/multiplayerStore.ts
  - client/src/stores/gameStore.ts
  - client/src/constants/game.ts
  - client/src/hooks/usePlayerId.ts
  - client/src/pages/GamePage.tsx
  - client/src/pages/MenuPage.tsx
  - client/src/providers/GameProvider.tsx
  - client/src/components/board/ActionButton.tsx
  - client/src/components/board/PermanentCard.tsx
  - client/src/components/modal/CardChoiceModal.tsx
  - client/src/components/stack/StackEntry.tsx
  - client/src/components/targeting/TargetingOverlay.tsx
  - client/src/game/autoPass.ts
  - client/src/game/controllers/gameLoopController.ts
autonomous: true
requirements: [MP-BUG-A, MP-BUG-B, MP-BUG-C, MP-BUG-D, MP-BUG-E, MP-IDENT]

must_haves:
  truths:
    - "Starting a new online game never reconnects to a dead session from a previous game"
    - "Cannot enter an online game without selecting a deck first"
    - "Opponent actions are visible in real-time without needing to take your own action"
    - "getAiAction on WebSocketAdapter does not crash in multiplayer"
    - "Player 1 (joiner) sees and can interact with all prompts (mulligan, targeting, mana payment)"
    - "Player has a persistent UUID identity and display name stored in localStorage"
  artifacts:
    - path: "client/src/stores/multiplayerStore.ts"
      provides: "Player identity (UUID, display name), server address, connection status"
      exports: ["useMultiplayerStore"]
    - path: "client/src/adapter/ws-adapter.ts"
      provides: "Fixed WebSocket adapter with Bug A-E fixes"
      contains: "stateChanged"
    - path: "client/src/adapter/__tests__/ws-adapter.test.ts"
      provides: "Test scaffolds for Bug C (stateChanged emission), Bug D (getAiAction no-op), Bug E (activePlayerId from GameStarted)"
    - path: "client/src/stores/__tests__/multiplayerStore.test.ts"
      provides: "Test scaffolds for store identity persistence, connection status, activePlayerId"
  key_links:
    - from: "client/src/adapter/ws-adapter.ts"
      to: "client/src/stores/gameStore.ts"
      via: "stateChanged event emitted on unsolicited StateUpdate"
      pattern: "emit.*stateChanged"
    - from: "client/src/pages/GamePage.tsx"
      to: "client/src/stores/multiplayerStore.ts"
      via: "dynamic playerId read instead of PLAYER_ID constant"
      pattern: "useMultiplayerStore|playerId"
---

<objective>
Fix all 5 known multiplayer bugs (A-E), create the multiplayer store with player identity, and establish test scaffolds.

Purpose: These bugs make multiplayer unplayable — opponent actions are invisible (Bug C), joiner can't interact with prompts (Bug E), stale sessions cause broken reconnects (Bug A), missing deck validation crashes (Bug B), and missing getAiAction throws (Bug D). The multiplayerStore establishes player identity needed by all subsequent plans. Test scaffolds ensure the bug fixes are verified beyond type-checking.

Output: Working WebSocket multiplayer where both players can see each other's actions and interact with all prompts. A Zustand multiplayerStore with persistent UUID, display name, server address, and connection status. Test files for ws-adapter and multiplayerStore.

NOTE: This plan modifies 17 files, but Task 2 (PLAYER_ID replacement) is mechanical — each file gets the same 1-3 line change replacing `PLAYER_ID` imports with `usePlayerId()` or `getPlayerId()` calls. The scope is proportional to the number of consumer files, not conceptual complexity.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-polish-and-fix-multi-player-with-lobby-and-embedded-server/26-CONTEXT.md
@.planning/phases/26-polish-and-fix-multi-player-with-lobby-and-embedded-server/26-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From client/src/adapter/types.ts:
```typescript
export interface EngineAdapter {
  initialize(): Promise<void>;
  initializeGame(deckData?: unknown): Promise<GameEvent[]> | GameEvent[];
  submitAction(action: GameAction): Promise<GameEvent[]>;
  getState(): Promise<GameState>;
  getLegalActions(): Promise<GameAction[]>;
  getAiAction(difficulty: string): Promise<GameAction | null> | GameAction | null;
  restoreState(state: GameState): void;
  dispose(): void;
}
```

From client/src/adapter/ws-adapter.ts:
```typescript
export type WsAdapterEvent =
  | { type: "gameCreated"; gameCode: string }
  | { type: "waitingForOpponent" }
  | { type: "opponentDisconnected"; graceSeconds: number }
  | { type: "opponentReconnected" }
  | { type: "gameOver"; winner: PlayerId | null; reason: string }
  | { type: "error"; message: string }
  | { type: "reconnecting"; attempt: number; maxAttempts: number }
  | { type: "reconnected" }
  | { type: "reconnectFailed" };
```

From client/src/constants/game.ts:
```typescript
export const PLAYER_ID = 0;
export const AI_PLAYER_ID = 1;
```

From client/src/stores/gameStore.ts:
```typescript
// gameStore has: gameState, waitingFor, adapter, gameId
// Functions: initGame, resumeGame, reset, loadGame, saveGame
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create test scaffolds for ws-adapter bug fixes and multiplayerStore</name>
  <files>
    client/src/adapter/__tests__/ws-adapter.test.ts,
    client/src/stores/__tests__/multiplayerStore.test.ts
  </files>
  <action>
1. Create `client/src/adapter/__tests__/ws-adapter.test.ts` with failing test skeletons:
   - Test: "emits stateChanged event when StateUpdate arrives without pendingResolve" (Bug C)
     - Create a WebSocketAdapter instance (mock the WebSocket constructor via vi.fn or use a minimal mock)
     - Simulate receiving a `StateUpdate` message when no pending action exists
     - Assert the adapter emits a `stateChanged` event with the state and events
   - Test: "getAiAction returns null without throwing" (Bug D)
     - Create a WebSocketAdapter instance
     - Call `getAiAction("easy")`
     - Assert it returns null (not throws)
   - Test: "sets activePlayerId in multiplayerStore on GameStarted" (Bug E)
     - Create a WebSocketAdapter instance
     - Simulate receiving a `GameStarted` message with `your_player: 1`
     - Assert `useMultiplayerStore.getState().activePlayerId` equals 1
   - Mark all tests with `it.todo()` or `it.skip()` initially — they will fail until Task 1 implements the fixes. The executor should convert them to real tests as part of Task 1 implementation.

2. Create `client/src/stores/__tests__/multiplayerStore.test.ts` with failing test skeletons:
   - Test: "initializes with a stable UUID playerId"
     - Access `useMultiplayerStore.getState().playerId`
     - Assert it's a valid UUID string (regex: `/^[0-9a-f]{8}-/`)
     - Access again, assert same value
   - Test: "persists displayName across store resets"
     - Set displayName via `setDisplayName("TestPlayer")`
     - Assert `getState().displayName` equals "TestPlayer"
   - Test: "does not persist connectionStatus or activePlayerId"
     - Set connectionStatus to 'connected' and activePlayerId to 1
     - These are transient — just verify they're settable (persistence is tested by partialize config)
   - Test: "setActivePlayerId updates activePlayerId"
     - Call `setActivePlayerId(1)`, assert `getState().activePlayerId === 1`
     - Call `setActivePlayerId(null)`, assert `getState().activePlayerId === null`
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm test -- --run --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|skip|todo)" | head -20</automated>
  </verify>
  <done>
    - ws-adapter.test.ts exists with test skeletons for Bugs C, D, E
    - multiplayerStore.test.ts exists with test skeletons for store identity and state
    - Tests are either skipped/todo (awaiting Task 1 implementation) or fail as expected
  </done>
</task>

<task type="auto">
  <name>Task 1: Create multiplayerStore and fix ws-adapter bugs A-E</name>
  <files>
    client/src/stores/multiplayerStore.ts,
    client/src/adapter/ws-adapter.ts,
    client/src/pages/MenuPage.tsx,
    client/src/providers/GameProvider.tsx,
    client/src/adapter/__tests__/ws-adapter.test.ts,
    client/src/stores/__tests__/multiplayerStore.test.ts
  </files>
  <action>
1. Create `client/src/stores/multiplayerStore.ts` — Zustand store with `persist` middleware (storage name `"phase-multiplayer"`):
   - `playerId: string` — initialized via `crypto.randomUUID()` on first creation (stable across sessions)
   - `displayName: string` — initially empty string, user-editable
   - `serverAddress: string` — defaults to `"ws://localhost:8080/ws"`
   - `connectionStatus: 'disconnected' | 'connecting' | 'connected'` — not persisted (transient)
   - `activePlayerId: PlayerId | null` — the player index (0 or 1) assigned by the server for the current game session, not persisted
   - `opponentDisplayName: string | null` — set from GameStarted, not persisted
   - Actions: `setDisplayName`, `setServerAddress`, `setConnectionStatus`, `setActivePlayerId`, `setOpponentDisplayName`
   - Use `partialize` in persist config to exclude `connectionStatus`, `activePlayerId`, and `opponentDisplayName` from storage

2. Fix Bug A in `client/src/pages/MenuPage.tsx`:
   - In `handleHostOnlineGame()`, add `sessionStorage.removeItem("phase-ws-session")` before navigating
   - In `handleJoinSubmit()`, add `sessionStorage.removeItem("phase-ws-session")` before navigating

3. Fix Bug B in `client/src/pages/MenuPage.tsx`:
   - In both `handleHostOnlineGame` and `handleJoinSubmit`, check `activeDeckName` is not null before proceeding
   - If null, flash the deck gallery (set menuView to "deck-gallery-online") — same guard pattern as the AI path

4. Fix Bug C in `client/src/adapter/ws-adapter.ts`:
   - Add `stateChanged` to the `WsAdapterEvent` union: `| { type: "stateChanged"; state: GameState; events: GameEvent[] }`
   - In `handleMessage` case `"StateUpdate"`: after setting `this.gameState = data.state`, check if `this.pendingResolve` exists. If yes, resolve as before. If no, call `this.emit({ type: "stateChanged", state: data.state, events: data.events })` so the store can pick up unsolicited state updates from opponent actions.

5. Fix Bug D in `client/src/adapter/ws-adapter.ts`:
   - Add the missing `getAiAction` method to `WebSocketAdapter` class:
     ```
     getAiAction(_difficulty: string): GameAction | null {
       return null;
     }
     ```

6. Fix Bug E in `client/src/adapter/ws-adapter.ts`:
   - In `handleMessage` case `"GameStarted"`: after setting `this._playerId = data.your_player`, also call `useMultiplayerStore.getState().setActivePlayerId(data.your_player)` (import the store at the top of the file).
   - Also read `data.opponent_name` if present and call `useMultiplayerStore.getState().setOpponentDisplayName(data.opponent_name ?? null)`.

7. Wire `stateChanged` event in `client/src/providers/GameProvider.tsx`:
   - In the online branch of the setup effect, after creating wsAdapter and subscribing via `onEvent`, add a SECOND event listener that listens for `stateChanged` events and calls `useGameStore.getState().setGameState(event.state)` and `useGameStore.getState().setWaitingFor(event.state.waiting_for)`. Alternatively, handle this inside the existing onWsEvent callback chain — the key is that when a `stateChanged` event arrives, the Zustand gameStore must be updated.
   - NOTE: Check if `gameStore` has `setGameState`/`setWaitingFor` setters. If not, add them to `gameStore.ts` as minimal state setters.

8. Convert the test skeletons from Task 0 into real passing tests:
   - Un-skip/un-todo all tests in ws-adapter.test.ts and multiplayerStore.test.ts
   - Ensure Bug C, D, E tests pass with the actual implementation
   - Ensure multiplayerStore tests pass with the actual store
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check && pnpm test -- --run</automated>
  </verify>
  <done>
    - multiplayerStore.ts exists and exports useMultiplayerStore with persisted UUID, displayName, serverAddress
    - ws-adapter.ts has getAiAction returning null, emits stateChanged on unsolicited StateUpdate, stores activePlayerId
    - Bug A: sessionStorage cleared before new game creation
    - Bug B: deck validation guard exists for online mode
    - stateChanged events update gameStore via GameProvider
    - All test skeletons from Task 0 converted to passing tests
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded PLAYER_ID with dynamic player ID across all components</name>
  <files>
    client/src/hooks/usePlayerId.ts,
    client/src/pages/GamePage.tsx,
    client/src/components/board/ActionButton.tsx,
    client/src/components/board/PermanentCard.tsx,
    client/src/components/modal/CardChoiceModal.tsx,
    client/src/components/stack/StackEntry.tsx,
    client/src/components/targeting/TargetingOverlay.tsx,
    client/src/game/autoPass.ts,
    client/src/game/controllers/gameLoopController.ts
  </files>
  <action>
Create a helper hook or function that returns the correct player ID based on the game mode:

1. Create a `usePlayerId()` hook (can live in `client/src/hooks/usePlayerId.ts` or inline in constants/game.ts):
   - Reads `activePlayerId` from `useMultiplayerStore`
   - Falls back to `PLAYER_ID` (0) if null (AI/local mode)
   - Returns a `PlayerId` (number)

2. For non-React files (`autoPass.ts`, `gameLoopController.ts`), create a `getPlayerId()` function that reads `useMultiplayerStore.getState().activePlayerId ?? PLAYER_ID`.

3. Replace ALL `PLAYER_ID` usages in these files with the dynamic version:
   - `GamePage.tsx` (lines 399-414): 6 references — use `usePlayerId()` hook
   - `ActionButton.tsx` (lines 27, 33, 39, 229, 251): 5 references — use `usePlayerId()` hook
   - `PermanentCard.tsx` (line 51): 1 reference — use `usePlayerId()` hook
   - `CardChoiceModal.tsx` (lines 30, 33, 36): 3 references — use `usePlayerId()` hook
   - `StackEntry.tsx` (line 32): 1 reference — use `usePlayerId()` hook
   - `TargetingOverlay.tsx` (line 79): 1 reference — use `usePlayerId()` hook
   - `autoPass.ts` (lines 24, 40): 2 references — use `getPlayerId()`
   - `gameLoopController.ts` (line 37): 1 reference — use `getPlayerId()`

4. Keep `PLAYER_ID` and `AI_PLAYER_ID` constants in `constants/game.ts` as defaults — they're still the correct values for AI/local mode.

5. Do NOT change `aiController.ts` — it correctly uses `AI_PLAYER_ID` since it only runs in AI mode.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check && pnpm test -- --run</automated>
  </verify>
  <done>
    - No remaining direct `PLAYER_ID` imports in GamePage, ActionButton, PermanentCard, CardChoiceModal, StackEntry, TargetingOverlay, autoPass, gameLoopController
    - All replaced with dynamic lookup from multiplayerStore that falls back to 0 for AI/local mode
    - TypeScript compiles cleanly, existing tests still pass
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/matt/dev/forge.rs/client && pnpm run type-check` — all TypeScript compiles
2. `cd /Users/matt/dev/forge.rs/client && pnpm test -- --run` — all tests pass (including new ws-adapter and multiplayerStore tests)
3. Grep for raw `PLAYER_ID` usage in GamePage and components confirms replacement
</verification>

<success_criteria>
- All 5 bugs fixed with specific code changes
- multiplayerStore exists with persistent player identity
- Dynamic player ID replaces all hardcoded PLAYER_ID references in multiplayer-relevant components
- Test scaffolds for bugs C/D/E and store identity all pass
- Type-check and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-polish-and-fix-multi-player-with-lobby-and-embedded-server/26-01-SUMMARY.md`
</output>
