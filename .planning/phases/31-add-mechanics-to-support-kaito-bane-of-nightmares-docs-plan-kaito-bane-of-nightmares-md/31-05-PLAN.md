---
phase: 31-kaito-mechanics
plan: 05
type: execute
wave: 3
depends_on: ["31-01", "31-02", "31-03", "31-04"]
files_modified:
  - client/src/adapter/types.ts
  - client/src/components/zone/CommandZone.tsx
  - crates/engine/tests/kaito_integration.rs
autonomous: false
requirements: [K31-INT]
must_haves:
  truths:
    - "Kaito card data is fully parsed from Oracle text with no Unimplemented gaps"
    - "Kaito end-to-end integration test passes: cast via Ninjutsu, create emblem, animate as creature, Surveil + variable draw"
    - "Emblems render in command zone UI"
    - "Card data regeneration produces correct JSON for all Kaito abilities"
  artifacts:
    - path: "crates/engine/tests/kaito_integration.rs"
      provides: "Full Kaito end-to-end integration test"
      contains: "kaito"
    - path: "client/src/components/zone/CommandZone.tsx"
      provides: "Emblem rendering in command zone"
      contains: "emblem"
  key_links:
    - from: "crates/engine/tests/kaito_integration.rs"
      to: "crates/engine/src/game/keywords.rs"
      via: "Tests Ninjutsu activation"
      pattern: "ActivateNinjutsu"
    - from: "crates/engine/tests/kaito_integration.rs"
      to: "crates/engine/src/game/effects/create_emblem.rs"
      via: "Tests emblem creation from loyalty ability"
      pattern: "emblem"
---

<objective>
Integration testing and frontend wiring: regenerate card data, run full Kaito end-to-end integration test exercising all five building blocks, add emblem UI to command zone, and verify gameplay.

Purpose: Proves all building blocks work together for Kaito, Bane of Nightmares. Verifies the card data pipeline produces correct output.
Output: Passing integration test, emblem UI, gameplay verification.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-add-mechanics-to-support-kaito-bane-of-nightmares-docs-plan-kaito-bane-of-nightmares-md/31-CONTEXT.md
@.planning/phases/31-add-mechanics-to-support-kaito-bane-of-nightmares-docs-plan-kaito-bane-of-nightmares-md/31-01-SUMMARY.md
@.planning/phases/31-add-mechanics-to-support-kaito-bane-of-nightmares-docs-plan-kaito-bane-of-nightmares-md/31-02-SUMMARY.md
@.planning/phases/31-add-mechanics-to-support-kaito-bane-of-nightmares-docs-plan-kaito-bane-of-nightmares-md/31-03-SUMMARY.md
@.planning/phases/31-add-mechanics-to-support-kaito-bane-of-nightmares-docs-plan-kaito-bane-of-nightmares-md/31-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate card data + Kaito integration test</name>
  <files>crates/engine/tests/kaito_integration.rs, client/public/card-data.json</files>
  <read_first>
    - docs/plan-kaito-bane-of-nightmares.md (Oracle text, all 5 gaps)
    - crates/engine/src/game/scenario.rs (GameScenario test harness API)
    - crates/engine/tests/ (existing integration test patterns)
    - client/public/card-data.json (current Kaito entry — check via jq)
  </read_first>
  <action>
1. Regenerate card data to pick up all type changes from Plans 01-04:
   ```bash
   ./scripts/gen-card-data.sh
   ```

2. Verify Kaito's card data is fully parsed:
   ```bash
   jq '.["kaito, bane of nightmares"] | {abilities: [.abilities[]? | select(.effect.type == "Unimplemented")], triggers: [.triggers[]? | select(.mode == "Unknown")]}' client/public/card-data.json
   ```
   Expected: empty arrays (no unimplemented gaps).

3. Create `crates/engine/tests/kaito_integration.rs` with a comprehensive end-to-end test:

   **Test: kaito_ninjutsu_activation**
   - Set up: Player 0 has Kaito in hand with Ninjutsu cost available. Player 0 has an unblocked 1/1 attacker.
   - Action: Activate Ninjutsu, return the attacker, Kaito enters attacking.
   - Assert: Attacker is in hand, Kaito is on battlefield tapped and attacking.
   - Assert: Kaito has loyalty counters (enters as planeswalker with 4 loyalty).

   **Test: kaito_emblem_creation**
   - Set up: Kaito on battlefield with loyalty counters.
   - Action: Activate [+1] loyalty ability.
   - Assert: An emblem exists in command zone with "Ninjas you control get +1/+1" static.
   - Assert: A Ninja creature on the battlefield gets +1/+1.
   - Assert: A non-Ninja creature does NOT get the bonus.

   **Test: kaito_animation**
   - Set up: Kaito on battlefield with 4 loyalty, during controller's turn.
   - Assert: Kaito is a 3/4 Ninja creature with hexproof (compound condition met).
   - Action: Switch to opponent's turn.
   - Assert: Kaito is NOT a creature (condition not met — not your turn).
   - Action: Remove all loyalty counters.
   - Assert: Kaito is NOT a creature even on your turn (condition not met — no loyalty).

   **Test: kaito_surveil_and_draw**
   - Set up: Kaito on battlefield. Opponent has lost 2 life this turn. Another opponent (if multiplayer) has lost 0 life.
   - Action: Activate [0] loyalty ability.
   - Assert: Surveil 2 happens, then draw 1 card (only 1 opponent lost life in 2-player).

4. Run full test suite to confirm nothing is broken:
   ```bash
   cargo test --all
   cargo clippy --all-targets -- -D warnings
   ```
  </action>
  <verify>
    <automated>cargo test -p engine -- kaito -x && cargo test --all && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - crates/engine/tests/kaito_integration.rs exists
    - Test kaito_ninjutsu_activation passes
    - Test kaito_emblem_creation passes
    - Test kaito_animation passes
    - Test kaito_surveil_and_draw passes
    - `jq '.["kaito, bane of nightmares"].abilities[] | select(.effect.type == "Unimplemented")' client/public/card-data.json` produces empty output
    - `cargo test --all` passes
  </acceptance_criteria>
  <done>Kaito is fully playable: all 5 building blocks verified in integration tests. Card data pipeline produces correct output.</done>
</task>

<task type="auto">
  <name>Task 2: Emblem command zone UI rendering</name>
  <files>client/src/adapter/types.ts, client/src/components/zone/CommandZone.tsx</files>
  <read_first>
    - client/src/adapter/types.ts (GameObject type, Zone type, existing type definitions)
    - client/src/components/zone/ (existing zone components)
    - client/src/components/board/GameBoard.tsx (how zones are rendered, where command zone appears)
    - client/src/components/card/ (card rendering components)
  </read_first>
  <action>
1. In `client/src/adapter/types.ts`, add `is_emblem` to the GameObject type if not already present (tsify should generate this from the Rust type, but verify):
   ```typescript
   // In GameObject interface/type:
   is_emblem?: boolean;
   ```
   Also add `command_zone` to GameState if not already present:
   ```typescript
   command_zone?: ObjectId[];
   ```

2. Create or update `client/src/components/zone/CommandZone.tsx`:
   - Render emblems as mini-cards in a horizontal strip
   - Filter GameState objects to find those with `is_emblem === true`
   - Display emblem name and a brief description of its abilities
   - Style: small card-like appearance, positioned near the player's life total / command zone area
   - Use existing card component patterns for consistent look

3. Wire CommandZone into the board layout:
   - Find where commanders are rendered (if there's already a command zone strip)
   - Add emblems alongside commanders in the same strip
   - If no command zone UI exists yet, add a small row above/below the player's area

4. Verify frontend builds:
   ```bash
   cd client && pnpm run type-check && pnpm build
   ```
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - types.ts contains `is_emblem` on GameObject type
    - types.ts contains `command_zone` on GameState type (or similar)
    - A component exists that renders emblems in the command zone
    - `pnpm run type-check` passes
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>Emblems render visually in the command zone UI alongside commanders.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Kaito gameplay end-to-end</name>
  <files>none</files>
  <action>
Human verifies full Kaito gameplay. All automated work is complete in Tasks 1-2.
  </action>
  <verify>Human verification of gameplay described below.</verify>
  <done>Human confirms Kaito plays correctly: Ninjutsu, animation, emblem, surveil+draw all work as expected.</done>
  <what-built>Full Kaito, Bane of Nightmares implementation: Ninjutsu runtime, emblem infrastructure, compound conditions, animation, dynamic quantities, parser coverage, integration tests, and emblem UI</what-built>
  <how-to-verify>
    1. Start the dev environment: `cd client && pnpm dev`
    2. Build a deck with Kaito, Bane of Nightmares and some Ninja creatures
    3. Verify Ninjutsu: During combat, when you have an unblocked attacker, check that Ninjutsu activation is offered. Activate it and verify the attacker returns to hand while Kaito enters attacking.
    4. Verify animation: On your turn, with loyalty counters, Kaito should appear as a 3/4 creature. On opponent's turn, Kaito should be a planeswalker only.
    5. Verify emblem: Activate the +1 loyalty ability. Check that an emblem appears in the command zone UI. Verify Ninja creatures get +1/+1.
    6. Verify surveil+draw: Activate the 0 loyalty ability after dealing damage to opponent. Verify surveil 2 happens, then draw based on opponents who lost life.
    7. Run `cargo test --all` to confirm all tests pass.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cargo test --all` passes
- `cargo clippy --all-targets -- -D warnings` clean
- `cd client && pnpm run type-check && pnpm build` succeeds
- Kaito card data has zero Unimplemented effects
- All 5 building blocks verified in integration test
- Human verified gameplay
</verification>

<success_criteria>
- Kaito, Bane of Nightmares is fully playable with all abilities working correctly
- Integration tests cover all 5 building blocks exercised together
- Emblem UI visible in command zone
- Card data pipeline produces correct Kaito data from Oracle text
</success_criteria>

<output>
After completion, create `.planning/phases/31-add-mechanics-to-support-kaito-bane-of-nightmares-docs-plan-kaito-bane-of-nightmares-md/31-05-SUMMARY.md`
</output>
