---
phase: 25-forge-removal-relicensing
plan: 03
type: execute
wave: 3
depends_on: [25-02]
files_modified:
  - LICENSE-MIT
  - LICENSE-APACHE
  - NOTICE
  - Cargo.toml
  - crates/engine/Cargo.toml
  - crates/engine-wasm/Cargo.toml
  - crates/phase-ai/Cargo.toml
  - crates/server-core/Cargo.toml
  - crates/phase-server/Cargo.toml
  - client/src-tauri/Cargo.toml
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [LICN-01, LICN-02]

must_haves:
  truths:
    - "The project is dual-licensed under MIT/Apache-2.0 with proper license files"
    - "All Cargo.toml files declare the MIT OR Apache-2.0 license"
    - "MTGJSON's MIT license is attributed in a NOTICE file"
    - "No Forge references remain in ungated project documentation"
    - "PROJECT.md reflects MTGJSON + own ability format as the data pipeline"
  artifacts:
    - path: "LICENSE-MIT"
      provides: "MIT license text"
      contains: "MIT License"
    - path: "LICENSE-APACHE"
      provides: "Apache License 2.0 text"
      contains: "Apache License"
    - path: "NOTICE"
      provides: "Third-party attribution for MTGJSON"
      contains: "MTGJSON"
    - path: "Cargo.toml"
      provides: "Workspace-level license declaration"
      contains: "MIT OR Apache-2.0"
  key_links:
    - from: "Cargo.toml"
      to: "crates/*/Cargo.toml"
      via: "workspace.package license inheritance"
      pattern: "license.workspace = true"
    - from: "LICENSE-MIT"
      to: "Cargo.toml"
      via: "License files match Cargo.toml declaration"
      pattern: "MIT OR Apache-2.0"
---

<objective>
Create license files, update all Cargo.toml manifests, and scrub Forge references from project documentation.

Purpose: With all GPL-coupled Forge data removed (Plan 02) and Forge code feature-gated (Plan 02), the project can now be relicensed as MIT/Apache-2.0 dual license following Rust ecosystem conventions. Project documentation (CLAUDE.md, PROJECT.md) is updated to reflect the project's independence -- phase.rs stands on its own with MTGJSON + own ability format.

Output: LICENSE-MIT, LICENSE-APACHE, NOTICE files created. All 7 Cargo.toml files declare MIT OR Apache-2.0. CLAUDE.md and PROJECT.md scrubbed of Forge heritage language.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-forge-removal-relicensing/25-CONTEXT.md
@.planning/phases/25-forge-removal-relicensing/25-RESEARCH.md
@.planning/phases/25-forge-removal-relicensing/25-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create license files and update all Cargo.toml manifests</name>
  <files>
    LICENSE-MIT,
    LICENSE-APACHE,
    NOTICE,
    Cargo.toml,
    crates/engine/Cargo.toml,
    crates/engine-wasm/Cargo.toml,
    crates/phase-ai/Cargo.toml,
    crates/server-core/Cargo.toml,
    crates/phase-server/Cargo.toml,
    client/src-tauri/Cargo.toml
  </files>
  <action>
    **Step 1: Create LICENSE-MIT.**
    Standard MIT license text per Rust ecosystem convention. Copyright holder: "phase.rs contributors". Year range: 2024-2026.

    **Step 2: Create LICENSE-APACHE.**
    Full Apache License 2.0 text. Standard boilerplate from https://www.apache.org/licenses/LICENSE-2.0.

    **Step 3: Create NOTICE.**
    Apache-2.0 convention for third-party attribution:
    ```
    phase.rs
    Copyright 2024-2026 phase.rs contributors

    This project uses card data from the following sources:

    MTGJSON (https://mtgjson.com/)
    Licensed under the MIT License
    Card metadata including names, mana costs, types, oracle text, and other
    game-relevant fields is sourced from MTGJSON's open data sets.
    ```

    **Step 4: Update workspace root Cargo.toml.**
    Add a `[workspace.package]` section with the license field:
    ```toml
    [workspace.package]
    license = "MIT OR Apache-2.0"
    ```
    Place this after the `[workspace]` section and before `[workspace.dependencies]`.

    **Step 5: Update all 6 crate Cargo.toml files.**
    In each of these files, add `license.workspace = true` to the `[package]` section:
    - `crates/engine/Cargo.toml`
    - `crates/engine-wasm/Cargo.toml`
    - `crates/phase-ai/Cargo.toml`
    - `crates/server-core/Cargo.toml`
    - `crates/phase-server/Cargo.toml`
    - `client/src-tauri/Cargo.toml`

    For engine/Cargo.toml specifically, add the license line to the existing `[package]` section (which already has the `[features]` section from Plan 02).
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && test -f LICENSE-MIT && test -f LICENSE-APACHE && test -f NOTICE && rg 'license' Cargo.toml crates/*/Cargo.toml client/src-tauri/Cargo.toml 2>&1 | head -15</automated>
  </verify>
  <done>LICENSE-MIT, LICENSE-APACHE, and NOTICE files exist at repository root. All 7 Cargo.toml files declare MIT OR Apache-2.0 license via workspace inheritance.</done>
</task>

<task type="auto">
  <name>Task 2: Scrub Forge references from project documentation</name>
  <files>
    CLAUDE.md,
    .planning/PROJECT.md
  </files>
  <action>
    **Step 1: Update CLAUDE.md.**
    Read the file first, then make these targeted changes:
    - Remove "Originally derived from the open-source MTG Forge project's card definitions" from the Project Overview paragraph. Replace with language describing phase.rs as an independent project: "phase.rs is a Magic: The Gathering game engine written in Rust..."
    - Update the `parser/` description in Engine Internals: change "Parses Forge's `.txt` card definition format into typed Rust structures." to reference the ability parser for SVar/SubAbility chain resolution, noting the Forge card file parser is optional (behind `forge-compat` feature).
    - Update `database/` description to mention JSON as the primary card loading path.
    - In the Key Patterns section, update any references to Forge card format.
    - Update the CI section: remove mention of dual coverage gates, reflect the single JSON coverage gate.
    - Keep the `parser/` entry in the Engine Internals crate listing but note it's `parser/ — Ability text parser (card file parser behind forge-compat feature gate)` or similar.

    **Step 2: Update .planning/PROJECT.md.**
    Read the file first, then make these targeted changes:

    a) **"What This Is" section:** Remove "porting Forge's 32,300+ card definitions" language. Describe as: "A Rust/TypeScript Magic: The Gathering game engine with MTGJSON card data and custom ability definitions..."

    b) **Requirements > Validated list:**
    - Update the first item: change "Parse Forge's 32,300+ card definition files (.txt format) into typed Rust structures" to "Load card data from MTGJSON (MIT) metadata + typed JSON ability definitions"
    - Keep all other validated requirements as-is (they describe shipped features accurately)

    c) **Requirements > Active list:**
    - Check items off that are now complete (MTGJSON integration, own ability format, card format migration, remove Forge coupling, license change, test suite)
    - Move completed items to Validated section or mark as done

    d) **Context paragraph:**
    - Update "Card format" description from Forge .txt to MTGJSON + ability JSON
    - Update crate descriptions if they reference Forge (e.g., `forge-ai` -> `phase-ai`)
    - Check "Crate structure" line: crates were already renamed to `phase-ai`, `phase-server` -- verify accuracy

    e) **Constraints section:**
    - The "Card format" constraint already says "MTGJSON (MIT) for card metadata + own typed JSON ability format" -- verify this is accurate
    - Update "License" constraint from "Open source" to "MIT OR Apache-2.0 dual license"

    f) **Key Decisions table:**
    - Update the "Forge's .txt card format preserved" row to reflect the completed migration: "MTGJSON + typed ability JSON" with rationale about MIT licensing and typed schema
    - Add new row: "MIT/Apache-2.0 dual license" with rationale about removing GPL coupling

    g) **Current Milestone section:**
    - Update to reflect v1.2 completion status

    Brief origin mention is acceptable per user decision ("originally inspired by open-source MTG projects") but no "Forge" by name in ungated code/docs.

    **Step 3: Search for remaining Forge references in ungated code.**
    Run `rg -i 'forge' --type rust crates/ -l` to find any remaining Forge references. For files NOT behind `#[cfg(feature = "forge-compat")]`:
    - Comments mentioning Forge in production code: update to neutral language
    - Type names like `forge-ai` crate references: these are already renamed to `phase-ai`
    - Doc comments in public API: update

    NOTE: References in `.planning/` directory, git history, and code behind `forge-compat` are explicitly left as-is.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && rg -i 'forge' CLAUDE.md .planning/PROJECT.md --count-matches 2>&1 && rg -i '\bforge\b' --type rust crates/ -l --glob '!*/parser/*' --glob '!*/card_parser*' --glob '!*/migrate*' --glob '!*/parity*' 2>&1 | head -10</automated>
  </verify>
  <done>CLAUDE.md describes phase.rs as independent project without Forge heritage language. PROJECT.md constraints, key decisions, and active requirements reflect MTGJSON + own ability format. License constraint updated to MIT/Apache-2.0. No Forge references in ungated production code or documentation.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `test -f LICENSE-MIT && test -f LICENSE-APACHE && test -f NOTICE` -- all license files exist
2. `rg 'license.*=.*"MIT OR Apache-2.0"' Cargo.toml` -- workspace license defined
3. `rg 'license.workspace = true' crates/*/Cargo.toml client/src-tauri/Cargo.toml` -- all 6 crates inherit
4. `rg -i '\bforge\b' CLAUDE.md` -- zero or near-zero matches (only if referencing forge-compat feature name)
5. `cargo test --all` -- still passes (documentation changes don't break tests)
</verification>

<success_criteria>
- LICENSE-MIT file exists with MIT license text, copyright "phase.rs contributors"
- LICENSE-APACHE file exists with Apache 2.0 license text
- NOTICE file exists with MTGJSON attribution
- Workspace Cargo.toml declares `license = "MIT OR Apache-2.0"`
- All 6 crate Cargo.toml files have `license.workspace = true`
- CLAUDE.md describes phase.rs without Forge heritage language
- PROJECT.md constraints, key decisions, and requirements updated for MTGJSON + MIT/Apache-2.0
- No Forge references in ungated project documentation
</success_criteria>

<output>
After completion, create `.planning/phases/25-forge-removal-relicensing/25-03-SUMMARY.md`
</output>
