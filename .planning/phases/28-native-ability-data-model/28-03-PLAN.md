---
phase: 28-native-ability-data-model
plan: 03
type: execute
wave: 3
depends_on: [28-01, 28-02]
files_modified:
  - data/abilities/*.json
  - crates/engine/src/database/json_loader.rs
  - crates/engine/src/schema/mod.rs
  - data/abilities/schema.json
autonomous: true
requirements: [NAT-05, NAT-06]

must_haves:
  truths:
    - "All 32K data/abilities/*.json files use typed fields (no params HashMap in JSON)"
    - "json_loader builds CardFace without svars field"
    - "card-data.json exports in new typed format"
    - "cargo test --all passes including all downstream crates"
    - "schema.json reflects the new typed struct definitions"
    - "WASM builds successfully (cargo wasm)"
  artifacts:
    - path: "data/abilities/schema.json"
      provides: "Updated JSON schema for new typed format"
    - path: "crates/engine/src/database/json_loader.rs"
      provides: "JSON loader that builds CardFace from typed definitions"
  key_links:
    - from: "data/abilities/*.json"
      to: "crates/engine/src/types/ability.rs"
      via: "serde deserialization of typed definition structs"
      pattern: "TriggerDefinition|StaticDefinition|ReplacementDefinition"
    - from: "crates/engine/src/database/json_loader.rs"
      to: "crates/engine/src/types/card.rs"
      via: "build_card_face produces CardFace without svars"
      pattern: "CardFace"
---

<objective>
Run the data migration across all 32K ability JSON files, update the JSON loader, regenerate card-data.json, and validate the full pipeline.

Purpose: This plan executes the one-time migration of all ability JSON files from the old Forge-param format to the new typed format, updates the json_loader to stop setting svars, regenerates card-data.json and schema.json, and validates that the entire pipeline works end-to-end: JSON files -> json_loader -> CardFace -> GameObject -> engine runtime -> tests pass -> WASM builds.

Output: 32K migrated JSON files, updated json_loader, new card-data.json, updated schema.json
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-native-ability-data-model/28-CONTEXT.md
@.planning/phases/28-native-ability-data-model/28-RESEARCH.md
@.planning/phases/28-native-ability-data-model/28-01-SUMMARY.md
@.planning/phases/28-native-ability-data-model/28-02-SUMMARY.md

@crates/engine/src/database/json_loader.rs
@crates/engine/src/schema/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run migration binary on all 32K ability JSON files</name>
  <files>data/abilities/*.json</files>
  <action>
Run the migration binary created in Plan 01 against the full abilities directory:

```bash
cargo run --bin migrate-typed --features forge-compat -- data/abilities/
```

This converts all 32,274 JSON files from old format (with `params` HashMaps, SVars, and Forge-style keys) to new format (with typed struct fields, resolved execute chains, no params).

After running:
1. Verify the migration report shows 0 critical errors (some warnings for unresolved SVars are acceptable).
2. Spot-check 3-5 migrated files to confirm typed fields are populated and params/svars are gone.
3. Verify a sample ETB trigger card (e.g., `data/abilities/elvish_visionary.json`) has `"execute": { ... typed AbilityDefinition ... }` instead of `"params": { "Execute": "TrigDraw" }`.
4. Verify a sample lord card (e.g., `data/abilities/glorious_anthem.json`) has `"add_power": 1, "add_toughness": 1, "affected": "Creature.YouCtrl"` instead of `"params": { "AddPower": "1" }`.

If the migration binary doesn't exist yet from Plan 01, or if the typed format differs from what Plan 02 expects, adjust the migration to produce JSON that matches the final struct definitions (no `params` field at all in the JSON output -- `#[serde(skip_serializing_if = "HashMap::is_empty")]` or remove the field entirely).
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && python3 -c "
import json, glob, random
files = glob.glob('data/abilities/*.json')
print(f'Total files: {len(files)}')
sample = random.sample(files, min(100, len(files)))
violations = 0
for f in sample:
    data = json.load(open(f))
    for t in data.get('triggers', []):
        if 'params' in t and t['params']:
            violations += 1
    for s in data.get('statics', []):
        if 'params' in s and s['params']:
            violations += 1
    for r in data.get('replacements', []):
        if 'params' in r and r['params']:
            violations += 1
print(f'Sampled: {len(sample)} files, params violations: {violations}')
assert violations == 0, f'{violations} files still have non-empty params in definitions'
print('PASS: No params found in sampled trigger/static/replacement definitions')
"</automated>
  </verify>
  <done>All 32K JSON files migrated to typed format. No params HashMap in trigger/static/replacement definitions. SVars resolved into execute fields. Verified by sampling 100 random files with zero violations.</done>
</task>

<task type="auto">
  <name>Task 2: Update json_loader, regenerate card-data.json and schema, validate full suite</name>
  <files>crates/engine/src/database/json_loader.rs, crates/engine/src/schema/mod.rs, data/abilities/schema.json</files>
  <action>
**`crates/engine/src/database/json_loader.rs`:**
- Remove `svars: HashMap::new()` from `build_card_face()` -- the CardFace struct no longer has an svars field (removed in Plan 02).
- If there were any svars-related processing in the loader (e.g., reading svars from the ability file and setting them on CardFace), remove it entirely.
- Ensure `synthesize_basic_land_mana()` and `synthesize_equip()` still work -- they create AbilityDefinition values. Update their construction to not include `remaining_params` (removed in Plan 02). They should set typed condition fields if needed, or rely on defaults.
- Fix all test helpers in json_loader tests that construct CardFace with `svars: HashMap::new()` -- remove the svars field.

**`crates/engine/src/schema/mod.rs`:**
- Run `cargo test -p engine -- schema_generation` to regenerate `data/abilities/schema.json` via the schemars-based test. The schema should now reflect typed fields on all definition structs.
- Verify schema.json no longer has `params` as a required or optional field on definition types.

**Regenerate card-data.json:**
- If the project has a card-data-export binary, run it: `cargo run --bin card-data-export --features forge-compat -- data/`
- If the export is done via `gen-card-data.sh`, run that script.
- Verify the output `data/card-data.json` (or `client/public/card-data.json`) uses the new format.

**Validate the full test suite:**
- `cargo test --all` -- all Rust tests pass (engine, phase-ai, engine-wasm, server-core, phase-server)
- `cargo clippy --all-targets -- -D warnings` -- no lint warnings
- `cargo fmt --all -- --check` -- formatting correct
- Check WASM compilation: `cargo build --target wasm32-unknown-unknown -p engine-wasm` (or `cargo wasm`)
- Run coverage report: `cargo run --bin coverage-report -- data/ --ci` to verify 100% Standard coverage preserved

**If any downstream crate fails:**
- `phase-ai`: may reference params or svars in evaluation code -- fix any remaining HashMap access
- `engine-wasm`: thin wrapper, should just need recompile
- `server-core` / `phase-server`: use `CardDatabase::from_export()` which deserializes CardFace -- if CardFace shape changed (no svars), the serialized card-data.json must match
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test --all 2>&1 | tail -5</automated>
  </verify>
  <done>json_loader updated, card-data.json regenerated in new format, schema.json updated, cargo test --all passes, WASM builds, coverage report passes.</done>
</task>

</tasks>

<verification>
- `cargo test --all` passes
- `cargo clippy --all-targets -- -D warnings` passes
- `cargo build --target wasm32-unknown-unknown -p engine-wasm` succeeds
- `data/abilities/schema.json` reflects typed fields, no params
- No `"params"` key in any `data/abilities/*.json` trigger/static/replacement definition
- `data/card-data.json` is in new format
</verification>

<success_criteria>
- All 32K ability JSON files in native typed format
- card-data.json uses new format and loads correctly via from_export()
- schema.json generated from new struct definitions
- cargo test --all passes
- WASM build succeeds
- Coverage report preserves 100% Standard coverage
</success_criteria>

<output>
After completion, create `.planning/phases/28-native-ability-data-model/28-03-SUMMARY.md`
</output>
