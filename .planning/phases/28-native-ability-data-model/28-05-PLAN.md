---
phase: 28-native-ability-data-model
plan: 05
type: execute
wave: 4
depends_on: ["28-04"]
files_modified:
  - client/src/adapter/types.ts
  - crates/engine-wasm/src/lib.rs
  - client/src/wasm/engine_wasm.d.ts
autonomous: false
requirements: [NAT-01, NAT-02, NAT-03, NAT-04, NAT-05, NAT-06]

must_haves:
  truths:
    - "Frontend TypeScript types match the new Rust type shapes"
    - "WASM boundary serializes all new types correctly"
    - "pnpm run type-check passes"
    - "cargo test --all passes"
    - "cargo clippy --all-targets -- -D warnings passes"
    - "pnpm test passes"
  artifacts:
    - path: "client/src/adapter/types.ts"
      provides: "Updated TS types matching new Rust data model"
    - path: "crates/engine-wasm/src/lib.rs"
      provides: "WASM bindings with new types"
  key_links:
    - from: "client/src/adapter/types.ts"
      to: "crates/engine/src/types/ability.rs"
      via: "tsify auto-generation from Rust types"
      pattern: "TargetFilter|ContinuousModification|Duration|PtValue"
---

<objective>
Update the frontend TypeScript types and WASM boundary to match the new Rust data model, rebuild WASM, and verify the full CI pipeline (Rust tests + frontend lint/type-check/test + WASM build).

Purpose: Complete the data model migration across the full stack. After this plan, the entire system — engine types, runtime, data files, WASM bridge, and frontend — uses the native typed data model with zero HashMap<String, String> anywhere.
Output: Updated TypeScript types, WASM rebuild, full CI green.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-native-ability-data-model/28-CONTEXT.md
@.planning/phases/28-native-ability-data-model/28-01-SUMMARY.md
@.planning/phases/28-native-ability-data-model/28-04-SUMMARY.md

@client/src/adapter/types.ts
@crates/engine-wasm/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update frontend types and rebuild WASM</name>
  <files>client/src/adapter/types.ts, crates/engine-wasm/src/lib.rs</files>
  <action>
**Step 1: Rebuild WASM** to generate new TypeScript type definitions:
```bash
./scripts/build-wasm.sh debug
```
This will compile the engine with tsify and produce updated `.d.ts` files in `client/src/wasm/`.

**Step 2: Update `client/src/adapter/types.ts`** based on compilation errors from the new types:

Key breaking changes to address:
- **Remove `svars: Record<string, string>`** from any GameObject type definition (approximately line 101). The field no longer exists on the Rust side.
- **`keywords` type change**: Was `string[]`, now a typed Keyword union from tsify. Update any code that constructs or reads keywords as strings to use the typed format.
- **`power`/`toughness` type change**: Was `string | null`, now `PtValue | null` (where PtValue is `{ type: "Fixed", value: number } | { type: "Variable", value: string }`). Update any code that reads P/T.
- **Effect variants change shape**: If the frontend inspects Effect objects (for logging, animation, etc.), update the discriminated union types.
- **TargetSpec -> TargetFilter**: If the frontend references TargetSpec anywhere, update to TargetFilter.

**Step 3: Fix TypeScript compilation errors:**
Run `pnpm run type-check` from client/ and fix any errors. Typical fixes:
- Remove references to `.svars` on game objects
- Update keyword comparison code (if any component checks `keywords.includes("Flying")` it needs to match the typed Keyword shape)
- Update P/T display code to handle PtValue objects instead of strings

**Step 4: Update test mocks** in client test files:
Any test that constructs mock game state objects with `svars: {}` must remove that field. Any mock with `keywords: ["Flying"]` must use the typed format.

**Step 5: Run frontend tests:**
```bash
cd client && pnpm test -- --run
```

**Step 6: Run full lint + type-check:**
```bash
cd client && pnpm lint && pnpm run type-check
```
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check 2>&1 | tail -10 && pnpm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <done>Frontend TypeScript types match new Rust data model. No svars references. Keywords and P/T use typed representations. type-check and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Full CI verification and invariant checks</name>
  <files></files>
  <action>
Run the complete CI pipeline locally to verify everything passes:

**Rust:**
```bash
cargo fmt --all -- --check
cargo clippy --all-targets -- -D warnings
cargo test --all
```

**WASM build:**
```bash
./scripts/build-wasm.sh
```

**Frontend:**
```bash
cd client && pnpm lint && pnpm run type-check && pnpm test -- --run && pnpm build
```

Fix any remaining issues. Common final problems:
- clippy warnings from new code (unused variables, unnecessary clones)
- Format issues from generated/modified code
- Any test that was missed in earlier plans

After all checks pass, verify the key invariants:
```bash
grep -r "HashMap<String, String>" crates/engine/src/types/ | grep -v "test" | grep -v "counters"
grep -r "remaining_params" crates/engine/src/types/
grep -r "Effect::Other" crates/engine/src/ | grep -v "forge-compat" | grep -v "target"
grep -r "\.svars" crates/engine/src/game/ | grep -v "test" | grep -v "#\[cfg"
```
All four should return empty (zero HashMap in types, zero remaining_params, zero Effect::Other in non-gated code, zero svars access).
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test --all 2>&1 | tail -10 && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>Full CI pipeline passes: cargo fmt, clippy, test --all, WASM build, pnpm lint, type-check, test, build. Zero HashMap in type definitions, zero Effect::Other, zero svars, zero remaining_params, zero parse_ability() at runtime.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify game plays correctly with new data model</name>
  <files></files>
  <action>
Human verification checkpoint. Play a game to confirm the new typed data model works end-to-end.

Steps:
1. Run `pnpm dev` in client/ and start a game against AI
2. Play several turns — cast creatures, use abilities, trigger effects
3. Verify cards behave correctly (damage resolves, abilities fire, combat works)
4. Check the browser console for any TypeScript errors or WASM failures
5. Open data/abilities/benalish_marshal.json and verify it uses typed modifications instead of params HashMap
6. Run `cargo test --all` and confirm all tests pass
  </action>
  <verify>Human approval — type "approved" or describe issues</verify>
  <done>Game plays correctly with new typed data model. User approved.</done>
</task>

</tasks>

<verification>
After this plan:
- `cargo fmt --all -- --check` passes
- `cargo clippy --all-targets -- -D warnings` passes
- `cargo test --all` passes
- `./scripts/build-wasm.sh` succeeds
- `cd client && pnpm build` succeeds
- `cd client && pnpm run type-check` passes
- `cd client && pnpm test -- --run` passes
- Zero `HashMap<String, String>` in types/ (except counters)
- Zero `remaining_params` anywhere
- Zero `Effect::Other` in non-forge-compat code
- Zero `svars` access in game/ runtime
- Zero `parse_ability()` calls outside forge-compat
</verification>

<success_criteria>
- Full CI pipeline green (Rust + WASM + Frontend)
- Game playable in browser with new data model
- All Phase 28 success criteria met:
  1. TriggerDefinition, StaticDefinition, ReplacementDefinition use typed struct fields
  2. svars eliminated from CardFace and GameObject
  3. remaining_params removed from AbilityDefinition
  4. TargetSpec replaced with typed TargetFilter
  5. parse_ability() gated behind forge-compat
  6. All 32K JSON files migrated to native typed schema
  7. cargo test --all passes and card-data.json uses new format
</success_criteria>

<output>
After completion, create `.planning/phases/28-native-ability-data-model/28-05-SUMMARY.md`
</output>
