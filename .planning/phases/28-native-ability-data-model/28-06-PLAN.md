---
phase: 28-native-ability-data-model
plan: 06
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - crates/engine/src/game/effects/token.rs
  - crates/engine/src/game/effects/animate.rs
  - crates/engine/src/game/effects/change_zone.rs
  - crates/engine/src/game/effects/bounce.rs
  - crates/engine/src/game/effects/dig.rs
  - crates/engine/src/game/engine.rs
  - crates/engine/src/game/planeswalker.rs
  - crates/engine/src/game/mana_abilities.rs
  - crates/engine/src/game/coverage.rs
  - crates/engine/src/game/targeting.rs
autonomous: true
requirements: [NAT-02, NAT-04]

must_haves:
  truths:
    - "All 56 Effect::Other test usages across 22 files replaced with typed Effect variants"
    - "Effect handler files (token, animate, change_zone, bounce, dig) read typed fields (Zone, ManaColor, Keyword) instead of Strings"
    - "All test ResolvedAbility construction uses no params or svars HashMap"
    - "Targeting module uses TargetFilter instead of TargetSpec"
  artifacts:
    - path: "crates/engine/src/game/effects/token.rs"
      provides: "Token handler reading Vec<ManaColor> and Vec<Keyword> instead of Vec<String>"
    - path: "crates/engine/src/game/effects/change_zone.rs"
      provides: "ChangeZone handler reading Option<Zone> instead of String"
    - path: "crates/engine/src/game/targeting.rs"
      provides: "Targeting using TargetFilter instead of TargetSpec"
  key_links:
    - from: "crates/engine/src/game/effects/token.rs"
      to: "crates/engine/src/types/ability.rs"
      via: "Effect::Token typed fields"
      pattern: "ManaColor|Keyword"
    - from: "crates/engine/src/game/targeting.rs"
      to: "crates/engine/src/types/ability.rs"
      via: "TargetFilter enum"
      pattern: "TargetFilter::"
---

<objective>
Update all remaining effect handler files and rewrite all 56 Effect::Other test usages across the engine test suite to use typed Effect variants. This is the bulk file update split from Plan 03 to stay within context budget.

Purpose: Complete the Effect variant typing across all handler and test files. After this plan, every effect handler reads typed fields and every test constructs typed Effect variants directly.
Output: Updated token.rs, animate.rs, change_zone.rs, bounce.rs, dig.rs, engine.rs, planeswalker.rs, mana_abilities.rs, coverage.rs, targeting.rs — all with typed Effect construction and zero Effect::Other usage.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-native-ability-data-model/28-CONTEXT.md
@.planning/phases/28-native-ability-data-model/28-RESEARCH.md
@.planning/phases/28-native-ability-data-model/28-01-SUMMARY.md

@crates/engine/src/game/effects/token.rs
@crates/engine/src/game/effects/animate.rs
@crates/engine/src/game/effects/change_zone.rs
@crates/engine/src/game/effects/bounce.rs
@crates/engine/src/game/effects/dig.rs
@crates/engine/src/game/engine.rs
@crates/engine/src/game/targeting.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update effect handler files for typed fields</name>
  <files>crates/engine/src/game/effects/token.rs, crates/engine/src/game/effects/animate.rs, crates/engine/src/game/effects/change_zone.rs, crates/engine/src/game/effects/bounce.rs, crates/engine/src/game/effects/dig.rs, crates/engine/src/game/targeting.rs</files>
  <action>
**effects/token.rs**: Update to handle `colors: Vec<ManaColor>` and `keywords: Vec<Keyword>` instead of `Vec<String>`. The token creation logic that parses color strings becomes direct enum use. Update any test code in this file.

**effects/animate.rs**: Update any params access to use typed Effect fields. Update test code.

**effects/change_zone.rs**: Update `origin: String, destination: String` to `origin: Option<Zone>, destination: Zone`. Zone matching becomes pattern matching on Zone enum variants instead of string comparison. Update test code.

**effects/bounce.rs**: Update `destination: String` to `destination: Option<Zone>`. Update test code.

**effects/dig.rs**: Update `destination: String` to `destination: Option<Zone>`. Update test code.

**targeting.rs**: Update any TargetSpec references to TargetFilter. The targeting system that validates targets should use `matches_target_filter` from filter.rs instead of string-based filter matching.

For each file, also rewrite any test code that constructs `Effect::Other { api_type: "...".into(), params: HashMap::new() }` to use the correct typed Effect variant.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine game::effects 2>&1 | tail -20</automated>
  </verify>
  <done>All effect handlers compile with typed Effect variant fields (Zone, ManaColor, Keyword). Targeting uses TargetFilter. Tests in handler files use typed Effect construction.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite all remaining Effect::Other test usages across engine test suite</name>
  <files>crates/engine/src/game/engine.rs, crates/engine/src/game/planeswalker.rs, crates/engine/src/game/mana_abilities.rs, crates/engine/src/game/coverage.rs</files>
  <action>
Replace ALL remaining `Effect::Other` test usages with typed Effect variants across the engine test suite. The RESEARCH.md quantitative analysis lists 56 usages across 22 files. Tasks 1 (above) and Plan 03 handle handler-specific files. This task covers the remaining cross-cutting test files:

**engine.rs** (5 usages): Integration test helpers that construct `Effect::Other { api_type: "GainLife".into(), .. }` etc. Replace each with the correct typed variant:
- `Effect::Other { api_type: "GainLife" }` -> `Effect::GainLife { amount: N }`
- `Effect::Other { api_type: "DealDamage" }` -> `Effect::DealDamage { amount: DamageAmount::Fixed(N), target: TargetFilter::Any }`
- etc. (match each api_type string to its corresponding typed Effect variant)

**planeswalker.rs** (1 usage): Update test code using parse_ability() — rewrite to construct typed abilities directly.

**mana_abilities.rs** (1 usage): Update test code using parse_ability().

**coverage.rs**: Update parse_ability() usage. Coverage analysis reads card data to check effect support — if it used parse_ability() on SVars, rewrite to check the typed effect field instead. Since coverage.rs is behind forge-compat feature, the parse_ability usage there may need to be feature-gated.

Also replace every `ResolvedAbility::from_raw(api_type, params)` across all test files with:
```rust
ResolvedAbility {
    effect: typed_effect,
    targets: vec![],
    source_id,
    controller,
    sub_ability: None,
}
```

Search for any remaining `Effect::Other` or `from_raw` across the entire `crates/engine/src/game/` directory and fix them. After this task, `grep -r "Effect::Other" crates/engine/src/game/` should return nothing (except possibly coverage.rs behind forge-compat).
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine 2>&1 | tail -30</automated>
  </verify>
  <done>All 56 Effect::Other test usages replaced with typed variants across the entire engine test suite. Zero `Effect::Other` in non-forge-compat code. Zero `from_raw` calls. `cargo test -p engine` passes.</done>
</task>

</tasks>

<verification>
After this plan:
- `grep -r "Effect::Other" crates/engine/src/game/` returns nothing (or only coverage.rs behind forge-compat)
- `grep -r "from_raw" crates/engine/src/game/` returns nothing
- `cargo test -p engine` passes — all effect handler tests and engine integration tests green
- All effect handler files compile with typed Effect variant fields
</verification>

<success_criteria>
- All 56 Effect::Other test usages replaced with typed Effect variants
- Effect handlers (token, animate, change_zone, bounce, dig) read typed fields
- Targeting module uses TargetFilter
- Zero Effect::Other in non-forge-compat code
- Zero from_raw calls
- `cargo test -p engine` passes
</success_criteria>

<output>
After completion, create `.planning/phases/28-native-ability-data-model/28-06-SUMMARY.md`
</output>
