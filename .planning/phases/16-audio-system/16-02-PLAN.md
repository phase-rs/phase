---
phase: 16-audio-system
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - client/src/game/dispatch.ts
  - client/src/providers/GameProvider.tsx
  - client/src/pages/MenuPage.tsx
autonomous: true
requirements: [AUDIO-01, AUDIO-02, AUDIO-05]

must_haves:
  truths:
    - "SFX fire during animation step playback in the dispatch pipeline"
    - "SFX still fire when animation speed is Instant (multiplier = 0)"
    - "Music auto-starts when game initializes if not muted"
    - "Music fades out on GameOver event"
    - "AudioContext is warmed up before GamePage mounts via MenuPage interaction"
  artifacts:
    - path: "client/src/game/dispatch.ts"
      provides: "SFX firing during step processing, instant-speed SFX, GameOver music fade"
      contains: "fireSfxForStep"
    - path: "client/src/providers/GameProvider.tsx"
      provides: "Music auto-start on game initialization"
      contains: "audioManager.startMusic"
    - path: "client/src/pages/MenuPage.tsx"
      provides: "AudioContext warm-up on first user interaction"
      contains: "initAudioOnInteraction"
  key_links:
    - from: "client/src/game/dispatch.ts"
      to: "client/src/audio/AudioManager.ts"
      via: "audioManager.playSfxForStep() call during step processing"
      pattern: "audioManager\\.playSfxForStep"
    - from: "client/src/providers/GameProvider.tsx"
      to: "client/src/audio/AudioManager.ts"
      via: "audioManager.startMusic() on game init, audioManager.stopMusic() on cleanup"
      pattern: "audioManager\\.(startMusic|stopMusic)"
    - from: "client/src/pages/MenuPage.tsx"
      to: "client/src/audio/AudioManager.ts"
      via: "initAudioOnInteraction() call on mount"
      pattern: "initAudioOnInteraction"
---

<objective>
Wire AudioManager into the dispatch pipeline for SFX playback and into GameProvider/MenuPage for music and AudioContext lifecycle.

Purpose: Connect the audio foundation (Plan 01) to the game engine so sounds actually play during gameplay. SFX fire on animation steps, music starts on game init, and AudioContext warms up from the menu page.

Output: SFX in dispatch.ts, music in GameProvider, warm-up in MenuPage
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-audio-system/16-CONTEXT.md
@.planning/phases/16-audio-system/16-RESEARCH.md
@.planning/phases/16-audio-system/16-01-SUMMARY.md

@client/src/game/dispatch.ts
@client/src/providers/GameProvider.tsx
@client/src/pages/MenuPage.tsx
@client/src/animation/types.ts

<interfaces>
<!-- From Plan 01 outputs -->

From client/src/audio/AudioManager.ts:
```typescript
class AudioManager {
  warmUp(): void;
  preloadSfx(): Promise<void>;
  playSfx(eventType: string, volume?: number): void;
  playSfxForStep(effects: StepEffect[]): void;
  startMusic(): void;
  stopMusic(fadeOut?: number): void;
  updateVolumes(): void;
  dispose(): void;
}
export const audioManager: AudioManager;
export function initAudioOnInteraction(): void;
```

From client/src/audio/sfxMap.ts:
```typescript
export const SFX_MAP: Record<string, string>;
export const MUSIC_TRACKS: string[];
```

From client/src/animation/types.ts:
```typescript
export interface StepEffect {
  type: string;
  data: unknown;
  duration: number;
}
export interface AnimationStep {
  effects: StepEffect[];
  duration: number;
}
export const SPEED_MULTIPLIERS: Record<AnimationSpeed, number>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire SFX into dispatch pipeline</name>
  <files>client/src/game/dispatch.ts</files>
  <action>
Modify `client/src/game/dispatch.ts`:
- Import `audioManager` from `../audio/AudioManager`.
- In `processAction()`, after step 4 (normalize events into animation steps), add SFX firing logic:

For the **animated path** (multiplier > 0, steps.length > 0):
- Before the `await new Promise` timeout, iterate over each step and call `audioManager.playSfxForStep(step.effects)` at the appropriate time offset. Use setTimeout to schedule each step's SFX at its cumulative offset (sum of prior step durations * multiplier). This ensures SFX sync with the visual animation.

For the **instant path** (multiplier === 0 OR steps.length === 0, but steps exist):
- Fire SFX for ALL steps immediately in a single pass: `steps.forEach(step => audioManager.playSfxForStep(step.effects))`. Per locked decision, SFX still play when animation speed is Instant.

Additionally, after step 6 (update game state), check if events contain a GameOver event. If so, call `audioManager.stopMusic(2.0)` to fade out music over 2 seconds. Also check if the GameOver winner is not the human player (player 0) and play the "lose_duel" SFX instead of "win_duel" — do this by calling `audioManager.playSfx("GameOver")` but with a separate check: if `events.some(e => e.type === "GameOver" && e.data.winner !== 0)`, call `audioManager.playSfx("GameOverLoss")` — but since GameOver SFX mapping is "win_duel" in SFX_MAP, handle win/loss distinction by: checking GameOver event data.winner in dispatch. If winner === 0, play "win_duel" SFX directly. If winner !== 0, play "lose_duel" SFX directly. Both via `audioManager.playSfx` but bypassing SFX_MAP for this one case (add a method or just call with the raw filename). SIMPLER approach: just use SFX_MAP's "win_duel" for all GameOver (Claude's discretion per CONTEXT.md — game-over music behavior). The music fade-out is the important part.

Keep the existing flow unchanged. Do not modify the mutex, queue, or state update logic.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check</automated>
  </verify>
  <done>dispatch.ts fires SFX for each animation step (synced with visual timing), fires SFX immediately when animation speed is Instant, and fades out music on GameOver. No changes to existing dispatch flow.</done>
</task>

<task type="auto">
  <name>Task 2: Music auto-start in GameProvider and AudioContext warm-up in MenuPage</name>
  <files>client/src/providers/GameProvider.tsx, client/src/pages/MenuPage.tsx</files>
  <action>
Modify `client/src/pages/MenuPage.tsx`:
- Import `initAudioOnInteraction` from `../audio/AudioManager`.
- Add a `useEffect` at the top of the MenuPage component (before any other effects) that calls `initAudioOnInteraction()` on mount. No cleanup needed (the function itself is idempotent — it only attaches listeners if AudioContext hasn't been created yet).
- This ensures AudioContext is warmed up by any user click/tap/key on the menu page, so by the time GamePage mounts, audio is ready.

Modify `client/src/providers/GameProvider.tsx`:
- Import `audioManager` from `../audio/AudioManager`.
- In the `useEffect` that initializes the game, after the game is successfully initialized (after `initGame().then(...)` resolves for both AI/local and online modes), call `audioManager.startMusic()`.
- In the cleanup function (return () => ...), call `audioManager.stopMusic(0)` (immediate stop, no fade — user is leaving the page). This ensures music stops when navigating away from the game.
- For the resume path (savedState exists), also call `audioManager.startMusic()` after `resumeGame().then(...)` resolves.
- Do NOT start music during the waiting-for-opponent phase for online games. Start it after `onReadyRef.current?.()` (when the game actually begins).
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check</automated>
  </verify>
  <done>MenuPage warms up AudioContext on first user interaction. GameProvider starts music on game init and stops it on cleanup/unmount. Music does not start until game actually begins (not during opponent wait).</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `cd client && pnpm run type-check`
- Full test suite green: `cd client && pnpm test -- --run`
- ESLint clean: `cd client && pnpm lint`
</verification>

<success_criteria>
- SFX fire during animation step processing in dispatch.ts, synced with visual timing
- SFX fire immediately when animation speed is Instant
- Music auto-starts on game initialization, stops on GamePage unmount
- Music fades out on GameOver
- AudioContext warms up from MenuPage on first user interaction
- No regressions in existing dispatch, GameProvider, or MenuPage behavior
</success_criteria>

<output>
After completion, create `.planning/phases/16-audio-system/16-02-SUMMARY.md`
</output>
