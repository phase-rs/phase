---
phase: 16-audio-system
plan: 03
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - client/src/components/hud/PlayerHud.tsx
  - client/src/components/settings/PreferencesModal.tsx
autonomous: true
requirements: [AUDIO-04]

must_haves:
  truths:
    - "Player sees a speaker icon in the HUD that toggles master mute on click"
    - "Speaker icon visually reflects muted state (e.g. slashed speaker when muted)"
    - "PreferencesModal has an Audio section with SFX volume slider and Music volume slider"
    - "Each slider has its own mute toggle checkbox"
    - "Volume sliders range from 0-100 and update preferences store in real time"
  artifacts:
    - path: "client/src/components/hud/PlayerHud.tsx"
      provides: "Master mute speaker icon button in the HUD"
      contains: "masterMuted"
    - path: "client/src/components/settings/PreferencesModal.tsx"
      provides: "Audio section with SFX/Music volume sliders and mute toggles"
      contains: "sfxVolume"
  key_links:
    - from: "client/src/components/hud/PlayerHud.tsx"
      to: "client/src/stores/preferencesStore.ts"
      via: "masterMuted state + setMasterMuted action"
      pattern: "setMasterMuted"
    - from: "client/src/components/settings/PreferencesModal.tsx"
      to: "client/src/stores/preferencesStore.ts"
      via: "sfxVolume/musicVolume/sfxMuted/musicMuted state + setters"
      pattern: "sfxVolume|musicVolume|sfxMuted|musicMuted"
---

<objective>
Add audio UI controls: master mute toggle in the player HUD and independent SFX/Music volume sliders with mute toggles in the PreferencesModal.

Purpose: Give the player control over audio volume and muting during gameplay. The HUD icon provides quick access to mute everything; the PreferencesModal provides fine-grained control.

Output: Updated PlayerHud with speaker icon, updated PreferencesModal with Audio section
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-audio-system/16-CONTEXT.md
@.planning/phases/16-audio-system/16-01-SUMMARY.md

@client/src/components/hud/PlayerHud.tsx
@client/src/components/settings/PreferencesModal.tsx
@client/src/stores/preferencesStore.ts

<interfaces>
<!-- From Plan 01 outputs -->

From client/src/stores/preferencesStore.ts (extended):
```typescript
interface PreferencesState {
  // ... existing fields ...
  sfxVolume: number;        // 0-100, default 70
  musicVolume: number;      // 0-100, default 40
  sfxMuted: boolean;        // default false
  musicMuted: boolean;      // default false
  masterMuted: boolean;     // default false
}

interface PreferencesActions {
  // ... existing setters ...
  setSfxVolume: (vol: number) => void;
  setMusicVolume: (vol: number) => void;
  setSfxMuted: (muted: boolean) => void;
  setMusicMuted: (muted: boolean) => void;
  setMasterMuted: (muted: boolean) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add master mute speaker icon to PlayerHud</name>
  <files>client/src/components/hud/PlayerHud.tsx</files>
  <action>
Modify `client/src/components/hud/PlayerHud.tsx`:
- Import `masterMuted` and `setMasterMuted` from preferencesStore using existing selector pattern.
- Add a speaker icon button to the left of the existing settings gear button in the HUD bar.
- The button toggles `masterMuted` on click via `setMasterMuted(!masterMuted)`.
- Use two inline SVG icons (no external icon library):
  - When NOT muted: speaker with sound waves (Heroicons `SpeakerWaveIcon` path — a 20x20 viewBox speaker icon)
  - When muted: speaker with X/slash (Heroicons `SpeakerXMarkIcon` path — a 20x20 viewBox muted speaker icon)
- Style: same as the existing settings button (`rounded p-1 text-gray-500 transition-colors hover:bg-white/10 hover:text-gray-300`). When muted, use `text-red-400` instead of `text-gray-500` for visual feedback.
- aria-label: "Mute audio" when unmuted, "Unmute audio" when muted.
- Keep icon size at h-4 w-4 to match the settings gear.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check</automated>
  </verify>
  <done>PlayerHud displays a speaker icon button that toggles master mute. Icon changes appearance between speaker-with-waves (unmuted) and speaker-with-X (muted). Icon turns red when muted.</done>
</task>

<task type="auto">
  <name>Task 2: Add Audio section to PreferencesModal</name>
  <files>client/src/components/settings/PreferencesModal.tsx</files>
  <action>
Modify `client/src/components/settings/PreferencesModal.tsx`:
- Import the new audio preference selectors and setters from preferencesStore: sfxVolume, musicVolume, sfxMuted, musicMuted, setSfxVolume, setMusicVolume, setSfxMuted, setMusicMuted.
- Add an "Audio" section between the "Animation Speed" section and the end of the settings list. Use a visual divider (thin horizontal line, `border-t border-gray-700 pt-4`) before the Audio section to separate it from the visual settings above.
- The Audio section contains:

1. **SFX Volume** — a `SettingGroup` with label "SFX Volume" containing:
   - A horizontal flex row with:
     - A small mute toggle checkbox (styled as a small button with speaker icon or checkbox). Use a simple checkbox input with a label "Mute". When checked, sets `setSfxMuted(true)`.
     - A range input slider (min=0, max=100, value=sfxVolume). On change, calls `setSfxVolume(Number(e.target.value))`.
     - A text label showing the current value (e.g., "70%").
   - When sfxMuted is true, the slider should appear dimmed (opacity-50) but still interactive.

2. **Music Volume** — same pattern as SFX Volume but with musicVolume/musicMuted/setMusicVolume/setMusicMuted. Label "Music Volume".

Style the range sliders to match the dark theme:
- Use Tailwind's `accent-cyan-500` on the range input for the slider thumb color.
- Full width within the SettingGroup.
- The percentage label uses `text-xs text-gray-400 w-10 text-right` for consistent width.
- The mute checkbox uses `accent-cyan-500` styling, with label text `text-xs text-gray-400`.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check</automated>
  </verify>
  <done>PreferencesModal has an Audio section with SFX Volume slider (0-100, mute toggle) and Music Volume slider (0-100, mute toggle). Sliders update preferences in real time. Section visually separated from other settings.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `cd client && pnpm run type-check`
- Full test suite green: `cd client && pnpm test -- --run`
- ESLint clean: `cd client && pnpm lint`
</verification>

<success_criteria>
- PlayerHud has a speaker icon that toggles master mute (both SFX and music)
- Speaker icon visually changes between unmuted and muted states
- PreferencesModal has Audio section with independent SFX and Music volume sliders
- Each slider has a mute toggle
- All controls update preferencesStore in real time (which triggers AudioManager volume updates via the store subscription set up in Plan 01)
- No visual regressions to existing HUD or PreferencesModal
</success_criteria>

<output>
After completion, create `.planning/phases/16-audio-system/16-03-SUMMARY.md`
</output>
