---
phase: 20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics
plan: 05
type: execute
wave: 2
depends_on: [20-01, 20-02]
files_modified:
  - crates/engine/src/game/transform.rs
  - crates/engine/src/game/engine.rs
  - crates/engine/src/game/zones.rs
  - crates/engine/src/game/game_object.rs
  - crates/engine/src/game/mod.rs
  - crates/engine/src/types/card.rs
  - crates/engine/src/types/actions.rs
  - crates/engine/src/types/events.rs
  - client/src/adapter/types.ts
  - client/src/components/card/ArtCropCard.tsx
autonomous: true
requirements: [ENG-10, ENG-11]

must_haves:
  truths:
    - "A DFC can transform between its front and back face"
    - "Transformed state changes the card's characteristics (name, P/T, abilities, types)"
    - "DFCs that change zones become front-face-up (Rule 711.8)"
    - "Player can see the back face of a DFC via hover-to-peek"
  artifacts:
    - path: "crates/engine/src/game/transform.rs"
      provides: "Transform/DFC face switching logic"
      exports: ["transform_permanent", "apply_back_face"]
  key_links:
    - from: "crates/engine/src/game/transform.rs"
      to: "crates/engine/src/types/card.rs"
      via: "Reads CardLayout::Transform(front, back) to swap characteristics"
      pattern: "CardLayout::Transform"
    - from: "crates/engine/src/game/zones.rs"
      to: "crates/engine/src/game/game_object.rs"
      via: "Reset transformed flag on zone change"
      pattern: "transformed = false"
---

<objective>
Implement transform/DFC (double-faced card) mechanics -- face switching that changes card characteristics, zone-change reset, GameAction::Transform, and UI hover-to-peek for back face.

Purpose: Transform/DFC is a major Standard mechanic (werewolves, MDFCs). The `transformed` field exists on GameObject but no face-switching logic exists. This blocks all transform cards. Depends on Plans 01 and 02 completing first since they modify shared files (engine.rs, actions.rs, events.rs, mod.rs).

Output: New transform module, face-switching logic, zone reset, GameAction::Transform, UI hover peek.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-CONTEXT.md
@.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-01-SUMMARY.md
@.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-02-SUMMARY.md

@crates/engine/src/game/game_object.rs
@crates/engine/src/game/zones.rs
@crates/engine/src/types/card.rs
@crates/engine/src/game/engine.rs
@crates/engine/src/types/actions.rs
@crates/engine/src/types/events.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transform module with face switching and zone reset</name>
  <files>crates/engine/src/game/transform.rs, crates/engine/src/game/zones.rs, crates/engine/src/game/game_object.rs, crates/engine/src/game/mod.rs, crates/engine/src/types/events.rs</files>
  <behavior>
    - Test: transform_permanent flips transformed flag and swaps name/power/toughness/abilities from back face
    - Test: transforming back restores front face characteristics
    - Test: zone change resets transformed to false (Rule 711.8)
    - Test: non-DFC card (Single layout) cannot be transformed (returns error)
  </behavior>
  <action>
1. Create `crates/engine/src/game/transform.rs`:
   - The function needs access to the card database to look up the back face. Add a `back_face_data` field or a mechanism to store the back face characteristics on the GameObject itself (e.g., `back_face: Option<BackFaceData>` with name, power, toughness, abilities, keywords, types).
   - Alternative (simpler): Store the back face data on the GameObject when it enters the battlefield from a Transform layout card. The parser already has CardLayout::Transform(front, back). When creating the object from a Transform card, populate a new `back_face` field.
   - `transform_permanent(state: &mut GameState, object_id: ObjectId, events: &mut Vec<GameEvent>) -> Result<(), EngineError>`:
     - Toggle `obj.transformed`
     - If transforming to back: swap current characteristics with back_face data
     - If transforming to front: swap back to original (store original in back_face before swapping)
     - Emit `GameEvent::Transformed { object_id }` (add this variant to events.rs)
     - Mark layers_dirty = true
2. In `zones.rs` `move_to_zone()`: when an object changes zones, if it's transformed, set `transformed = false` and restore front-face characteristics.
3. Add `back_face_name: Option<String>`, `back_face_power: Option<i32>`, `back_face_toughness: Option<i32>` fields to GameObject (or a `BackFaceData` struct). These get populated when a Transform-layout card enters the game.
4. Add `GameEvent::Transformed { object_id: ObjectId }` to events.rs.
5. Add `pub mod transform;` to mod.rs.
6. Write 3-4 inline tests.</action>
  <verify>
    <automated>cargo test -p engine -- transform -x</automated>
  </verify>
  <done>Transform toggles face, swaps characteristics, zone change resets to front. Non-DFC cards rejected.</done>
</task>

<task type="auto">
  <name>Task 2: GameAction::Transform, engine wiring, TS types, and hover-to-peek UI</name>
  <files>crates/engine/src/game/engine.rs, crates/engine/src/types/actions.rs, client/src/adapter/types.ts, client/src/components/card/ArtCropCard.tsx</files>
  <action>
1. Add `GameAction::Transform { object_id: ObjectId }` to actions.rs.
2. In engine.rs, add match arm for `(WaitingFor::Priority { player }, GameAction::Transform { object_id })`:
   - Validate object is on battlefield, controlled by player, and has a back face
   - Call `transform::transform_permanent(state, object_id, &mut events)`
   - Return WaitingFor::Priority { player }
   Note: In practice, transform is usually triggered by abilities/triggers (not a direct player action), but the action exists for the engine to invoke during effect resolution. Most transform triggers will call transform_permanent directly from within their trigger/effect handler.
3. Update `client/src/adapter/types.ts`:
   - Add `| { type: "Transform"; data: { object_id: ObjectId } }` to GameAction union.
   - Add `| { type: "Transformed"; data: { object_id: ObjectId } }` to GameEvent union.
   - Add `back_face_name?: string` to GameObject interface.
4. In `ArtCropCard.tsx` (or PermanentCard): When the card has `back_face_name`, show a small DFC indicator icon. On hover (desktop) or long-press (mobile), show the back face art in the CardPreview panel.
5. Run full test suite and type-check.</action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check && cd /Users/matt/dev/forge.rs && cargo test --all</automated>
  </verify>
  <done>Transform action works in engine. TypeScript types updated. DFC cards show hover-to-peek for back face. All tests pass.</done>
</task>

</tasks>

<verification>
- `cargo test -p engine -- transform` passes
- `cargo test --all` passes
- `cd client && pnpm run type-check` passes
- `cargo clippy --all-targets -- -D warnings` clean
</verification>

<success_criteria>
- DFCs can transform between front and back face
- Characteristics swap correctly (name, P/T, abilities, types)
- Zone change resets to front face
- GameEvent::Transformed emitted
- UI shows hover-to-peek for DFC back face
</success_criteria>

<output>
After completion, create `.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-05-SUMMARY.md`
</output>
