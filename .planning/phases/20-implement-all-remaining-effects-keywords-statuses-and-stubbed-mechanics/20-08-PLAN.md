---
phase: 20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics
plan: 08
type: execute
wave: 4
depends_on: [20-05, 20-06]
files_modified:
  - crates/engine/src/game/day_night.rs
  - crates/engine/src/game/turns.rs
  - crates/engine/src/game/mod.rs
  - crates/engine/src/types/game_state.rs
  - crates/engine/src/types/events.rs
  - crates/engine/src/game/triggers.rs
autonomous: true
requirements: [ENG-16]

must_haves:
  truths:
    - "Day/night is a global game state that transitions based on spells cast per turn"
    - "Daybound creatures transform to nightbound face when night begins, and vice versa"
    - "DayTimeChanges trigger fires on day/night transition"
  artifacts:
    - path: "crates/engine/src/game/day_night.rs"
      provides: "Day/night global state tracking and transition logic"
      exports: ["check_day_night_transition", "DayNightState"]
  key_links:
    - from: "crates/engine/src/game/day_night.rs"
      to: "crates/engine/src/game/turns.rs"
      via: "Day/night check at end of turn based on spells cast"
      pattern: "check_day_night_transition"
    - from: "crates/engine/src/game/day_night.rs"
      to: "crates/engine/src/game/transform.rs"
      via: "Uses transform_permanent to flip daybound/nightbound creatures"
      pattern: "transform::transform_permanent"
---

<objective>
Implement Day/Night global state with Daybound/Nightbound transformation triggers.

Purpose: Day/Night affects werewolves and similar Standard cards. The global state tracks whether it's day or night, and Daybound/Nightbound creatures auto-transform on transitions. Depends on Plan 05 (transform module) and Plan 06 (trigger matchers) being complete.

Output: Day/night module with global state tracking, spell-cast counting, turn-end transition checks, and auto-transformation of Daybound/Nightbound permanents.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-CONTEXT.md
@.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-05-SUMMARY.md
@.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-06-SUMMARY.md

@crates/engine/src/game/engine.rs
@crates/engine/src/game/turns.rs
@crates/engine/src/game/game_object.rs
@crates/engine/src/types/game_state.rs
@crates/engine/src/types/events.rs
@crates/engine/src/game/triggers.rs
@crates/engine/src/game/transform.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Day/Night global state enum and spell-cast tracking</name>
  <files>crates/engine/src/types/game_state.rs, crates/engine/src/game/turns.rs, crates/engine/src/types/events.rs</files>
  <behavior>
    - Test: Game starts with no day/night state (None)
    - Test: First daybound/nightbound card entering sets it to Day
    - Test: spells_cast_this_turn increments on spell cast and resets at turn start
  </behavior>
  <action>
1. Add `day_night: Option<DayNight>` field to GameState (where `DayNight` is an enum `{ Day, Night }`). Derive Serialize/Deserialize/PartialEq/Eq/Clone/Debug.
2. Add `spells_cast_this_turn: u8` field to GameState (tracked per turn, reset at turn start).
3. In `turns.rs`: reset spells_cast_this_turn to 0 at turn start. Increment it in the spell-cast handler (engine.rs or casting.rs where spells resolve).
4. Add `GameEvent::DayNightChanged { new_state: String }` to events.rs.
5. Write 2-3 inline tests for spell counting and initial None state.</action>
  <verify>
    <automated>cargo test -p engine -- day_night -x && cargo test -p engine -- spells_cast -x</automated>
  </verify>
  <done>GameState has day_night field (starts None) and spells_cast_this_turn counter that resets each turn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Day/Night transition logic and Daybound/Nightbound auto-transform</name>
  <files>crates/engine/src/game/day_night.rs, crates/engine/src/game/mod.rs, crates/engine/src/game/turns.rs, crates/engine/src/game/triggers.rs</files>
  <behavior>
    - Test: If active player cast no spells on their turn, transitions Day to Night
    - Test: If active player cast 2+ spells on their turn, transitions Night to Day
    - Test: Daybound creatures transform when night begins
    - Test: Nightbound creatures transform when day begins
    - Test: DayTimeChanges trigger fires on day/night transition
  </behavior>
  <action>
1. Create `crates/engine/src/game/day_night.rs`:
   - `check_day_night_transition(state: &mut GameState, events: &mut Vec<GameEvent>)` -- called at end of turn (in turns.rs):
     - If day_night is None, skip.
     - If Day and active player cast 0 spells this turn -> transition to Night.
     - If Night and active player cast 2+ spells this turn -> transition to Day.
     - On transition: emit `GameEvent::DayNightChanged { new_state }`. Transform all Daybound/Nightbound permanents accordingly (call transform::transform_permanent for each).
   - `initialize_day_night(state: &mut GameState)` -- sets day_night to Some(Day) when a daybound/nightbound card first enters the game.
2. In `turns.rs`: call `check_day_night_transition` at end of turn.
3. In `triggers.rs`: promote `DayTimeChanges` from unimplemented to real matcher that checks for DayNightChanged event.
4. Add `pub mod day_night;` to mod.rs.
5. Write 3-4 inline tests.</action>
  <verify>
    <automated>cargo test -p engine -- day_night -x && cargo test -p engine -- test_daynight -x && cargo test --all</automated>
  </verify>
  <done>Day/night transitions based on spell count. Daybound/nightbound permanents auto-transform. DayTimeChanges trigger fires.</done>
</task>

</tasks>

<verification>
- `cargo test -p engine -- day_night` passes
- `cargo test --all` passes
- `cargo clippy --all-targets -- -D warnings` clean
</verification>

<success_criteria>
- Day/night transitions correctly based on spells cast
- Daybound/nightbound creatures auto-transform on transition
- DayTimeChanges trigger fires on transition
- Game starts with no day/night state until a daybound card enters
</success_criteria>

<output>
After completion, create `.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-08-SUMMARY.md`
</output>
