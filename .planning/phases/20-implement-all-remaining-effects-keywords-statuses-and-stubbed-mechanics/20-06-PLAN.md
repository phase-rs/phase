---
phase: 20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics
plan: 06
type: execute
wave: 3
depends_on: [20-02, 20-04, 20-05]
files_modified:
  - crates/engine/src/game/static_abilities.rs
  - crates/engine/src/game/triggers.rs
  - crates/engine/src/game/combat.rs
  - crates/engine/src/game/casting.rs
  - crates/engine/src/game/sba.rs
autonomous: true
requirements: [ENG-12, ENG-13]

must_haves:
  truths:
    - "Indestructible prevents destruction by lethal damage and destroy effects"
    - "CantBeCountered spells cannot be countered"
    - "FlashBack allows casting spells from the graveyard"
    - "Promoted trigger matchers fire on correct events (AttackerBlocked, Cycled, Attached, etc.)"
    - "Static abilities that were stubs now return proper StaticEffect values"
  artifacts:
    - path: "crates/engine/src/game/static_abilities.rs"
      provides: "Promoted static handlers for Standard-relevant stubs"
    - path: "crates/engine/src/game/triggers.rs"
      provides: "Promoted trigger matchers for Standard-relevant stubs"
  key_links:
    - from: "crates/engine/src/game/static_abilities.rs"
      to: "crates/engine/src/game/sba.rs"
      via: "Indestructible check in lethal damage SBA"
      pattern: "has_keyword.*Indestructible"
    - from: "crates/engine/src/game/triggers.rs"
      to: "crates/engine/src/types/events.rs"
      via: "New matchers respond to existing GameEvent variants"
      pattern: "match_attacker_blocked|match_attached"
---

<objective>
Batch promote static ability stubs and trigger matcher stubs to real handlers. Focus on Standard-relevant mechanics: Indestructible (real SBA integration), CantBeCountered, FlashBack, Vigilance/Menace/Reach (static-granted versions), and high-frequency trigger matchers (AttackerBlocked, Attached, Cycled, Milled, Exiled).

Purpose: The engine has ~47 static stubs and ~100 trigger stubs. Many of these are needed for Standard cards. Promoting them in a batch is efficient since they all follow the same registry pattern.

Output: 15-20 promoted static handlers, 15-20 promoted trigger matchers.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-CONTEXT.md

@crates/engine/src/game/static_abilities.rs
@crates/engine/src/game/triggers.rs
@crates/engine/src/game/combat.rs
@crates/engine/src/game/sba.rs
@crates/engine/src/game/casting.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote Standard-relevant static ability stubs to real handlers</name>
  <files>crates/engine/src/game/static_abilities.rs, crates/engine/src/game/sba.rs, crates/engine/src/game/casting.rs, crates/engine/src/game/combat.rs</files>
  <behavior>
    - Test: handle_indestructible returns RuleModification with "Indestructible" mode
    - Test: CantBeCountered static prevents counter effect from resolving
    - Test: FlashBack returns RuleModification allowing cast from graveyard
    - Test: Vigilance static returns RuleModification (grants vigilance via static, not keyword)
    - Test: Menace static returns RuleModification (requires 2+ blockers)
    - Test: CantBeDestroyed returns proper RuleModification
  </behavior>
  <action>Promote the following stubs from the `stubs` array in `build_static_registry()` to real handlers. Move each from the stubs array to an explicit `registry.insert()` with a dedicated handler function:

**High priority (Standard-legal impact):**
1. `Indestructible` -> handle_indestructible: returns `RuleModification { mode: "Indestructible" }`. Already partially implemented via keyword check in sba.rs -- ensure static-granted Indestructible also works (check_static_ability in combat/sba paths).
2. `CantBeCountered` -> handle_cant_be_countered: returns `RuleModification { mode: "CantBeCountered" }`. Wire into counter effect (effects/counter.rs) to check before countering.
3. `FlashBack` -> handle_flashback: returns `RuleModification { mode: "FlashBack" }`. Wire into casting.rs to allow casting from graveyard with flashback cost. After resolution, exile instead of graveyard.
4. `CantBeDestroyed` -> handle_cant_be_destroyed: returns `RuleModification { mode: "CantBeDestroyed" }`.
5. `Vigilance` -> handle_static_vigilance: returns `RuleModification { mode: "Vigilance" }` (for static-granted, not keyword).
6. `Menace` -> handle_static_menace: returns `RuleModification { mode: "Menace" }`.
7. `Reach` -> handle_static_reach: returns `RuleModification { mode: "Reach" }`.
8. `Flying` -> handle_static_flying: returns `RuleModification { mode: "Flying" }`.
9. `Trample` -> handle_static_trample: returns `RuleModification { mode: "Trample" }`.
10. `Deathtouch` -> handle_static_deathtouch: returns `RuleModification { mode: "Deathtouch" }`.
11. `Lifelink` -> handle_static_lifelink: returns `RuleModification { mode: "Lifelink" }`.
12. `Shroud` -> handle_shroud: returns `RuleModification { mode: "Shroud" }`.
13. `CantTap` / `CantUntap` -> handle_rule_mod (already use this pattern).
14. `MustBeBlocked` -> handle_rule_mod.
15. `CantAttackAlone` / `CantBlockAlone` -> handle_rule_mod.

For keyword statics (Flying through Lifelink): these are for effects that grant keywords via static abilities (e.g., "All creatures you control have flying"). They return the same RuleModification pattern. The layer system then applies them.

Write 5+ inline tests verifying the promoted handlers return correct StaticEffect values.</action>
  <verify>
    <automated>cargo test -p engine -- test_static -x && cargo test -p engine -- static_abilit -x</automated>
  </verify>
  <done>15+ statics promoted from stub to real handlers. Key integrations (Indestructible SBA, CantBeCountered counter, FlashBack casting) wired. All return proper StaticEffect values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote Standard-relevant trigger matcher stubs to real matchers</name>
  <files>crates/engine/src/game/triggers.rs</files>
  <behavior>
    - Test: AttackerBlocked fires when source is among blocked attackers
    - Test: AttackerUnblocked fires when source attacks and is not blocked
    - Test: Attached fires when equipment/aura attaches to a permanent
    - Test: Milled fires on mill event with correct player filter
    - Test: Exiled fires on zone change to exile
    - Test: Cycled fires on discard-from-cycling event (when cycling is implemented)
  </behavior>
  <action>Promote the following trigger matchers from the `unimplemented_modes` array to real matcher functions:

**High priority (Standard-legal impact):**
1. `AttackerBlocked` / `AttackerBlockedOnce` -> match_attacker_blocked: check BlockersDeclared event, see if source_id is among blocked attackers.
2. `AttackerBlockedByCreature` -> match_attacker_blocked_by_creature: similar but includes which creature blocked.
3. `AttackerUnblocked` / `AttackerUnblockedOnce` -> match_attacker_unblocked: check if source attacked but was not assigned any blockers.
4. `Attached` -> match_attached: match on a new GameEvent or on ZoneChanged when an attachment enters. Check if source just became attached to something.
5. `Unattach` -> match_unattach: fires when attachment is removed.
6. `Milled` / `MilledOnce` / `MilledAll` -> match_milled: match on ZoneChanged from Library to Graveyard caused by mill.
7. `Exiled` -> match_exiled: match on ZoneChanged to Exile zone.
8. `Cycled` -> match_cycled: match on a cycling-specific event (or Discarded with cycling flag).
9. `Explored` -> match_explored: fires when explore resolves.
10. `Revealed` -> match_revealed: fires on reveal event.
11. `Shuffled` -> match_shuffled: fires when library is shuffled.
12. `ChangesController` -> match_changes_controller: fires when controller changes (GainControl effect).
13. `TapsForMana` -> match_taps_for_mana: fires when a permanent taps to produce mana.
14. `Immediate` / `Always` -> match_always: always returns true (used for state triggers).
15. `Fight` / `FightOnce` -> match_fight: fires on fight event.

For matchers that depend on events not yet emitted (e.g., Cycled needs a CyclingEvent), implement the matcher to respond to the closest existing event or add a simple event variant. Write the matcher logic even if the triggering event doesn't fire yet -- it will when the mechanic is implemented.

Write 5+ inline tests covering the key matchers.</action>
  <verify>
    <automated>cargo test -p engine -- trigger -x && cargo test --all</automated>
  </verify>
  <done>15+ trigger matchers promoted from stub. Key Standard triggers (AttackerBlocked, Attached, Milled, Exiled) fire on correct events. All tests pass.</done>
</task>

</tasks>

<verification>
- `cargo test -p engine -- static_abilit` passes
- `cargo test -p engine -- trigger` passes
- `cargo test --all` passes
- `cargo clippy --all-targets -- -D warnings` clean
</verification>

<success_criteria>
- Static stubs reduced from ~47 to ~30 (most Standard-relevant promoted)
- Trigger stubs reduced from ~100 to ~85 (most Standard-relevant promoted)
- Indestructible properly integrated with SBA
- CantBeCountered wired into counter effect
- FlashBack casting from graveyard works
- AttackerBlocked and Attached triggers fire correctly
</success_criteria>

<output>
After completion, create `.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-06-SUMMARY.md`
</output>
