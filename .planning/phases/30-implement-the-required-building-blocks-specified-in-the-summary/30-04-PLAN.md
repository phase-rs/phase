---
phase: 30-implement-the-required-building-blocks-specified-in-the-summary
plan: 04
type: execute
wave: 4
depends_on: ["30-02", "30-03"]
files_modified:
  - client/src/adapter/types.ts
  - client/src/components/modal/AdventureCastModal.tsx
  - client/src/components/card/CardRenderer.tsx
  - client/src/components/zone/ExileZone.tsx
  - crates/engine-wasm/src/lib.rs
  - crates/engine/tests/integration_adventure.rs
autonomous: false
requirements:
  - BB-03
  - BB-ALL

must_haves:
  truths:
    - "Frontend shows Adventure casting choice modal when casting from hand"
    - "Exile zone shows castable Adventure creatures with a cast button"
    - "TypeScript types include AdventureCastChoice and ChooseAdventureFace"
    - "Bonecrusher Giant integration test exercises all four building blocks together"
  artifacts:
    - path: "client/src/components/modal/AdventureCastModal.tsx"
      provides: "Adventure face choice modal"
    - path: "client/src/adapter/types.ts"
      provides: "TypeScript types for Adventure WaitingFor/GameAction"
    - path: "crates/engine/tests/integration_adventure.rs"
      provides: "Bonecrusher Giant end-to-end integration test"
  key_links:
    - from: "client/src/components/modal/AdventureCastModal.tsx"
      to: "client/src/adapter/types.ts"
      via: "Reads WaitingFor.AdventureCastChoice, dispatches ChooseAdventureFace"
      pattern: "AdventureCastChoice|ChooseAdventureFace"
    - from: "crates/engine/tests/integration_adventure.rs"
      to: "crates/engine/src/game/casting.rs"
      via: "Tests full Adventure casting flow end-to-end"
      pattern: "adventure|bonecrusher"
---

<objective>
Add frontend UI for Adventure casting (face choice modal, exile cast button) and create the comprehensive Bonecrusher Giant integration test that exercises all four building blocks together. Also update TypeScript types and WASM bridge.

Purpose: Complete the Adventure full-stack experience and verify all building blocks work together in a realistic card scenario.
Output: Adventure casting modal, exile zone cast button, TypeScript types, Bonecrusher Giant integration test.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-implement-the-required-building-blocks-specified-in-the-summary/30-CONTEXT.md
@.planning/phases/30-implement-the-required-building-blocks-specified-in-the-summary/30-RESEARCH.md
@.planning/phases/30-implement-the-required-building-blocks-specified-in-the-summary/30-01-SUMMARY.md
@.planning/phases/30-implement-the-required-building-blocks-specified-in-the-summary/30-02-SUMMARY.md
@.planning/phases/30-implement-the-required-building-blocks-specified-in-the-summary/30-03-SUMMARY.md

@client/src/adapter/types.ts
@client/src/components/modal/ModeChoiceModal.tsx
@client/src/components/modal/CardChoiceModal.tsx
@client/src/stores/gameStore.ts
@crates/engine-wasm/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend Adventure UI and TypeScript types</name>
  <files>client/src/adapter/types.ts, client/src/components/modal/AdventureCastModal.tsx, client/src/components/zone/ExileZone.tsx, crates/engine-wasm/src/lib.rs</files>
  <read_first>
    - client/src/adapter/types.ts (WaitingFor type, GameAction type — discriminated union patterns)
    - client/src/components/modal/ModeChoiceModal.tsx (existing modal pattern — layout, dispatch, gameStore integration)
    - client/src/components/modal/CardChoiceModal.tsx (card display pattern for modals)
    - client/src/stores/gameStore.ts (dispatch function, state management)
    - client/src/components/zone/ExileZone.tsx (if exists — exile zone rendering, how cards in exile are displayed)
    - crates/engine-wasm/src/lib.rs (tsify derives for WaitingFor/GameAction — verify auto-generation or manual types)
  </read_first>
  <action>
**TypeScript Types (client/src/adapter/types.ts):**

1. Add `AdventureCastChoice` variant to `WaitingFor` discriminated union:
   ```typescript
   | { type: "AdventureCastChoice"; data: { player: number; object_id: number; card_id: number } }
   ```

2. Add `ChooseAdventureFace` variant to `GameAction`:
   ```typescript
   | { type: "ChooseAdventureFace"; data: { creature: boolean } }
   ```

3. Add `CastingPermission` type:
   ```typescript
   type CastingPermission = { type: "AdventureCreature" };
   ```

4. Add `GameRestriction` type (for state inspection if needed):
   ```typescript
   type GameRestriction = {
     type: "DamagePreventionDisabled";
     data: { source: number; expiry: { type: string }; scope?: { type: string } | null };
   };
   ```

5. Update `GameObject` type to include `casting_permissions: CastingPermission[]` if not already present.

6. Update `GameState` type to include `restrictions: GameRestriction[]` if not already present.

**Adventure Cast Modal (client/src/components/modal/AdventureCastModal.tsx):**

Create a new modal component that appears when `waitingFor.type === "AdventureCastChoice"`. Follow the existing `ModeChoiceModal` pattern:

- Display the card with both faces visible (creature name + Adventure name)
- Two buttons: "Cast as [Creature Name]" and "Cast [Adventure Name]"
- Dispatching:
  - Creature button → `dispatch({ type: "ChooseAdventureFace", data: { creature: true } })`
  - Adventure button → `dispatch({ type: "ChooseAdventureFace", data: { creature: false } })`
- Style consistent with existing modals (Tailwind, framer-motion if used)
- Show mana costs for both faces so the player can make an informed choice

**Wire modal into game flow:**

Register `AdventureCastModal` in the appropriate parent component (likely wherever `ModeChoiceModal` and `CardChoiceModal` are conditionally rendered based on `waitingFor.type`).

**Exile Zone Cast Button:**

In the exile zone component (or wherever exile cards are rendered):
- Check each card for `casting_permissions` containing `AdventureCreature`
- If present, show a "Cast Creature" button or highlight on that card
- Clicking dispatches `CastSpell` with the card's ID (normal casting flow)

**WASM Bridge (crates/engine-wasm/src/lib.rs):**

Verify that `WaitingFor::AdventureCastChoice`, `GameAction::ChooseAdventureFace`, `CastingPermission`, and `GameRestriction` types have `Tsify` derives or are properly exposed. If auto-generated via `tsify`, confirm the output matches the manual TypeScript types. If manual, add the types.

**Tests:**

No automated test for frontend — this is verified in the checkpoint below.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `client/src/adapter/types.ts` contains `AdventureCastChoice`
    - `client/src/adapter/types.ts` contains `ChooseAdventureFace`
    - `client/src/adapter/types.ts` contains `CastingPermission`
    - `client/src/components/modal/AdventureCastModal.tsx` exists
    - `client/src/components/modal/AdventureCastModal.tsx` contains `ChooseAdventureFace`
    - `pnpm run type-check` exits 0 (no TypeScript errors)
    - `pnpm lint` exits 0 (no lint errors)
  </acceptance_criteria>
  <done>Frontend shows Adventure casting choice modal with creature/Adventure options. Exile zone shows castable Adventure creatures. TypeScript types are up to date for all new WaitingFor/GameAction/state types. Type-check and lint pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bonecrusher Giant integration test</name>
  <files>crates/engine/tests/integration_adventure.rs</files>
  <read_first>
    - crates/engine/src/game/scenario.rs (GameScenario builder, CardBuilder — how integration tests are structured)
    - crates/engine/tests/ (existing integration test files — patterns, imports, GameRunner usage)
    - crates/engine/src/types/ability.rs (Effect::DealDamage, TargetFilter::TriggeringSpellController, Effect::AddRestriction, TriggerMode::BecomesTarget)
    - crates/engine/src/types/card.rs (CardLayout::Adventure)
  </read_first>
  <behavior>
    - Test 1 (adventure_cast_stomp_from_hand): Cast Adventure card, choose Adventure face, spell goes on stack with Adventure characteristics
    - Test 2 (adventure_exile_on_resolve): Adventure spell resolves, card in exile with AdventureCreature permission
    - Test 3 (adventure_countered_to_graveyard): Adventure spell countered, card in graveyard (NOT exile)
    - Test 4 (adventure_cast_creature_from_exile): Card in exile with AdventureCreature, cast as creature from exile
    - Test 5 (bonecrusher_becomes_target_trigger): Bonecrusher Giant creature on battlefield, opponent targets it, trigger fires, deals 2 damage to opponent (TriggeringSpellController resolves)
    - Test 6 (stomp_damage_prevention_disabled): Cast Stomp, DamagePreventionDisabled restriction active, damage prevention replacement skipped
    - Test 7 (bonecrusher_full_flow): End-to-end full lifecycle of the card across multiple turns
  </behavior>
  <action>
Create `crates/engine/tests/integration_adventure.rs` as a comprehensive integration test file.

Use the `GameScenario` builder to set up game states. For Bonecrusher Giant, construct the card data programmatically:

**Card data setup:**
- Creature face: Bonecrusher Giant — 4/3, {2}{R}, triggers: BecomesTarget -> DealDamage(2, TriggeringSpellController)
- Adventure face: Stomp — Instant, {1}{R}, effects: AddRestriction(DamagePreventionDisabled EndOfTurn) + DealDamage(2, Any)
- Layout: `CardLayout::Adventure(creature_face, adventure_face)`

Build tests using `CardBuilder` with explicit `AbilityDefinition`/`TriggerDefinition` structs for both faces.

For each test, verify:
- Zone transitions (hand -> stack -> exile -> stack -> battlefield)
- State mutations (restrictions added/removed, permissions added)
- Event emissions (SpellCast, EffectResolved, DamageDealt, ZoneChanged)
- Trigger firing and event-context target resolution

The final integration test (bonecrusher_full_flow) should exercise the complete lifecycle of the card across multiple turns.

Register the test file in `Cargo.toml` if needed (usually auto-discovered for files in `tests/`).
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine --test integration_adventure -- --test-threads=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/engine/tests/integration_adventure.rs` exists
    - `crates/engine/tests/integration_adventure.rs` contains `fn adventure_cast_stomp_from_hand`
    - `crates/engine/tests/integration_adventure.rs` contains `fn adventure_exile_on_resolve`
    - `crates/engine/tests/integration_adventure.rs` contains `fn adventure_countered_to_graveyard`
    - `crates/engine/tests/integration_adventure.rs` contains `fn adventure_cast_creature_from_exile`
    - `crates/engine/tests/integration_adventure.rs` contains `fn bonecrusher_becomes_target_trigger`
    - `crates/engine/tests/integration_adventure.rs` contains `fn stomp_damage_prevention_disabled`
    - `crates/engine/tests/integration_adventure.rs` contains `fn bonecrusher_full_flow`
    - `cargo test -p engine --test integration_adventure` passes (7 tests)
    - `cargo test --all` passes (no regressions across all crates)
    - `cargo clippy --all-targets -- -D warnings` passes
  </acceptance_criteria>
  <done>All 7 integration tests pass: Adventure casting, exile-on-resolve, countered-to-graveyard, cast-from-exile, BecomesTarget trigger with event-context target, damage prevention disabling, and full Bonecrusher Giant lifecycle. All four building blocks verified working together. Zero regressions across all crates.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all building blocks work end-to-end</name>
  <what-built>Complete Adventure casting subsystem with all four building blocks. Player can cast Adventure cards, prevention can be disabled, triggers resolve event-context targets, and the frontend shows appropriate modals.</what-built>
  <how-to-verify>
    1. Run `cd /Users/matt/dev/forge.rs && cargo test --all` — all tests pass
    2. Run `cd /Users/matt/dev/forge.rs && cargo clippy --all-targets -- -D warnings` — no warnings
    3. Run `cd /Users/matt/dev/forge.rs/client && pnpm build` — frontend builds
    4. (Optional) Start the dev server and test Adventure casting UI if Adventure card data is available
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cargo test --all` — all crates pass
- `cargo test -p engine --test integration_adventure` — integration tests pass
- `cargo clippy --all-targets -- -D warnings` — no warnings
- `pnpm run type-check` — frontend types pass
- `pnpm build` — frontend builds
</verification>

<success_criteria>
1. Frontend Adventure casting modal shows creature/Adventure choice
2. Exile zone shows castable Adventure creatures
3. TypeScript types cover all new WaitingFor/GameAction/state types
4. Bonecrusher Giant integration test exercises all four building blocks
5. All tests pass across all crates — zero regressions
6. Human verification confirms quality
</success_criteria>

<output>
After completion, create `.planning/phases/30-implement-the-required-building-blocks-specified-in-the-summary/30-04-SUMMARY.md`
</output>
