---
phase: 29-support-n-players
plan: 13
type: execute
wave: 7
depends_on: ["29-07", "29-08", "29-09", "29-10", "29-11", "29-12"]
files_modified:
  - scripts/build-wasm.sh
  - client/src/wasm/
autonomous: false
requirements:
  - NP-INTEGRATION
  - NP-WASM-BUILD

must_haves:
  truths:
    - "WASM builds successfully with N-player engine"
    - "cargo test --all passes"
    - "cargo clippy --all-targets -- -D warnings passes"
    - "pnpm build succeeds"
    - "pnpm test -- --run passes"
    - "2-player Standard game plays identically to before"
    - "4-player Commander game starts and runs with AI opponents"
    - "Zero PlayerId(1-x) patterns remaining in codebase"
  artifacts:
    - path: "client/src/wasm/"
      provides: "Rebuilt WASM with N-player support"
  key_links:
    - from: "scripts/build-wasm.sh"
      to: "client/src/wasm/"
      via: "WASM build output"
      pattern: "wasm-bindgen"
---

<objective>
Full integration verification: rebuild WASM, run all test suites, verify 2-player parity and multiplayer gameplay.

Purpose: Final verification that all N-player changes integrate correctly. WASM must rebuild, all tests must pass, 2-player games must be identical to before, and multiplayer games must function.

Output: Rebuilt WASM, verified test suites, human-verified gameplay.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build and test verification</name>
  <files>scripts/build-wasm.sh</files>
  <action>
    1. Run complete Rust test suite:
       - `cargo test --all`
       - `cargo clippy --all-targets -- -D warnings`
       - `cargo fmt --all -- --check`

    2. Verify zero PlayerId(1-x) remaining:
       - `rg "PlayerId\(1 -" crates/` must return zero results

    3. Rebuild WASM:
       - `./scripts/build-wasm.sh`
       - Verify wasm-bindgen output includes updated TypeScript types

    4. Run frontend verification:
       - `cd client && pnpm install && pnpm build`
       - `cd client && pnpm test -- --run`
       - `cd client && pnpm lint`
       - `cd client && pnpm run type-check`

    5. If any failures, fix them before proceeding to checkpoint
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test --all 2>&1 | tail -5 && cd client && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>All Rust tests pass, WASM builds, frontend builds and tests pass, zero PlayerId(1-x) remaining</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full gameplay verification</name>
  <action>Human verifies 2-player parity and multiplayer gameplay</action>
  <what-built>Complete N-player engine and UI: format-aware games, Commander rules, multiplayer board layout, AI adaptation, lobby/setup flow</what-built>
  <how-to-verify>
    1. Start dev server: `cd client && pnpm dev`
    2. **2-Player Standard parity test:**
       - Start a Standard game vs AI (Medium difficulty)
       - Play several turns — verify board looks identical to before this phase
       - Cast spells, attack, block, verify all mechanics work
    3. **4-Player Commander test:**
       - Select Commander format, 4 players (1 human + 3 AI)
       - Select or use a pre-built Commander deck
       - Verify all 4 player areas render (your full area + 3 compact opponents)
       - Play several turns — verify priority passes through all players
       - Verify commander zone, commander tax, and commander damage display
       - Attack different opponents to test per-creature targeting
    4. **Elimination test:**
       - In a multiplayer game, let an AI die — verify they are eliminated and game continues
       - Verify eliminated player's permanents are exiled
    5. **Lobby test (if server available):**
       - Start server: `cargo serve`
       - Create Commander game, verify format appears in lobby
       - Verify ready room shows player slots
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cargo test --all` passes
- `cargo clippy --all-targets -- -D warnings` clean
- `./scripts/build-wasm.sh` succeeds
- `pnpm build` succeeds
- `pnpm test -- --run` passes
- Human verification of gameplay
</verification>

<success_criteria>
- All builds succeed (Rust, WASM, frontend)
- All tests pass across all crates and frontend
- 2-player Standard game visually identical to pre-phase
- 4-player Commander game starts and plays correctly
- Elimination works correctly
- Human approves gameplay quality
</success_criteria>

<output>
After completion, create `.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-13-SUMMARY.md`
</output>
