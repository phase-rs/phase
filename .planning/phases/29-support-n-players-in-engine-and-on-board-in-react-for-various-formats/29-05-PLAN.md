---
phase: 29-support-n-players
plan: 05
type: execute
wave: 3
depends_on: ["29-01", "29-02", "29-03", "29-04"]
files_modified:
  - crates/engine/src/game/engine.rs
  - crates/engine/src/game/priority.rs
  - crates/engine/src/game/filter.rs
  - crates/engine/src/game/targeting.rs
  - crates/engine/src/game/casting.rs
  - crates/engine/src/game/stack.rs
  - crates/engine/src/game/static_abilities.rs
  - crates/engine/src/game/triggers.rs
  - crates/engine/src/game/zones.rs
autonomous: true
requirements:
  - NP-OPPONENT-MIGRATION

must_haves:
  truths:
    - "No remaining uses of PlayerId(1 - player.0) in core engine modules"
    - "opponent() function in priority.rs removed or deprecated"
    - "All core callers use appropriate replacement: next_player(), opponents(), or direct player references"
    - "All existing tests still pass after migration"
  artifacts:
    - path: "crates/engine/src/game/priority.rs"
      provides: "opponent() function removed"
  key_links:
    - from: "All core engine files"
      to: "crates/engine/src/game/players.rs"
      via: "replaced PlayerId(1-x) with players:: functions"
      pattern: "players::(next_player|opponents)"
---

<objective>
Replace all hardcoded 2-player opponent assumptions in core engine modules (engine.rs, priority.rs, filter.rs, targeting.rs, casting.rs, stack.rs, static_abilities.rs, triggers.rs, zones.rs).

Purpose: This is Part 1 of the PlayerId(1-x) migration. Core modules are migrated first as they are the most critical and have the most complex replacement patterns.

Output: Zero remaining PlayerId(1-x) in core engine modules. opponent() removed from priority.rs.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-CONTEXT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-RESEARCH.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-01-SUMMARY.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-02-SUMMARY.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-03-SUMMARY.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace PlayerId(1 - x.0) in core engine modules</name>
  <files>crates/engine/src/game/engine.rs, crates/engine/src/game/priority.rs, crates/engine/src/game/filter.rs, crates/engine/src/game/targeting.rs, crates/engine/src/game/casting.rs, crates/engine/src/game/stack.rs, crates/engine/src/game/static_abilities.rs, crates/engine/src/game/triggers.rs, crates/engine/src/game/zones.rs</files>
  <action>
    Systematically replace every `PlayerId(1 - something.0)` with the correct N-player equivalent in core modules:

    1. **priority.rs** (1 location): `opponent()` function — remove it entirely. All callers should use `players::next_player()` instead. If any external crate (phase-ai) still calls it, leave a deprecated wrapper.

    2. **engine.rs** (1 location): `PlayerId(1 - state.active_player.0)` for defending player in combat entry — replace with appropriate logic.

    3. **filter.rs** (1 location): opponent filtering — replace with `players::opponents()` iteration.

    4. **targeting.rs**, **casting.rs**, **stack.rs**, **static_abilities.rs**, **triggers.rs**, **zones.rs**: Search for any remaining `PlayerId(1 -` patterns and replace with players:: functions.

    5. Also search for `1 - player` and `1 - active` patterns that might use different variable names.

    Run `rg "PlayerId\(1 -" crates/engine/src/game/` after changes to verify zero remaining instances in these files.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine --no-fail-fast 2>&1 | tail -5 && rg "PlayerId\(1 -" crates/engine/src/game/engine.rs crates/engine/src/game/priority.rs crates/engine/src/game/filter.rs crates/engine/src/game/targeting.rs crates/engine/src/game/casting.rs crates/engine/src/game/stack.rs crates/engine/src/game/static_abilities.rs crates/engine/src/game/triggers.rs crates/engine/src/game/zones.rs --count-matches 2>&1 | grep -v "^$" || echo "CLEAN: no PlayerId(1-x) remaining"</automated>
  </verify>
  <done>Zero remaining PlayerId(1 - x.0) in core engine modules. opponent() removed from priority.rs. All engine tests pass</done>
</task>

</tasks>

<verification>
- `rg "PlayerId\(1 -" crates/engine/src/game/{engine,priority,filter,targeting,casting,stack,static_abilities,triggers,zones}.rs` returns zero results
- `cargo test -p engine` all tests pass
- `cargo clippy -p engine -- -D warnings` clean
</verification>

<success_criteria>
- Complete elimination of PlayerId(1 - x.0) from core engine modules
- opponent() function removed from priority.rs
- All callers migrated to players:: functions
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-05-SUMMARY.md`
</output>
