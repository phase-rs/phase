---
phase: 29-support-n-players
plan: 15
type: execute
wave: 4
depends_on: ["29-05"]
files_modified:
  - crates/engine/src/game/layers.rs
  - crates/engine/src/game/derived.rs
  - crates/engine/src/game/morph.rs
  - crates/engine/src/game/devotion.rs
  - crates/engine/src/game/transform.rs
  - crates/engine/src/game/planeswalker.rs
  - crates/engine/src/game/mana_abilities.rs
  - crates/engine/src/game/mana_payment.rs
  - crates/engine/src/game/day_night.rs
  - crates/engine/src/game/replacement.rs
  - crates/engine/src/game/scenario.rs
  - crates/engine/tests/rules/targeting.rs
  - crates/engine/tests/rules/sba.rs
autonomous: true
requirements:
  - NP-OPPONENT-MIGRATION

must_haves:
  truths:
    - "No remaining uses of PlayerId(1 - player.0) in remaining game modules or tests"
    - "scenario.rs supports N-player game setup"
    - "All existing tests still pass after migration"
    - "new_two_player() usages in tests still compile and work"
  artifacts:
    - path: "crates/engine/src/game/scenario.rs"
      provides: "GameScenario with N-player support"
  key_links:
    - from: "All remaining game modules"
      to: "crates/engine/src/game/players.rs"
      via: "replaced PlayerId(1-x) with players:: functions"
      pattern: "players::(next_player|opponents)"
---

<objective>
Replace all hardcoded 2-player opponent assumptions in remaining game modules (layers, derived, morph, etc.) and test files.

Purpose: This is Part 3 of the PlayerId(1-x) migration. Remaining utility modules and test files need the same treatment. After this plan, zero PlayerId(1-x) patterns remain in the entire engine crate.

Output: Zero remaining PlayerId(1-x) in entire engine crate including utilities and tests.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-CONTEXT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace PlayerId(1 - x.0) in utility game modules</name>
  <files>crates/engine/src/game/layers.rs, crates/engine/src/game/derived.rs, crates/engine/src/game/morph.rs, crates/engine/src/game/devotion.rs, crates/engine/src/game/transform.rs, crates/engine/src/game/planeswalker.rs, crates/engine/src/game/mana_abilities.rs, crates/engine/src/game/mana_payment.rs, crates/engine/src/game/day_night.rs, crates/engine/src/game/replacement.rs</files>
  <action>
    Search all remaining game modules for `PlayerId(1 -` and `1 - player` patterns:

    1. **layers.rs, derived.rs**: Likely have opponent checks for continuous effects — replace with `players::opponents()` iteration
    2. **morph.rs, devotion.rs, transform.rs**: May not have opponent patterns — verify and update if present
    3. **planeswalker.rs**: May reference opponent for planeswalker targeting — replace with players:: functions
    4. **mana_abilities.rs, mana_payment.rs**: Unlikely to have patterns — verify
    5. **day_night.rs**: Unlikely — verify
    6. **replacement.rs**: May have "each opponent" replacement effects — replace

    For each file, determine semantic intent and replace appropriately.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine --no-fail-fast 2>&1 | tail -5 && rg "PlayerId\(1 -" crates/engine/src/game/layers.rs crates/engine/src/game/derived.rs crates/engine/src/game/morph.rs crates/engine/src/game/devotion.rs crates/engine/src/game/transform.rs crates/engine/src/game/planeswalker.rs crates/engine/src/game/mana_abilities.rs crates/engine/src/game/mana_payment.rs crates/engine/src/game/day_night.rs crates/engine/src/game/replacement.rs 2>&1 | grep -c ":" || echo "0 matches"</automated>
  </verify>
  <done>Zero remaining PlayerId(1 - x.0) in utility game modules. All engine tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Replace PlayerId(1 - x.0) in scenario.rs and test files, final sweep</name>
  <files>crates/engine/src/game/scenario.rs, crates/engine/tests/rules/targeting.rs, crates/engine/tests/rules/sba.rs</files>
  <action>
    1. Update `scenario.rs`:
       - Ensure GameScenario works with N players — add `new_n_player(count: u8, seed: u64)` variant if not already present
       - Update any `PlayerId(1 - x.0)` in scenario setup

    2. Update test files:
       - `tests/rules/targeting.rs`: Replace any `PlayerId(1 - x.0)` with explicit player IDs
       - `tests/rules/sba.rs`: Replace similarly

    3. Final comprehensive sweep:
       - `rg "PlayerId\(1 -" crates/engine/` must return zero results
       - `rg "1 - player" crates/engine/` and `rg "1 - active" crates/engine/` must return zero results for the opponent pattern
       - Fix any remaining instances found
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine --no-fail-fast 2>&1 | tail -5 && rg "PlayerId\(1 -" crates/engine/ 2>&1 | grep -c ":" || echo "0 matches — MIGRATION COMPLETE"</automated>
  </verify>
  <done>Zero remaining PlayerId(1 - x.0) in entire engine crate. All engine tests pass. Full migration complete</done>
</task>

</tasks>

<verification>
- `rg "PlayerId\(1 -" crates/engine/` returns zero results
- `cargo test -p engine` all tests pass
- `cargo clippy -p engine -- -D warnings` clean
</verification>

<success_criteria>
- Complete elimination of PlayerId(1 - x.0) pattern from entire engine crate
- GameScenario supports N-player setup
- All existing tests pass
- No behavioral changes in 2-player mode
</success_criteria>

<output>
After completion, create `.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-15-SUMMARY.md`
</output>
