---
phase: 29-support-n-players
plan: 09
type: execute
wave: 5
depends_on: ["29-06"]
files_modified:
  - crates/phase-ai/src/eval.rs
  - crates/phase-ai/src/search.rs
  - crates/phase-ai/src/combat_ai.rs
  - crates/phase-ai/src/card_hints.rs
  - crates/phase-ai/src/lib.rs
autonomous: true
requirements:
  - NP-AI-THREAT
  - NP-AI-SEARCH
  - NP-AI-SEAT

must_haves:
  truths:
    - "AI evaluates threat level per opponent based on board state, life, commander damage"
    - "AI targets highest-threat opponent with attacks and removal"
    - "Search depth scales inversely with player count (depth 2 for 3-4, depth 1 for 5-6)"
    - "AI fills empty seats: any seat without a human gets AI control"
    - "Per-seat difficulty configuration works"
    - "AI generates per-creature attack targets in multiplayer"
    - "AI performance stays responsive on WASM for multiplayer"
  artifacts:
    - path: "crates/phase-ai/src/eval.rs"
      provides: "Threat-aware evaluation across N opponents"
    - path: "crates/phase-ai/src/search.rs"
      provides: "Scaled search with player-count-aware budgets"
    - path: "crates/phase-ai/src/combat_ai.rs"
      provides: "Multi-opponent attack target selection"
  key_links:
    - from: "crates/phase-ai/src/eval.rs"
      to: "crates/engine/src/game/players.rs"
      via: "evaluates all opponents via opponents()"
      pattern: "players::opponents"
    - from: "crates/phase-ai/src/search.rs"
      to: "crates/phase-ai/src/eval.rs"
      via: "search uses eval for state scoring"
      pattern: "evaluate_state"
---

<objective>
Adapt the AI engine for threat-aware N-player evaluation and scaled search.

Purpose: The AI must play competently in multiplayer — not just legally but strategically. This means evaluating threats across all opponents, choosing attack targets wisely, and keeping search responsive as player count grows.

Output: Threat-aware evaluation, paranoid search for 3-4 players, heuristic-only for 5-6 players, per-creature attack target generation, budget scaling.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-CONTEXT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-RESEARCH.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-06-SUMMARY.md
@crates/phase-ai/src/eval.rs
@crates/phase-ai/src/search.rs
@crates/phase-ai/src/combat_ai.rs
@crates/phase-ai/src/card_hints.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Threat-aware evaluation and search scaling</name>
  <files>crates/phase-ai/src/eval.rs, crates/phase-ai/src/search.rs, crates/phase-ai/src/lib.rs</files>
  <action>
    1. Update `eval.rs`:
       - Add `threat_level(state: &GameState, evaluator: PlayerId, target: PlayerId) -> f64` function
       - Factors: board presence (creature count/power), life total relative to starting life, cards in hand, commander damage dealt to evaluator
       - Update main evaluation to consider all opponents: `eval = own_score - weighted_sum(opponent_scores)` where weights are threat levels
       - Highest-threat opponent gets highest weight (focus fire)

    2. Update `search.rs`:
       - Scale search budget by player count:
         - 2 players: current depth limits (unchanged)
         - 3-4 players: max depth 2, reduced node budget (paranoid search — treat all opponents as minimizers)
         - 5-6 players: max depth 1 (heuristic-only with evaluation), or fall back to Easy difficulty behavior
       - Paranoid search: in minimax, all opponent turns minimize AI's score (conservative but sound)
       - Platform scaling: multiply budget reduction on WASM (already exists, just ensure it compounds with player scaling)

    3. Update `lib.rs`:
       - `get_ai_action` function should accept difficulty per seat
       - Add `AiConfig` struct or extend existing difficulty levels with player-count-aware settings
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p phase-ai --no-fail-fast</automated>
  </verify>
  <done>AI evaluates threats across N opponents. Search scales with player count. Paranoid search for 3-4 players. Heuristic for 5-6. All AI tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Multi-opponent combat AI and attack target selection</name>
  <files>crates/phase-ai/src/combat_ai.rs, crates/phase-ai/src/card_hints.rs</files>
  <action>
    1. Update `combat_ai.rs`:
       - `choose_attackers` must now produce `Vec<(ObjectId, AttackTarget)>` instead of just `Vec<ObjectId>`
       - Strategy: assign each attacker to the highest-threat opponent, unless creature has specific targeting (e.g., must attack planeswalker)
       - Consider splitting attacks: if one opponent is near death, send enough to kill them
       - "Alpha strike" detection: can we eliminate one opponent by sending all creatures at them?
       - Blocker selection: unchanged (already operates per-creature)

    2. Update `card_hints.rs`:
       - Replace opponent references with multi-opponent logic
       - Target selection for removal spells: prefer highest-threat opponent's best creature
       - "Each opponent" effects correctly iterate all opponents

    3. Add tests:
       - AI in 3-player game attacks highest-threat opponent
       - AI in 3-player game splits attacks to finish off low-life opponent
       - AI correctly generates per-creature AttackTargets
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p phase-ai -- combat --no-fail-fast && cargo test -p phase-ai -- card_hints --no-fail-fast</automated>
  </verify>
  <done>AI generates per-creature attack targets. Threat-aware attack allocation. Multi-opponent combat strategy. All combat AI tests pass</done>
</task>

</tasks>

<verification>
- `cargo test -p phase-ai` all tests pass
- `cargo clippy -p phase-ai -- -D warnings` clean
- AI generates valid per-creature AttackTargets
</verification>

<success_criteria>
- Threat evaluation across N opponents
- Search budget scales with player count
- Paranoid search for 3-4 players
- Per-creature attack target generation
- AI targets highest-threat opponent
- Performance stays responsive on WASM
</success_criteria>

<output>
After completion, create `.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-09-SUMMARY.md`
</output>
