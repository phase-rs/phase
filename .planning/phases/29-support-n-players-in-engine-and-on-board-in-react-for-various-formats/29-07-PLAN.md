---
phase: 29-support-n-players
plan: 07
type: execute
wave: 4
depends_on: ["29-05", "29-06"]
files_modified:
  - client/src/components/board/PlayerArea.tsx
  - client/src/components/board/CompactStrip.tsx
  - client/src/components/board/GameBoard.tsx
  - client/src/components/board/CommanderDisplay.tsx
  - client/src/components/board/CommanderDamage.tsx
  - client/src/adapter/types.ts
  - client/src/stores/gameStore.ts
  - client/src/hooks/usePlayerId.ts
autonomous: false
requirements:
  - NP-BOARD-UI
  - NP-PLAYERAREA
  - NP-COMPACT
  - NP-1V1-PARITY
  - NP-COMMANDER-UI

must_haves:
  truths:
    - "In 2-player games, board looks identical to current top/bottom layout"
    - "In 3+ player games, all opponents render in compact mode by default"
    - "Clicking a compact opponent expands to focused mode"
    - "PlayerArea component renders full/focused/compact based on mode prop"
    - "Commander card displayed with visual highlight in player area"
    - "Commander damage from each source tracked and displayed"
    - "Frontend types updated for N-player state (eliminated_players, seat_order, format_config, commander_damage)"
  artifacts:
    - path: "client/src/components/board/PlayerArea.tsx"
      provides: "Unified player area with full/focused/compact modes"
    - path: "client/src/components/board/CompactStrip.tsx"
      provides: "Condensed opponent view with grouped permanents"
    - path: "client/src/components/board/GameBoard.tsx"
      provides: "N-player board layout rendering PlayerArea array"
    - path: "client/src/components/board/CommanderDisplay.tsx"
      provides: "Commander card with highlight"
    - path: "client/src/adapter/types.ts"
      provides: "Updated TypeScript types for N-player state"
  key_links:
    - from: "client/src/components/board/GameBoard.tsx"
      to: "client/src/components/board/PlayerArea.tsx"
      via: "renders PlayerArea for each player with appropriate mode"
      pattern: "PlayerArea.*mode"
    - from: "client/src/components/board/PlayerArea.tsx"
      to: "client/src/components/board/CompactStrip.tsx"
      via: "compact mode renders CompactStrip"
      pattern: "CompactStrip"
---

<objective>
Refactor the board UI from hardcoded 2-player top/bottom layout to a unified PlayerArea component supporting N players with full/focused/compact modes.

Purpose: The board must render 2-6 player games. In 2-player mode it must look identical to the current layout (hard requirement). In 3+ player mode, opponents show in compact strips with optional focus expansion.

Output: PlayerArea component with mode prop, CompactStrip for condensed opponents, refactored GameBoard, Commander display components. Frontend types updated for N-player state.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-CONTEXT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-01-SUMMARY.md
@client/src/components/board/GameBoard.tsx
@client/src/components/board/BattlefieldRow.tsx
@client/src/adapter/types.ts
@client/src/stores/gameStore.ts
@client/src/hooks/usePlayerId.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update frontend types and create PlayerArea/CompactStrip components</name>
  <files>client/src/adapter/types.ts, client/src/stores/gameStore.ts, client/src/components/board/PlayerArea.tsx, client/src/components/board/CompactStrip.tsx, client/src/components/board/CommanderDisplay.tsx, client/src/components/board/CommanderDamage.tsx</files>
  <action>
    1. Update `adapter/types.ts`:
       - Add `GameFormat` type: "Standard" | "Commander" | "FreeForAll" | "TwoHeadedGiant"
       - Add `FormatConfig` interface matching Rust struct
       - Add `AttackTarget` type: { type: "Player"; data: number } | { type: "Planeswalker"; data: number }
       - Update `GameState` type: add `seat_order: number[]`, `format_config: FormatConfig`, `eliminated_players: number[]`, `commander_damage: Record<string, number>` (key: `${playerId}-${commanderId}`)
       - Update `Player` type: add `is_eliminated: boolean`
       - Update `DeclareAttackers` action type to use attacks with AttackTarget

    2. Create `PlayerArea.tsx`:
       - Props: `playerId: number`, `mode: "full" | "focused" | "compact"`, `onFocus?: () => void`
       - `full` mode: renders current player's full board (lands, creatures, other permanents with current layout — reuse existing BattlefieldRow)
       - `focused` mode: renders opponent's full board (same as current top half of GameBoard — reuse BattlefieldRow)
       - `compact` mode: renders CompactStrip
       - In all modes, show player life, hand count, zone indicators
       - If Commander format: show CommanderDisplay and CommanderDamage

    3. Create `CompactStrip.tsx`:
       - Horizontal strip showing: player name, life total, hand count, grouped permanents as small art-crop thumbnails
       - Group permanents: creatures (with count), lands (with count), other (with count)
       - Use CSS custom properties for compact card sizing (smaller than normal)
       - Show commander card thumbnail if Commander format
       - Clickable: onClick triggers onFocus to expand to focused mode

    4. Create `CommanderDisplay.tsx`:
       - Shows commander card art with visual highlight (border glow using Tailwind ring/shadow)
       - Shows commander tax counter if cast count > 0
       - Compact variant for CompactStrip (smaller art)

    5. Create `CommanderDamage.tsx`:
       - Shows commander damage received from each opposing commander
       - Format: small icons with damage numbers, warn when approaching 21
       - Displayed in player info area

    6. Update `gameStore.ts`:
       - Handle eliminated players in state (skip rendering their actions)
       - Handle new state shape fields
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check</automated>
  </verify>
  <done>Frontend types updated for N-player. PlayerArea renders full/focused/compact. CompactStrip shows condensed opponent. Commander display and damage components exist. TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Refactor GameBoard for N-player layout</name>
  <files>client/src/components/board/GameBoard.tsx, client/src/hooks/usePlayerId.ts</files>
  <action>
    1. Refactor `GameBoard.tsx`:
       - Remove hardcoded `opponentId = myId === 0 ? 1 : 0`
       - Compute opponents from gameState: `state.seat_order.filter(id => id !== myId && !state.eliminated_players.includes(id))`
       - Layout logic:
         - 1 opponent (2-player): top=PlayerArea(opponent, focused), bottom=PlayerArea(me, full) — IDENTICAL to current layout (1v1 parity)
         - 2+ opponents: top section = row of CompactStrips for all opponents, bottom=PlayerArea(me, full)
         - When an opponent is focused (clicked): show that opponent as PlayerArea(focused) and others as CompactStrips
       - Use CSS Grid for layout: `grid-template-rows` adjusts based on opponent count
       - Compact strips in a scrollable horizontal row if many opponents

    2. Update `usePlayerId.ts`:
       - Ensure it works with N-player seat assignment (may need to read from multiplayerStore or gameState)

    3. Ensure 1v1 visual parity:
       - When exactly 1 opponent, the layout must produce the exact same visual as the current top/bottom split
       - Same land column on left, same creature/permanent areas, same sizing
       - Test by running 2-player game and comparing visually

    4. Handle eliminated player display:
       - Eliminated opponents show with greyed-out or crossed-out indicator
       - Their board area shrinks or is removed
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check && pnpm test -- --run</automated>
  </verify>
  <done>GameBoard renders N players dynamically. 2-player layout identical to current. 3+ player layout uses compact strips. Focus/expand works. Eliminated players handled. All frontend tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of board layout</name>
  <action>Human verifies the N-player board layout</action>
  <what-built>N-player board layout with PlayerArea full/focused/compact modes, CompactStrip for opponents, Commander display components</what-built>
  <how-to-verify>
    1. Start a 2-player game: `cd client && pnpm dev` then navigate to localhost:5173
    2. Verify the board looks IDENTICAL to the current layout (top opponent, bottom player)
    3. If possible, test with a 3+ player setup to verify compact strips render
    4. Check Commander display components render correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe layout issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm run type-check` passes
- `pnpm test -- --run` passes
- 2-player layout visually identical to current
- 3+ player layout shows compact opponent strips
</verification>

<success_criteria>
- PlayerArea component with full/focused/compact modes
- CompactStrip for condensed opponent view
- GameBoard dynamically renders N players
- 1v1 visual parity maintained (hard requirement)
- Commander display and damage tracking components
- Frontend types match engine N-player state
- Human verification of visual layout
</success_criteria>

<output>
After completion, create `.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-07-SUMMARY.md`
</output>
