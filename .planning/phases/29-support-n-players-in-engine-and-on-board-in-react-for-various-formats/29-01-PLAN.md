---
phase: 29-support-n-players
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/engine/src/types/format.rs
  - crates/engine/src/types/mod.rs
  - crates/engine/src/types/game_state.rs
  - crates/engine/src/types/player.rs
  - crates/engine/src/game/players.rs
  - crates/engine/src/game/mod.rs
autonomous: true
requirements:
  - NP-FORMAT
  - NP-SEAT
  - NP-ITER

must_haves:
  truths:
    - "FormatConfig::standard() creates a config with 20 life, 2 players, 60-card deck"
    - "FormatConfig::commander() creates a config with 40 life, 2-6 players, 100-card singleton"
    - "FormatConfig::free_for_all() creates a config with 20 life, 2-6 players, 60-card deck"
    - "FormatConfig::two_headed_giant() creates a config with 30 life per team, exactly 4 players, team-based"
    - "next_player() skips eliminated players and wraps around seat_order"
    - "opponents() returns all living players except the given player"
    - "apnap_order() returns living players starting from active player in seat order"
    - "GameState has seat_order, format_config, eliminated_players, commander_damage fields"
    - "Player has is_eliminated field"
    - "GameState::new(config, player_count, seed) creates N players with format-specific starting life"
    - "GameState::new_two_player(seed) still works identically to current behavior"
  artifacts:
    - path: "crates/engine/src/types/format.rs"
      provides: "GameFormat enum, FormatConfig struct with factory methods"
      exports: ["GameFormat", "FormatConfig"]
    - path: "crates/engine/src/game/players.rs"
      provides: "N-player iteration: next_player, opponents, apnap_order, teammates"
      exports: ["next_player", "opponents", "apnap_order", "teammates"]
    - path: "crates/engine/src/types/game_state.rs"
      provides: "Extended GameState with seat_order, format_config, eliminated_players, commander_damage, priority_passes"
    - path: "crates/engine/src/types/player.rs"
      provides: "Extended Player with is_eliminated field"
  key_links:
    - from: "crates/engine/src/types/format.rs"
      to: "crates/engine/src/types/game_state.rs"
      via: "FormatConfig stored on GameState"
      pattern: "format_config: FormatConfig"
    - from: "crates/engine/src/game/players.rs"
      to: "crates/engine/src/types/game_state.rs"
      via: "reads seat_order and eliminated_players"
      pattern: "state\\.seat_order"
---

<objective>
Define the foundational types and player iteration functions for N-player support.

Purpose: Every subsequent plan depends on FormatConfig, seat_order, and the player iteration functions. This plan creates the type contracts and utility functions that replace all hardcoded 2-player assumptions.

Output: `format.rs` with GameFormat/FormatConfig, `players.rs` with next_player/opponents/apnap_order/teammates, extended GameState and Player types. All existing tests still pass via backward-compatible `new_two_player()`.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-CONTEXT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-RESEARCH.md
@crates/engine/src/types/game_state.rs
@crates/engine/src/types/player.rs
@crates/engine/src/types/mod.rs
@crates/engine/src/game/mod.rs
@crates/engine/src/game/priority.rs

<interfaces>
<!-- Key types the executor needs -->

From crates/engine/src/types/player.rs:
```rust
#[derive(Debug, Clone, Copy, Default, PartialEq, Eq, Hash, Serialize, Deserialize, JsonSchema)]
#[serde(transparent)]
pub struct PlayerId(pub u8);

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct Player {
    pub id: PlayerId,
    pub life: i32,
    pub mana_pool: ManaPool,
    pub library: Vec<ObjectId>,
    pub hand: Vec<ObjectId>,
    pub graveyard: Vec<ObjectId>,
    pub has_drawn_this_turn: bool,
    pub lands_played_this_turn: u8,
    pub poison_counters: u32,
    pub can_look_at_top_of_library: bool,
}
```

From crates/engine/src/types/game_state.rs:
```rust
pub struct GameState {
    pub players: Vec<Player>,
    pub active_player: PlayerId,
    pub priority_player: PlayerId,
    pub priority_pass_count: u8,
    // ... many other fields
}

impl GameState {
    pub fn new_two_player(seed: u64) -> Self { /* current constructor */ }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define FormatConfig types and GameState/Player extensions</name>
  <files>crates/engine/src/types/format.rs, crates/engine/src/types/mod.rs, crates/engine/src/types/game_state.rs, crates/engine/src/types/player.rs</files>
  <behavior>
    - FormatConfig::standard() returns config with starting_life=20, min_players=2, max_players=2, deck_size=60, singleton=false, command_zone=false
    - FormatConfig::commander() returns config with starting_life=40, min_players=2, max_players=6, deck_size=100, singleton=true, command_zone=true, commander_damage_threshold=Some(21)
    - FormatConfig::free_for_all() returns config with starting_life=20, min_players=2, max_players=6, deck_size=60
    - FormatConfig::two_headed_giant() returns config with starting_life=30, min_players=4, max_players=4, team_based=true
    - FormatConfig roundtrips through serde_json
    - GameState::new(FormatConfig::standard(), 2, seed) produces same player count and life totals as current new_two_player(seed)
    - GameState::new(FormatConfig::commander(), 4, seed) creates 4 players with 40 life each
    - Player has is_eliminated defaulting to false
    - GameState has seat_order Vec<PlayerId> initialized to [0, 1, ..., N-1]
    - GameState has eliminated_players: Vec<PlayerId> initialized empty
    - GameState has commander_damage: HashMap<(PlayerId, ObjectId), u32> initialized empty
    - GameState has priority_passes: BTreeSet<PlayerId> replacing priority_pass_count
    - new_two_player(seed) still works and produces identical results to current behavior
  </behavior>
  <action>
    1. Create `crates/engine/src/types/format.rs`:
       - `GameFormat` enum: Standard, Commander, FreeForAll, TwoHeadedGiant — with Serialize, Deserialize, Clone, Debug, PartialEq, Eq, JsonSchema
       - `FormatConfig` struct with fields: format, starting_life (i32), min_players (u8), max_players (u8), deck_size (u16), singleton (bool), command_zone (bool), commander_damage_threshold (Option<u8>), range_of_influence (Option<u8>), team_based (bool) — with Serialize, Deserialize, Clone, Debug, PartialEq, Eq
       - Factory methods: standard(), commander(), free_for_all(), two_headed_giant()
       - Add `pub mod format;` to types/mod.rs

    2. Extend `Player` in player.rs:
       - Add `pub is_eliminated: bool` field, defaulting to false in Default impl

    3. Extend `GameState` in game_state.rs:
       - Add fields: `seat_order: Vec<PlayerId>`, `format_config: FormatConfig`, `eliminated_players: Vec<PlayerId>`, `commander_damage: HashMap<(PlayerId, ObjectId), u32>`, `priority_passes: BTreeSet<PlayerId>`
       - Keep `priority_pass_count: u8` for now (will be removed after priority.rs migration in Plan 02) to avoid breaking all existing code at once
       - Add `GameState::new(config: FormatConfig, player_count: u8, seed: u64) -> Self` that creates N players with config.starting_life
       - Update `new_two_player(seed)` to delegate to `Self::new(FormatConfig::standard(), 2, seed)`
       - Initialize seat_order to `(0..player_count).map(PlayerId).collect()`
       - Update PartialEq impl to include new fields

    4. Update imports: add `use std::collections::BTreeSet;` and `use super::format::FormatConfig;` in game_state.rs
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine -- format --no-fail-fast && cargo test -p engine -- new_two_player --no-fail-fast && cargo test -p engine -- game_state --no-fail-fast</automated>
  </verify>
  <done>FormatConfig with 4 format factories exists, GameState extended with seat_order/format_config/eliminated_players/commander_damage/priority_passes, Player has is_eliminated, new_two_player still works, all existing tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create player iteration functions</name>
  <files>crates/engine/src/game/players.rs, crates/engine/src/game/mod.rs</files>
  <behavior>
    - next_player(state, PlayerId(0)) with seat_order [0,1,2] returns PlayerId(1)
    - next_player(state, PlayerId(2)) with seat_order [0,1,2] wraps to PlayerId(0)
    - next_player skips eliminated players: seat_order [0,1,2] with player 1 eliminated, next_player(state, 0) returns PlayerId(2)
    - next_player returns current player if they are the only living player
    - opponents(state, PlayerId(0)) with 3 living players returns [PlayerId(1), PlayerId(2)]
    - opponents skips eliminated players
    - apnap_order(state) with active_player=1 and seat_order [0,1,2] returns [1, 2, 0]
    - apnap_order skips eliminated players
    - teammates(state, player) returns empty vec for non-team formats
    - teammates(state, PlayerId(0)) in 2HG returns [PlayerId(1)] (team pairs: 0+1 vs 2+3)
  </behavior>
  <action>
    1. Create `crates/engine/src/game/players.rs`:
       - `pub fn next_player(state: &GameState, current: PlayerId) -> PlayerId` — find current in seat_order, iterate forward skipping eliminated, wrap around
       - `pub fn opponents(state: &GameState, player: PlayerId) -> Vec<PlayerId>` — all living players in seat order except the given player
       - `pub fn apnap_order(state: &GameState) -> Vec<PlayerId>` — living players starting from active_player in seat order
       - `pub fn teammates(state: &GameState, player: PlayerId) -> Vec<PlayerId>` — for team_based formats (2HG: players 0+1 are team A, 2+3 are team B), empty vec otherwise
       - `pub fn is_alive(state: &GameState, player: PlayerId) -> bool` — convenience: player exists and not eliminated

    2. Add `pub mod players;` to game/mod.rs

    3. Write comprehensive unit tests in the same file covering all edge cases above
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine -- players --no-fail-fast</automated>
  </verify>
  <done>players.rs module with next_player, opponents, apnap_order, teammates, is_alive functions. All iteration respects seat_order and skips eliminated players. Tests pass for 2-player, 3-player, and 6-player configurations</done>
</task>

</tasks>

<verification>
- `cargo test -p engine` passes with all existing tests unchanged
- `cargo clippy -p engine -- -D warnings` clean
- FormatConfig serde roundtrip works
- new_two_player backward compatibility preserved
</verification>

<success_criteria>
- FormatConfig enum and struct with 4 format factory methods
- GameState extended with seat_order, format_config, eliminated_players, commander_damage, priority_passes
- Player extended with is_eliminated
- players.rs with next_player, opponents, apnap_order, teammates, is_alive
- All existing engine tests pass unchanged
- New types serialize/deserialize correctly
</success_criteria>

<output>
After completion, create `.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-01-SUMMARY.md`
</output>
