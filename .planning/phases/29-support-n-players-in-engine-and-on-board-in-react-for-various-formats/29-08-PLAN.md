---
phase: 29-support-n-players
plan: 08
type: execute
wave: 6
depends_on: ["29-03", "29-06", "29-07"]
files_modified:
  - client/src/components/controls/AttackTargetPicker.tsx
  - client/src/components/board/battlefields.ts
  - client/src/viewmodel/battlefieldProps.ts
  - client/src/components/controls/ActionButton.tsx
  - client/src/components/board/BlockAssignmentLines.tsx
autonomous: true
requirements:
  - NP-ATTACK-UI
  - NP-BLOCK-UI

must_haves:
  truths:
    - "Player can select per-creature attack targets in multiplayer"
    - "'Attack all' button sends all attackers at a selected target"
    - "Per-creature override allows splitting attacks across opponents"
    - "Blocking UI only shows creatures attacking the blocking player"
    - "Block assignment lines render correctly for multi-defender combat"
  artifacts:
    - path: "client/src/components/controls/AttackTargetPicker.tsx"
      provides: "Per-creature attack target selection UI"
  key_links:
    - from: "client/src/components/controls/AttackTargetPicker.tsx"
      to: "client/src/adapter/types.ts"
      via: "submits DeclareAttackers with AttackTarget"
      pattern: "DeclareAttackers.*attacks"
---

<objective>
Build the combat UI for per-creature attack target selection and multi-defender blocking.

Purpose: In multiplayer MTG, each creature can attack a different opponent or planeswalker. The UI needs to support this cleanly while keeping the common case (attack all at one target) simple.

Output: AttackTargetPicker component, "Attack all" button, updated blocking UI for multi-defender combat.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-CONTEXT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-03-SUMMARY.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-06-SUMMARY.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-07-SUMMARY.md
@client/src/components/controls/ActionButton.tsx
@client/src/components/board/BlockAssignmentLines.tsx
@client/src/adapter/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attack target picker and "Attack all" button</name>
  <files>client/src/components/controls/AttackTargetPicker.tsx, client/src/components/controls/ActionButton.tsx, client/src/components/board/battlefields.ts</files>
  <action>
    1. Create `AttackTargetPicker.tsx`:
       - Shows when WaitingFor is DeclareAttackers and there are multiple valid_attack_targets
       - Two modes:
         a) "Attack all" mode (default): show opponent buttons (one per valid target). Click one to send all selected attackers at that target
         b) "Split attacks" mode: toggle to per-creature assignment. Show each attacker with a dropdown/button to pick its target
       - In 2-player mode: skip the picker entirely (only one valid target), behave exactly like current
       - Submit builds `GameAction::DeclareAttackers { attacks: Vec<(ObjectId, AttackTarget)> }`

    2. Update `ActionButton.tsx`:
       - When DeclareAttackers state and multiple opponents, show "Attack All" button that opens AttackTargetPicker
       - Keep current behavior for 2-player (direct submit)

    3. Update `battlefields.ts`:
       - Helper to get valid attack targets from WaitingFor state
       - Helper to build attacks array from selected attackers + target

    4. Handle planeswalker attack targets: show planeswalker names/images alongside player names as targets
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check && pnpm test -- --run</automated>
  </verify>
  <done>AttackTargetPicker supports per-creature target selection and "Attack all" shortcut. 2-player mode unchanged. Frontend compiles and tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Multi-defender blocking UI</name>
  <files>client/src/components/board/BlockAssignmentLines.tsx, client/src/viewmodel/battlefieldProps.ts</files>
  <action>
    1. Update `BlockAssignmentLines.tsx`:
       - Current: draws lines from blockers to attackers for a single defender
       - New: draw lines only for attackers targeting the blocking player
       - Attackers targeting other players shown but dimmed/non-interactive
       - Group attackers by their target for visual clarity

    2. Update `battlefieldProps.ts`:
       - Filter functions should handle N-player battlefield partitioning
       - Use PlayerArea structural decisions from Plan 07 (see 29-07-SUMMARY.md for component layout)
       - Group attackers by their attack target for rendering

    3. Ensure blocking interaction only allows assigning blockers to creatures attacking the current player
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check && pnpm test -- --run</automated>
  </verify>
  <done>Blocking UI correctly shows only relevant attackers. Block assignment lines render for multi-defender combat. Frontend compiles and tests pass</done>
</task>

</tasks>

<verification>
- `pnpm run type-check` passes
- `pnpm test -- --run` passes
- 2-player combat UI unchanged
</verification>

<success_criteria>
- AttackTargetPicker handles per-creature target selection
- "Attack all" button for common case
- Blocking UI filtered per defender
- Block lines render correctly
- 2-player behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-08-SUMMARY.md`
</output>
