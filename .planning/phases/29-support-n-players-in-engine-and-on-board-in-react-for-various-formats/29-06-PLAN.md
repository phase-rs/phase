---
phase: 29-support-n-players
plan: 06
type: execute
wave: 3
depends_on: ["29-01", "29-02"]
files_modified:
  - crates/engine-wasm/src/lib.rs
  - crates/server-core/src/filter.rs
  - crates/server-core/src/session.rs
  - crates/server-core/src/protocol.rs
  - crates/phase-server/src/main.rs
  - crates/phase-ai/src/legal_actions.rs
  - crates/phase-ai/src/eval.rs
  - crates/phase-ai/src/combat_ai.rs
  - crates/phase-ai/src/card_hints.rs
  - crates/phase-ai/src/search.rs
autonomous: true
requirements:
  - NP-WASM
  - NP-SERVER
  - NP-FILTER
  - NP-AI-MIGRATE

must_haves:
  truths:
    - "WASM bridge accepts player_id parameter for get_ai_action instead of hardcoded PlayerId(1)"
    - "State filtering hides ALL opponents' hands and libraries (not just player 1's)"
    - "Server session manages N player connections per game"
    - "Server broadcasts state updates to all connected players"
    - "AI modules use players:: functions instead of PlayerId(1 - x.0)"
  artifacts:
    - path: "crates/engine-wasm/src/lib.rs"
      provides: "N-player WASM bridge with player_id parameter"
    - path: "crates/server-core/src/filter.rs"
      provides: "N-player state filtering"
    - path: "crates/server-core/src/session.rs"
      provides: "N-player session management"
  key_links:
    - from: "crates/engine-wasm/src/lib.rs"
      to: "crates/phase-ai/src/lib.rs"
      via: "get_ai_action passes player_id"
      pattern: "get_ai_action.*player"
    - from: "crates/server-core/src/filter.rs"
      to: "crates/engine/src/game/players.rs"
      via: "uses opponents() for hand hiding"
      pattern: "players::opponents"
---

<objective>
Update all transport layers (WASM bridge, server, protocol) and AI crate for N-player support.

Purpose: The engine core now supports N players, but the WASM bridge hardcodes PlayerId(1) for AI, the server manages exactly 2 players per session, and state filtering hides only one opponent's hand. All must be extended.

Output: WASM bridge with player_id parameter, N-player server sessions, N-player state filtering, AI crate migrated from opponent() to players:: functions.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-CONTEXT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-01-SUMMARY.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-02-SUMMARY.md
@crates/engine-wasm/src/lib.rs
@crates/server-core/src/filter.rs
@crates/server-core/src/session.rs
@crates/phase-server/src/main.rs
@crates/phase-ai/src/eval.rs
@crates/phase-ai/src/combat_ai.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: WASM bridge and AI crate N-player migration</name>
  <files>crates/engine-wasm/src/lib.rs, crates/phase-ai/src/legal_actions.rs, crates/phase-ai/src/eval.rs, crates/phase-ai/src/combat_ai.rs, crates/phase-ai/src/card_hints.rs, crates/phase-ai/src/search.rs</files>
  <action>
    1. Update `engine-wasm/src/lib.rs`:
       - Change `get_ai_action()` to accept a `player_id: u8` parameter instead of hardcoded `PlayerId(1)`
       - Pass `PlayerId(player_id)` to the AI functions
       - Update TypeScript type generation to reflect the new parameter

    2. Update `phase-ai` crate — replace all `PlayerId(1 - player.0)` with players:: functions:
       - `eval.rs`: Replace opponent references with `players::opponents()` — evaluate threat from all opponents
       - `combat_ai.rs`: Replace `PlayerId(1 - player.0)` with `players::opponents()` — consider attacks against multiple opponents
       - `card_hints.rs`: Replace opponent references
       - `legal_actions.rs`: Replace `new_two_player` in tests with appropriate setup
       - `search.rs`: Replace opponent references — search now considers all opponents' potential responses

    3. Run `rg "PlayerId\(1 -" crates/phase-ai/ crates/engine-wasm/` to verify zero remaining
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine-wasm -p phase-ai --no-fail-fast && rg "PlayerId\(1 -" crates/phase-ai/ crates/engine-wasm/ 2>&1 | grep -c ":" || echo "0 matches"</automated>
  </verify>
  <done>WASM bridge accepts player_id. AI crate uses players:: functions. Zero PlayerId(1-x) in AI or WASM crates. All tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Server session, protocol, and state filtering for N players</name>
  <files>crates/server-core/src/filter.rs, crates/server-core/src/session.rs, crates/server-core/src/protocol.rs, crates/phase-server/src/main.rs</files>
  <action>
    1. Update `filter.rs` `filter_state_for_player`:
       - Instead of hiding just `PlayerId(1 - viewer.0)` hand/library, hide ALL other players' hands and libraries
       - Iterate `players::opponents(state, viewer)` and zero out each opponent's hand/library contents
       - Keep hand sizes visible (card count) but hide actual cards

    2. Update `session.rs`:
       - Change from 2-player session model to N-player
       - Track `players: HashMap<PlayerId, PlayerConnection>` instead of `player1/player2` fields
       - Support up to 6 connections per session
       - Add AI seat tracking: which seats are AI-controlled

    3. Update `protocol.rs`:
       - Add format_config to CreateGameWithSettings message
       - Add player_count field
       - Add SpectatorJoin message type for spectator support
       - Update StateUpdate to include eliminated player info

    4. Update `phase-server/src/main.rs`:
       - Replace all `PlayerId(1 - x.0)` with iteration over session players
       - Broadcast state updates to ALL connected players (not just 2)
       - Handle AI seats: run AI for any seat not occupied by a human
       - Support disconnect/reconnect for any player index

    5. Run `rg "PlayerId\(1 -" crates/server-core/ crates/phase-server/` to verify zero remaining
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p server-core -p phase-server --no-fail-fast && rg "PlayerId\(1 -" crates/server-core/ crates/phase-server/ 2>&1 | grep -c ":" || echo "0 matches"</automated>
  </verify>
  <done>State filtering hides all opponents' data. Sessions support N players. Protocol includes format config and spectator support. Server broadcasts to all players. Zero PlayerId(1-x) in server crates. All tests pass</done>
</task>

</tasks>

<verification>
- `cargo test --all` passes
- `rg "PlayerId\(1 -" crates/` returns zero results across all crates
- `cargo clippy --all-targets -- -D warnings` clean
</verification>

<success_criteria>
- WASM get_ai_action accepts player_id parameter
- State filtering hides all opponents' hands/libraries
- Server sessions support 2-6 players
- Protocol supports format config and spectators
- AI crate fully migrated to N-player
- Zero PlayerId(1-x) remaining in entire codebase
</success_criteria>

<output>
After completion, create `.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-06-SUMMARY.md`
</output>
