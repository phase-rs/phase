---
phase: 29-support-n-players
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - crates/engine/src/game/priority.rs
  - crates/engine/src/game/turns.rs
  - crates/engine/src/game/sba.rs
  - crates/engine/src/game/elimination.rs
  - crates/engine/src/game/mod.rs
  - crates/engine/src/game/mulligan.rs
autonomous: true
requirements:
  - NP-PRIORITY
  - NP-TURNS
  - NP-ELIM

must_haves:
  truths:
    - "Priority passes clockwise through all living players via seat_order"
    - "Stack resolves only when ALL living players pass consecutively"
    - "Any action by any player resets the priority pass set"
    - "Turn rotation follows seat_order, skipping eliminated players"
    - "Player elimination exiles their permanents, removes their spells from stack, and marks them eliminated"
    - "Game ends only when one player/team remains after elimination"
    - "In 2-player games, behavior is identical to current (loss = immediate game over)"
    - "Mulligan proceeds through all players in seat order"
  artifacts:
    - path: "crates/engine/src/game/priority.rs"
      provides: "N-player priority passing with BTreeSet tracking"
    - path: "crates/engine/src/game/turns.rs"
      provides: "Clockwise turn rotation via seat_order"
    - path: "crates/engine/src/game/sba.rs"
      provides: "N-player SBA: elimination instead of immediate game over"
    - path: "crates/engine/src/game/elimination.rs"
      provides: "CR 800.4 elimination cleanup"
  key_links:
    - from: "crates/engine/src/game/priority.rs"
      to: "crates/engine/src/game/players.rs"
      via: "uses next_player for clockwise pass"
      pattern: "players::next_player"
    - from: "crates/engine/src/game/sba.rs"
      to: "crates/engine/src/game/elimination.rs"
      via: "calls eliminate_player on life/poison/commander loss"
      pattern: "elimination::eliminate_player"
---

<objective>
Convert the core game loop (priority, turns, SBAs, mulligan) from hardcoded 2-player to N-player using the foundation types from Plan 01.

Purpose: The game loop is the heart of MTG rules. Priority must pass clockwise through all living players, turns must rotate via seat_order, and player loss must trigger elimination (not immediate game over) in multiplayer.

Output: Rewritten priority.rs, turns.rs, sba.rs, new elimination.rs, updated mulligan.rs. 2-player games behave identically to current.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-CONTEXT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-RESEARCH.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-01-SUMMARY.md
@crates/engine/src/game/priority.rs
@crates/engine/src/game/turns.rs
@crates/engine/src/game/sba.rs
@crates/engine/src/game/mulligan.rs
@crates/engine/src/game/engine.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: N-player priority and turn rotation</name>
  <files>crates/engine/src/game/priority.rs, crates/engine/src/game/turns.rs, crates/engine/src/game/engine.rs</files>
  <behavior>
    - handle_priority_pass with 3 players: first pass by P0 does NOT resolve stack, passes to P1
    - handle_priority_pass with 3 players: P0, P1, P2 all pass consecutively -> stack resolves
    - Any non-pass action clears priority_passes set
    - reset_priority clears priority_passes and sets priority to active_player
    - start_next_turn rotates to next living player in seat_order (not toggle 0/1)
    - start_next_turn skips eliminated players
    - 2-player mode: priority behavior identical to current (2 passes = resolve)
    - All places in engine.rs that set priority_pass_count = 0 now clear priority_passes instead
  </behavior>
  <action>
    1. Rewrite `priority.rs`:
       - `handle_priority_pass`: Insert current priority_player into `state.priority_passes`. Check if `priority_passes.len() >= living_player_count`. If yes, clear set and resolve/advance. If no, advance to next_player via `players::next_player()`.
       - `reset_priority`: Clear `state.priority_passes` (keep clearing priority_pass_count too during transition)
       - Replace `opponent()` function with a deprecated wrapper that calls `players::next_player()` — this function is public and used in other files, so keep it working during migration (Plan 05 removes it)
       - Update all tests to work with N-player priority model

    2. Rewrite `turns.rs` `start_next_turn`:
       - Replace `PlayerId(1 - state.active_player.0)` with `players::next_player(state, state.active_player)`
       - Keep all other turn logic (reset counters, loyalty, day/night, etc.)

    3. Update `engine.rs`:
       - Replace all `state.priority_pass_count = 0` with `state.priority_passes.clear()` (there are ~5 locations)
       - Keep the old field working during transition

    4. Update `mulligan.rs`:
       - Replace hardcoded 2-player mulligan flow with seat_order iteration
       - After all players have kept, start game normally
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine -- priority --no-fail-fast && cargo test -p engine -- turn --no-fail-fast && cargo test -p engine -- mulligan --no-fail-fast</automated>
  </verify>
  <done>Priority passes clockwise through N living players. Turns rotate via seat_order. Mulligan iterates through all players. 2-player behavior unchanged. All priority/turn/mulligan tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Elimination system and N-player SBAs</name>
  <files>crates/engine/src/game/elimination.rs, crates/engine/src/game/sba.rs, crates/engine/src/game/mod.rs</files>
  <behavior>
    - eliminate_player removes player's spells from stack
    - eliminate_player exiles all permanents the eliminated player owns
    - eliminate_player marks player as eliminated (is_eliminated = true, added to eliminated_players)
    - eliminate_player emits PlayerEliminated event
    - Game ends (GameOver) only when 1 or fewer living players remain
    - In 2-player games, losing player = immediate GameOver with winner (matches current behavior)
    - SBA check_player_life calls eliminate_player instead of immediately ending game
    - SBA check_poison_counters calls eliminate_player instead of immediately ending game
    - Eliminated players are skipped by priority and turn rotation (via players.rs functions)
  </behavior>
  <action>
    1. Create `crates/engine/src/game/elimination.rs`:
       - `pub fn eliminate_player(state: &mut GameState, player: PlayerId, events: &mut Vec<GameEvent>)`:
         - Add player to `state.eliminated_players` and set `is_eliminated = true` on Player
         - CR 800.4a: Remove spells they control from stack
         - CR 800.4b: Exile permanents they own from battlefield (use zones::change_zone)
         - Emit `GameEvent::PlayerEliminated { player_id }`
         - Check if game is over: count living players. If <= 1, set WaitingFor::GameOver with winner (last living player or None)
         - For 2-player games, this naturally produces GameOver immediately when one player loses
       - Add `pub mod elimination;` to game/mod.rs

    2. Rewrite `sba.rs` `check_player_life`:
       - Instead of immediately setting GameOver with `PlayerId(1 - loser.0)` as winner, call `elimination::eliminate_player(state, loser, events)`
       - The elimination function handles checking if game should end
       - Check all players, not just first found (multiple can hit 0 life simultaneously)

    3. Rewrite `sba.rs` `check_poison_counters` similarly

    4. Add `PlayerEliminated { player_id: PlayerId }` to GameEvent enum if not already present

    5. Write tests covering: 2-player elimination (immediate GameOver), 3-player elimination (game continues), simultaneous elimination, elimination cleanup (permanents exiled, spells removed)
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine -- elimination --no-fail-fast && cargo test -p engine -- sba --no-fail-fast</automated>
  </verify>
  <done>Elimination system handles CR 800.4 cleanup. SBAs use elimination instead of immediate GameOver. 2-player games still end immediately when one player loses. 3+ player games continue after elimination. All SBA tests pass</done>
</task>

</tasks>

<verification>
- `cargo test -p engine` passes all tests
- `cargo clippy -p engine -- -D warnings` clean
- 2-player smoke test: create game, pass priority twice -> stack resolves
- 3-player scenario: eliminate player 1, verify player 0 and 2 continue playing
</verification>

<success_criteria>
- Priority system uses BTreeSet and passes clockwise through all living players
- Turn rotation uses next_player from players.rs
- Elimination system follows CR 800.4 ordering
- SBAs trigger elimination, not immediate game over
- 2-player backward compatibility preserved
- All existing engine tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-02-SUMMARY.md`
</output>
