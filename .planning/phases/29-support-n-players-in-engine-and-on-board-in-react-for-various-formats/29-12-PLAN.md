---
phase: 29-support-n-players
plan: 12
type: execute
wave: 6
depends_on: ["29-10", "29-11"]
files_modified:
  - client/src/pages/GameSetupPage.tsx
  - client/src/pages/MenuPage.tsx
  - client/src/components/menu/FormatPicker.tsx
  - client/src/components/menu/GamePresets.tsx
  - client/src/services/presets.ts
  - data/precons/README.md
autonomous: false
requirements:
  - NP-SETUP-FLOW
  - NP-PRESETS
  - NP-PRECONS

must_haves:
  truths:
    - "Format-first flow: pick format via big buttons, then format-specific config"
    - "Commander deck selection shows commander on deck tile art"
    - "Game preset saving and loading for quick-start"
    - "Pre-built Commander decks available for immediate play"
    - "Menu state machine handles format selection correctly"
  artifacts:
    - path: "client/src/pages/GameSetupPage.tsx"
      provides: "Format-first game setup flow"
    - path: "client/src/components/menu/FormatPicker.tsx"
      provides: "Big format selection buttons"
    - path: "client/src/components/menu/GamePresets.tsx"
      provides: "Saved game presets"
  key_links:
    - from: "client/src/components/menu/FormatPicker.tsx"
      to: "client/src/pages/GameSetupPage.tsx"
      via: "format selection triggers format-specific config"
      pattern: "onFormatSelect"
---

<objective>
Build the format-first game setup flow with presets and pre-built Commander decks.

Purpose: The game setup experience should be smooth — pick a format, configure it, select decks, and start. Presets allow quick-start for repeated setups. Pre-built Commander decks enable immediate Commander play.

Output: GameSetupPage with format-first flow, FormatPicker component, game presets system, 4-5 pre-built Commander decks.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-CONTEXT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-10-SUMMARY.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-11-SUMMARY.md
@client/src/pages/MenuPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Format-first game setup flow</name>
  <files>client/src/pages/GameSetupPage.tsx, client/src/pages/MenuPage.tsx, client/src/components/menu/FormatPicker.tsx</files>
  <action>
    1. Create `FormatPicker.tsx`:
       - Big visual buttons for each format: Standard, Commander, Free-for-All, Two-Headed Giant
       - Each button shows: format name, icon, brief description ("1v1, 20 life", "2-6 players, 40 life"), player count range
       - Visual styling: cards/tiles layout, color-coded per format
       - Selection calls `onFormatSelect(format: GameFormat)`

    2. Create or update `GameSetupPage.tsx`:
       - Step 1: FormatPicker (if not already selected)
       - Step 2: Format-specific config (starting life, player count, house rule overrides)
       - Step 3: Deck selection — filtered by format. Commander shows commander card on deck tile
       - Step 4: Mode selection (vs AI, Online, P2P) — 2-player formats allow P2P, 3+ must use server
       - Step 5: If online, transition to HostSetup/ReadyRoom flow
       - Step 5: If vs AI, configure AI seats and start
       - Navigation: back button returns to previous step

    3. Update `MenuPage.tsx`:
       - Replace or augment current flow to start with format selection
       - "Quick Play" option that skips format picker and uses saved preset
       - Preserve current menu for backward compat (Standard 2-player is default)
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check</automated>
  </verify>
  <done>Format-first setup flow works. FormatPicker renders 4 formats. Format-specific config screens render. Menu updated with format selection. TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Game presets and pre-built Commander decks</name>
  <files>client/src/components/menu/GamePresets.tsx, client/src/services/presets.ts, data/precons/README.md</files>
  <action>
    1. Create `services/presets.ts`:
       - `GamePreset` type: { name, format, formatConfig, deckId?, aiDifficulty?, playerCount }
       - Save presets to localStorage
       - Load/delete presets
       - Default presets: "Quick Standard (vs Medium AI)", "Quick Commander (4-player FFA)"

    2. Create `GamePresets.tsx`:
       - Shows saved presets as clickable cards
       - "Save current setup" button on GameSetupPage
       - Delete preset with confirmation
       - Default presets shown if no saved presets

    3. Create pre-built Commander decks:
       - 4-5 decks covering different color combinations:
         - Mono-White (equipment/tokens)
         - Blue-Black (control)
         - Red-Green (stompy/ramp)
         - White-Blue-Black (Esper control)
         - Red-Green-White (Naya aggro)
       - Each deck: 100 cards, valid Commander deck (singleton, color identity)
       - Use cards already in the engine's card database (no new cards)
       - Store as .dck files in `data/precons/` or similar
       - Commander auto-detected from sideboard line

    4. Wire precons into deck selection: show pre-built decks when Commander format selected, with "Pre-built" badge
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check && pnpm test -- --run</automated>
  </verify>
  <done>Game presets save/load from localStorage. Pre-built Commander decks available. Presets shown in setup flow. Tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of game setup flow</name>
  <action>Human verifies the format-first setup flow</action>
  <what-built>Format-first game setup flow with FormatPicker, game presets, pre-built Commander decks</what-built>
  <how-to-verify>
    1. Start dev server: `cd client && pnpm dev`
    2. Navigate to menu — verify format picker shows 4 formats with visual buttons
    3. Select Commander — verify Commander-specific config (starting life 40, player count 2-6)
    4. Check deck selection shows pre-built Commander decks with commander card art
    5. Verify "Save as preset" works and preset appears in menu
    6. Verify back navigation through setup steps works
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm run type-check` passes
- `pnpm test -- --run` passes
- Format-first flow is intuitive
- Presets save and load correctly
</verification>

<success_criteria>
- Format-first game setup with big format buttons
- Format-specific config screens
- Game presets save/load
- Pre-built Commander decks (4-5)
- Menu flow handles format selection correctly
- Human verification of setup UX
</success_criteria>

<output>
After completion, create `.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-12-SUMMARY.md`
</output>
