---
phase: 29-support-n-players
plan: 04
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - crates/engine/src/game/commander.rs
  - crates/engine/src/game/mod.rs
  - crates/engine/src/game/casting.rs
  - crates/engine/src/game/zones.rs
  - crates/engine/src/types/zones.rs
  - crates/engine/src/game/deck_loading.rs
autonomous: true
requirements:
  - NP-COMMANDER
  - NP-CMDZONE
  - NP-CMDTAX

must_haves:
  truths:
    - "Commander cards start in the command zone"
    - "Casting a commander from command zone costs +2 generic mana per previous cast (commander tax)"
    - "Commander tax counter persists across zone changes"
    - "When a commander would go to graveyard or exile, owner may redirect to command zone"
    - "Color identity enforcement prevents casting off-identity cards in Commander format"
    - "Deck validation enforces singleton rule for Commander format"
  artifacts:
    - path: "crates/engine/src/game/commander.rs"
      provides: "Commander zone management, tax calculation, zone redirection"
      exports: ["apply_commander_tax", "can_cast_in_color_identity", "redirect_to_command_zone"]
  key_links:
    - from: "crates/engine/src/game/commander.rs"
      to: "crates/engine/src/game/casting.rs"
      via: "commander tax applied during spell cost calculation"
      pattern: "commander_tax"
    - from: "crates/engine/src/game/commander.rs"
      to: "crates/engine/src/game/zones.rs"
      via: "zone redirection when commander would leave battlefield"
      pattern: "redirect_to_command_zone"
---

<objective>
Implement Commander-specific rules: command zone, commander tax, zone redirection, and color identity enforcement.

Purpose: Commander is the most popular casual MTG format. These rules are essential for Commander games to function correctly per the comprehensive rules.

Output: commander.rs module with all Commander-specific logic, integrated into casting and zone management. Command zone already exists as Zone::Command in the engine.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-CONTEXT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-RESEARCH.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-01-SUMMARY.md
@crates/engine/src/game/casting.rs
@crates/engine/src/game/zones.rs
@crates/engine/src/types/zones.rs
@crates/engine/src/game/deck_loading.rs

<interfaces>
From crates/engine/src/types/zones.rs:
```rust
pub enum Zone {
    Library, Hand, Battlefield, Graveyard, Stack, Exile, Command,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commander module with tax, zone redirection, and color identity</name>
  <files>crates/engine/src/game/commander.rs, crates/engine/src/game/mod.rs</files>
  <behavior>
    - commander_tax(state, player, commander_id) returns 2 * times_cast_from_command_zone
    - First cast from command zone: 0 extra tax. Second: +2. Third: +4
    - Commander tax tracks per-commander (partner support: two separate counters)
    - redirect_to_command_zone: when commander would move to graveyard or exile, redirect to command zone instead
    - redirect_to_command_zone does NOT redirect when moving to hand, library, or within command zone
    - can_cast_in_color_identity: checks if a card's colors are subset of commander's color identity
    - Color identity enforcement only active when format_config.command_zone is true
    - validate_commander_deck: 100 cards, singleton (no more than 1 copy except basics), all cards within commander's color identity
  </behavior>
  <action>
    1. Create `crates/engine/src/game/commander.rs`:
       - Add `commander_cast_count: HashMap<ObjectId, u32>` field to GameState (tracks how many times each commander was cast from command zone)
       - `pub fn commander_tax(state: &GameState, commander_id: ObjectId) -> u32` — returns 2 * cast_count for this commander
       - `pub fn record_commander_cast(state: &mut GameState, commander_id: ObjectId)` — increments count
       - `pub fn should_redirect_to_command_zone(state: &GameState, object_id: ObjectId, destination: Zone) -> bool` — returns true if object is a commander and destination is Graveyard or Exile
       - `pub fn can_cast_in_color_identity(state: &GameState, card_colors: &[ManaColor], player: PlayerId) -> bool` — checks card colors against commander's color identity
       - `pub fn validate_commander_deck(deck: &[CardId], commander_ids: &[CardId], database: &CardDatabase) -> Result<(), Vec<String>>` — validates singleton, size, color identity

    2. Add `pub mod commander;` to game/mod.rs

    3. Add `commander_cast_count: HashMap<ObjectId, u32>` to GameState with `#[serde(default)]`

    4. Write comprehensive tests:
       - Commander tax increments correctly
       - Zone redirection works for graveyard and exile but not hand/library
       - Color identity validation (mono-red commander blocks blue cards)
       - Deck validation catches non-singleton, wrong colors, wrong size
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine -- commander --no-fail-fast</automated>
  </verify>
  <done>Commander module with tax calculation, zone redirection, color identity validation, and deck validation. All commander tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Integrate commander rules into casting and zone management</name>
  <files>crates/engine/src/game/casting.rs, crates/engine/src/game/zones.rs, crates/engine/src/game/deck_loading.rs</files>
  <action>
    1. Update `casting.rs`:
       - When calculating spell cost, if the source is in Zone::Command, add commander_tax() to the generic mana cost
       - After successfully casting from command zone, call record_commander_cast()
       - If format_config.command_zone is true, allow casting from Zone::Command (add Command to legal cast zones)
       - Check color identity before allowing cast in Commander format

    2. Update `zones.rs`:
       - In zone change functions, check should_redirect_to_command_zone() before moving a commander to graveyard or exile
       - If redirect applies, change destination to Zone::Command

    3. Update `deck_loading.rs`:
       - When format_config.command_zone is true, put designated commander cards in Zone::Command instead of library
       - Mark commander GameObjects with is_commander = true (the flag added in Plan 03)
       - Support partner commanders (up to 2 commanders)

    4. Write integration test: create Commander game, cast commander from command zone, let it die, verify it returns to command zone, cast again with +2 tax
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine -- commander --no-fail-fast && cargo test -p engine -- casting --no-fail-fast && cargo test -p engine -- deck_loading --no-fail-fast</automated>
  </verify>
  <done>Commander tax applied during casting, zone redirection active for commanders, deck loading puts commanders in command zone. Integration test passes full commander lifecycle</done>
</task>

</tasks>

<verification>
- `cargo test -p engine` all tests pass
- Commander cast from command zone -> dies -> command zone -> recast with +2 works
- Non-Commander format games unaffected (no tax, no redirection)
</verification>

<success_criteria>
- Commander zone management fully functional
- Commander tax correctly calculated and applied
- Zone redirection intercepts graveyard/exile for commanders
- Color identity enforcement blocks illegal casts
- Deck validation works for Commander format
- No impact on Standard/FFA/2HG games
</success_criteria>

<output>
After completion, create `.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-04-SUMMARY.md`
</output>
