---
phase: 29-support-n-players
plan: 10
type: execute
wave: 5
depends_on: ["29-06", "29-07"]
files_modified:
  - client/src/components/lobby/LobbyView.tsx
  - client/src/components/lobby/HostSetup.tsx
  - client/src/components/lobby/WaitingScreen.tsx
  - client/src/components/lobby/ReadyRoom.tsx
  - client/src/stores/multiplayerStore.ts
  - client/src/adapter/WebSocketAdapter.ts
  - client/src/adapter/WasmAdapter.ts
autonomous: true
requirements:
  - NP-LOBBY
  - NP-READY-UP
  - NP-SPECTATOR
  - NP-DISCONNECT

must_haves:
  truths:
    - "Lobby shows format type and current/max player count for each game"
    - "Host setup includes format selection with per-format config"
    - "Ready room shows player list with ready status and AI seat configuration"
    - "Host starts game when enough players are ready"
    - "Format rules enforce min/max player counts"
    - "Spectators can join and watch with hidden-info view"
    - "Disconnected players trigger game pause with 2-min timeout"
    - "AI fills empty seats with configurable difficulty"
  artifacts:
    - path: "client/src/components/lobby/ReadyRoom.tsx"
      provides: "Pre-game ready room with player list and AI config"
    - path: "client/src/stores/multiplayerStore.ts"
      provides: "Extended multiplayer store with format and ready state"
  key_links:
    - from: "client/src/components/lobby/HostSetup.tsx"
      to: "client/src/stores/multiplayerStore.ts"
      via: "format config flows to store and then to server"
      pattern: "setFormatConfig"
    - from: "client/src/components/lobby/ReadyRoom.tsx"
      to: "client/src/adapter/WebSocketAdapter.ts"
      via: "ready toggle and game start sent via WebSocket"
      pattern: "sendReady"
---

<objective>
Update the lobby, host setup, and networking for format-aware multiplayer with ready-up system.

Purpose: Players need to create format-specific games, join lobbies with correct player counts, ready up in a pre-game room, and have AI fill empty seats. Spectators and disconnect handling are also needed.

Output: Format-aware lobby UI, ready room component, extended multiplayer store, updated WebSocket adapter.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-CONTEXT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-06-SUMMARY.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-07-SUMMARY.md
@client/src/components/lobby/LobbyView.tsx
@client/src/components/lobby/HostSetup.tsx
@client/src/components/lobby/WaitingScreen.tsx
@client/src/stores/multiplayerStore.ts
@client/src/adapter/WebSocketAdapter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Format-aware host setup and lobby</name>
  <files>client/src/components/lobby/HostSetup.tsx, client/src/components/lobby/LobbyView.tsx, client/src/stores/multiplayerStore.ts</files>
  <action>
    1. Update `multiplayerStore.ts`:
       - Add `formatConfig: FormatConfig | null` to store
       - Add `playerSlots: PlayerSlot[]` — array of { playerId, name, isReady, isAi, aiDifficulty }
       - Add `setFormatConfig`, `setPlayerSlots`, `toggleReady` actions
       - Add `spectators: string[]` for spectator names

    2. Update `HostSetup.tsx`:
       - Add format selection: big buttons for Standard, Commander, Free-for-All, Two-Headed Giant
       - Show format-specific settings after selection:
         - Starting life (editable for house rules)
         - Player count selector (bounded by format min/max)
         - Commander damage threshold (for Commander)
         - Timer settings (existing)
         - Password/visibility (existing)
       - AI seat configuration: for each non-host seat, toggle human/AI with difficulty picker
       - AI deck assignment: pick specific deck or "random" per AI seat

    3. Update `LobbyView.tsx`:
       - Show format badge on each listed game (color-coded: Standard=blue, Commander=purple, FFA=orange, 2HG=green)
       - Show current/max player count: "2/4 players"
       - Filter by format (optional filter buttons)
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check</automated>
  </verify>
  <done>Host setup has format selection with per-format config. Lobby shows format badges and player counts. Multiplayer store extended with format and player slot data. TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Ready room, spectator support, and disconnect handling</name>
  <files>client/src/components/lobby/ReadyRoom.tsx, client/src/components/lobby/WaitingScreen.tsx, client/src/adapter/WebSocketAdapter.ts, client/src/adapter/WasmAdapter.ts</files>
  <action>
    1. Create `ReadyRoom.tsx`:
       - Shows after creating/joining a game, before game starts
       - Player list: name, deck name (hidden for opponents), ready status indicator
       - AI seats shown with difficulty badge
       - Ready toggle button for current player
       - Host sees "Start Game" button — enabled when enough players are ready (min_players from format)
       - Chat area (simple text messages between players in room)
       - Auto-transition to game when host starts

    2. Update `WaitingScreen.tsx`:
       - Replace simple waiting with ReadyRoom when multiple players
       - Keep existing waiting behavior for 2-player P2P games

    3. Update `WebSocketAdapter.ts`:
       - Handle new protocol messages: ReadyToggle, SpectatorJoin, GamePaused, GameResumed
       - Implement disconnect handling: when PlayerDisconnected received, show pause overlay with countdown
       - After 2-minute timeout, offer AI takeover for disconnected seat
       - Reconnection: handle Reconnect message for any player index

    4. Update `WasmAdapter.ts`:
       - Support N AI players: when game has AI seats, run get_ai_action for each AI player's turn
       - Pass correct player_id for each AI seat
       - Per-seat difficulty configuration
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check && pnpm test -- --run</automated>
  </verify>
  <done>Ready room shows player list with ready status. Spectator join works. Disconnect pauses game. AI fills seats. All frontend tests pass</done>
</task>

</tasks>

<verification>
- `pnpm run type-check` passes
- `pnpm test -- --run` passes
- Format-specific host setup renders correctly
- Ready room shows correct player states
</verification>

<success_criteria>
- Format-aware lobby with badges and player counts
- Host setup with format selection and per-format config
- Ready room with player list and ready toggles
- AI seat configuration with per-seat difficulty
- Spectator support
- Disconnect pause with timeout
- All frontend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-10-SUMMARY.md`
</output>
