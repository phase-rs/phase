---
phase: 29-support-n-players
plan: 11
type: execute
wave: 6
depends_on: ["29-07", "29-10"]
files_modified:
  - client/src/pages/DeckBuilderPage.tsx
  - client/src/components/deck-builder/CommanderPanel.tsx
  - client/src/components/deck-builder/FormatFilter.tsx
  - client/src/components/deck-builder/LegalityBadge.tsx
  - client/src/services/deckParser.ts
  - crates/engine/src/database/json_loader.rs
autonomous: true
requirements:
  - NP-DECKBUILDER
  - NP-LEGALITY
  - NP-COMMANDER-DECK

must_haves:
  truths:
    - "Deck builder supports Commander format: commander designation, 100-card singleton, color identity validation"
    - "Format legality badges shown per card from MTGJSON data"
    - "Format filter in card search narrows to legal cards"
    - "Commander selection highlights commander in deck and enforces color identity"
    - "Deck import auto-detects commander from sideboard slot"
    - "Partner commander support (two commanders, combined color identity)"
  artifacts:
    - path: "client/src/components/deck-builder/CommanderPanel.tsx"
      provides: "Commander designation and color identity display"
    - path: "client/src/components/deck-builder/LegalityBadge.tsx"
      provides: "Format legality badges per card"
    - path: "client/src/components/deck-builder/FormatFilter.tsx"
      provides: "Format filter for card search"
  key_links:
    - from: "client/src/components/deck-builder/CommanderPanel.tsx"
      to: "client/src/pages/DeckBuilderPage.tsx"
      via: "commander selection updates deck state"
      pattern: "setCommander"
    - from: "client/src/components/deck-builder/LegalityBadge.tsx"
      to: "client/src/adapter/types.ts"
      via: "reads legalities from card data"
      pattern: "legalities"
---

<objective>
Add Commander deck builder support and format legality features.

Purpose: Players need to build Commander decks with proper rule enforcement (singleton, color identity, 100 cards) and see format legality information for all cards.

Output: Commander panel in deck builder, legality badges, format filter, deck import with commander auto-detection.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-CONTEXT.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-07-SUMMARY.md
@.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-10-SUMMARY.md
@client/src/pages/DeckBuilderPage.tsx
@client/src/services/deckParser.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commander deck builder with color identity and legality</name>
  <files>client/src/pages/DeckBuilderPage.tsx, client/src/components/deck-builder/CommanderPanel.tsx, client/src/components/deck-builder/FormatFilter.tsx, client/src/components/deck-builder/LegalityBadge.tsx</files>
  <action>
    1. Create `CommanderPanel.tsx`:
       - Shows designated commander(s) with large card art
       - Color identity display (WUBRG pips for commander's identity)
       - "Set as Commander" button on eligible legendary creatures
       - Partner support: up to 2 commanders, combined color identity
       - Validation warnings: cards outside color identity highlighted in red, card count (should be 100), singleton violations
       - Commander(s) stored as part of deck metadata

    2. Create `LegalityBadge.tsx`:
       - Small badge showing format legality per card: "Legal", "Banned", "Restricted"
       - Color coded: green=legal, red=banned, yellow=restricted
       - Uses MTGJSON `legalities` field from card data
       - Shows for selected format (changes when format filter changes)

    3. Create `FormatFilter.tsx`:
       - Dropdown or button group: Standard, Commander, Modern, Pioneer, Legacy, Vintage, Pauper
       - When selected, card search results filter to only legal cards in that format
       - Works with existing card search infrastructure

    4. Update `DeckBuilderPage.tsx`:
       - Add format selector at top (determines deck rules)
       - When Commander selected: show CommanderPanel, enforce 100-card singleton, show color identity warnings
       - When Standard selected: enforce 60-card minimum, 4-copy max
       - Show LegalityBadge on each card in search results
       - Show deck validation summary (card count, format violations)

    5. Update card data loading to include `legalities` and `colorIdentity` from MTGJSON:
       - Add these fields to the card type if not already present
       - May need to update `json_loader.rs` to include legalities in card-data.json export
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check && pnpm test -- --run</automated>
  </verify>
  <done>Deck builder supports Commander format with color identity enforcement. Legality badges shown. Format filter works. Partner commander support. TypeScript compiles and tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Deck import with commander auto-detection and MTGJSON legalities</name>
  <files>client/src/services/deckParser.ts, crates/engine/src/database/json_loader.rs</files>
  <action>
    1. Update `deckParser.ts`:
       - Add commander auto-detection: cards in "Sideboard" or "Commander" section of .dck/.dec files are treated as commanders
       - Parse commander designation from deck files
       - Support common commander deck formats (MTGO, Archidekt, Moxfield export)
       - Return `{ mainboard, commander: CardId[], format?: string }` from parse

    2. Update `json_loader.rs` (if needed):
       - Add `legalities: HashMap<String, String>` to `AtomicCard` struct if not already present
       - Include legalities in `card-data.json` export so frontend has access
       - Add `color_identity: Vec<ManaColor>` to `AtomicCard` struct if not already present
       - Include color_identity in card-data.json

    3. If card-data.json changes are needed, update the card-data-export binary to include these fields
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check && pnpm test -- --run && cd /Users/matt/dev/forge.rs && cargo test -p engine -- json_loader --no-fail-fast</automated>
  </verify>
  <done>Deck import auto-detects commander. MTGJSON legalities and colorIdentity included in card data. Frontend can read legality and color identity per card. Tests pass</done>
</task>

</tasks>

<verification>
- `pnpm run type-check` passes
- `pnpm test -- --run` passes
- `cargo test -p engine` passes
- Commander deck builder enforces rules
</verification>

<success_criteria>
- Commander deck builder with designation, color identity, singleton enforcement
- Format legality badges from MTGJSON data
- Format filter in card search
- Deck import auto-detects commander
- Partner commander support
- MTGJSON legalities/colorIdentity available to frontend
</success_criteria>

<output>
After completion, create `.planning/phases/29-support-n-players-in-engine-and-on-board-in-react-for-various-formats/29-11-SUMMARY.md`
</output>
