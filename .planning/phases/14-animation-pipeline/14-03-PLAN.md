---
phase: 14-animation-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - client/src/components/animation/ParticleCanvas.tsx
  - client/src/components/animation/FloatingNumber.tsx
  - client/src/components/animation/ScreenShake.tsx
  - client/src/components/animation/DamageVignette.tsx
  - client/src/components/animation/TurnBanner.tsx
  - client/src/components/animation/CardRevealBurst.tsx
autonomous: true
requirements: [VFX-02, VFX-03, VFX-04, VFX-05, VFX-08]

must_haves:
  truths:
    - "Floating damage/heal numbers respect animation speed multiplier"
    - "Screen shake fires on combat damage at 3 intensity levels via CSS transform"
    - "Card entry triggers scale 0.8->1.0 with WUBRG particle burst"
    - "Damage vignette flashes red radial gradient from edges, opacity scales with damage"
    - "Turn banner slides in from left, pauses, slides out right on turn start"
    - "All VFX respect quality tiers: full (everything), reduced (no shake/vignette, 50% particles no glow), minimal (floating numbers + text banners only)"
  artifacts:
    - path: "client/src/components/animation/ScreenShake.tsx"
      provides: "Screen shake function via CSS transform on game container"
      exports: ["applyScreenShake"]
    - path: "client/src/components/animation/DamageVignette.tsx"
      provides: "Red radial gradient overlay on player damage"
      exports: ["DamageVignette"]
    - path: "client/src/components/animation/TurnBanner.tsx"
      provides: "Turn announcement banner with slide animation"
      exports: ["TurnBanner"]
    - path: "client/src/components/animation/CardRevealBurst.tsx"
      provides: "Scale + particle burst on card battlefield entry"
      exports: ["CardRevealBurst"]
  key_links:
    - from: "client/src/components/animation/ScreenShake.tsx"
      to: "client/src/animation/types.ts"
      via: "reads SPEED_MULTIPLIERS for duration scaling"
      pattern: "SPEED_MULTIPLIERS"
    - from: "client/src/components/animation/ParticleCanvas.tsx"
      to: "client/src/stores/preferencesStore.ts"
      via: "reads vfxQuality to gate glow and particle count"
      pattern: "vfxQuality"
---

<objective>
Create all VFX components: enhanced particles with glow/gravity, floating number speed support, screen shake, damage vignette, turn banner, and card reveal burst. Each component respects VFX quality tiers and animation speed.

Purpose: These are the visual feedback components that make game state changes feel responsive and immersive. They are consumed by the AnimationOverlay refactor in Plan 04.
Output: Six component files ready to be wired into AnimationOverlay.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-animation-pipeline/14-CONTEXT.md
@.planning/phases/14-animation-pipeline/14-RESEARCH.md
@.planning/phases/14-animation-pipeline/14-01-SUMMARY.md
@.planning/phases/14-animation-pipeline/14-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
```typescript
// animation/types.ts
export type VfxQuality = 'full' | 'reduced' | 'minimal';
export type AnimationSpeed = 'slow' | 'normal' | 'fast' | 'instant';
export const SPEED_MULTIPLIERS: Record<AnimationSpeed, number>;
```

```typescript
// animation/wubrgColors.ts
export const WUBRG_COLORS: Record<ManaColor | 'Colorless', string>;
export function getCardColors(colors: ManaColor[]): string[];
```

```typescript
// stores/preferencesStore.ts
// vfxQuality: VfxQuality, animationSpeed: AnimationSpeed
```

<!-- Existing components to enhance -->
From client/src/components/animation/ParticleCanvas.tsx:
```typescript
export interface ParticleCanvasHandle {
  emitBurst: (x: number, y: number, color: string, count: number) => void;
  emitTrail: (from: { x: number; y: number }, to: { x: number; y: number }, color: string) => void;
}
// Uses forwardRef, fixed radius=3, no glow, no gravity, no variable size
```

From client/src/components/animation/FloatingNumber.tsx:
```typescript
interface FloatingNumberProps {
  value: number;
  position: { x: number; y: number };
  color: string;
  onComplete: () => void;
}
// Hardcoded duration: 0.8s, no speed multiplier support
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance ParticleCanvas with glow, gravity, and variable size; add speed to FloatingNumber</name>
  <files>client/src/components/animation/ParticleCanvas.tsx, client/src/components/animation/FloatingNumber.tsx</files>
  <action>
    Enhance `ParticleCanvas.tsx`:
    - Add `radius` field to Particle interface: random 2-6px (per user decision)
    - Add `gravity` field to Particle interface: slight downward pull (e.g., 0.03-0.05)
    - In tick loop: apply `p.vy += p.gravity` before position update
    - In tick loop: use `p.radius` instead of hardcoded `3` in `ctx.arc()`
    - Read `vfxQuality` from `usePreferencesStore.getState()` (non-reactive, read once per tick cycle or cache in a ref updated via subscription)
    - When `vfxQuality === 'full'`: enable `ctx.shadowBlur = 6` and `ctx.shadowColor = p.color` — set ONCE before the particle loop, reset ONCE after (per research anti-pattern guidance — NOT per particle)
    - When `vfxQuality === 'reduced'`: no glow (no shadowBlur), particle count passed to emitBurst is halved
    - When `vfxQuality === 'minimal'`: skip rendering entirely (early return in tick, don't push particles in emitBurst)
    - Add `emitBurst` param for quality-adjusted count: accept a `qualityAdjust?: boolean` flag, or let the caller handle count reduction (prefer caller handles it since AnimationOverlay will read quality)
    - Actually, simpler approach: read vfxQuality inside emitBurst and halve count internally when reduced, skip when minimal. This keeps the quality logic centralized in the particle system.

    Enhance `FloatingNumber.tsx`:
    - Add optional `speedMultiplier` prop (default: 1.0)
    - Apply to transition duration: `duration: 0.8 * speedMultiplier`
    - Apply to y animation distance: keep -60 (distance is speed-independent, just timing changes)
    - When `vfxQuality === 'minimal'`: still render (floating numbers show in all quality tiers per user decision)
    - Add `scale` initial animation: `initial: { opacity: 1, y: 0, scale: 1.2 }` → `animate: { opacity: 0, y: -60, scale: 1.0 }` (scale-in effect per requirement VFX-02)
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check</automated>
  </verify>
  <done>ParticleCanvas renders particles with variable radius (2-6px), gravity pull, and glow (full quality only). FloatingNumber accepts speedMultiplier prop and has scale-in animation. Both respect VFX quality tiers.</done>
</task>

<task type="auto">
  <name>Task 2: Create ScreenShake, DamageVignette, TurnBanner, and CardRevealBurst components</name>
  <files>client/src/components/animation/ScreenShake.tsx, client/src/components/animation/DamageVignette.tsx, client/src/components/animation/TurnBanner.tsx, client/src/components/animation/CardRevealBurst.tsx</files>
  <action>
    Create `ScreenShake.tsx`:
    - Export `applyScreenShake(element: HTMLElement, intensity: 'light' | 'medium' | 'heavy', speedMultiplier: number): void`
    - NOT a React component — a plain function that applies CSS transform animation via requestAnimationFrame
    - Intensity config per user decision:
      - light (1-3 dmg): amplitude +-2px, 150ms, 4 oscillations
      - medium (4-6 dmg): amplitude +-4px, 250ms, 5 oscillations
      - heavy (7+ dmg): amplitude +-8px, 350ms, 6 oscillations
    - Duration scaled by speedMultiplier
    - Decaying sine wave: `Math.sin(progress * oscillations * PI * 2) * amplitude * (1 - progress)`
    - Apply translate on both X and Y (Y at 50% of X amplitude per research example)
    - Reset `element.style.transform = ''` when complete
    - Quality gating: caller checks vfxQuality before calling (full only, not reduced or minimal)

    Create `DamageVignette.tsx`:
    - Framer Motion `motion.div` with `AnimatePresence`
    - Props: `active: boolean`, `damageAmount: number`, `speedMultiplier: number`
    - Style: `position: fixed`, `inset: 0`, `pointer-events: none`, `z-index: 45`
    - Background: `radial-gradient(ellipse at center, transparent 40%, rgba(239,68,68,OPACITY) 100%)`
    - Opacity scales with damage: clamp(damageAmount * 0.15, 0.2, 0.8)
    - Transition: 200ms fade-in, 300ms fade-out (per user decision), scaled by speedMultiplier
    - Quality gating: render null when vfxQuality is 'reduced' or 'minimal'

    Create `TurnBanner.tsx`:
    - Props: `turnNumber: number`, `isPlayerTurn: boolean`, `speedMultiplier: number`
    - Framer Motion `motion.div` with `AnimatePresence`
    - Slide-through animation per user decision:
      - initial: `{ x: '-100%', opacity: 0 }`
      - animate: `{ x: '0%', opacity: 1 }` with pause via keyframes or two-phase animation
      - exit: `{ x: '100%', opacity: 0 }`
    - Pause ~600ms in center (use Framer Motion `transition` with delay or keyframes approach)
    - Style: fixed position, centered vertically, full width, semi-transparent dark strip (`bg-black/70`)
    - Text: "Turn {N} — Your Turn" or "Turn {N} — Opponent's Turn" (per user decision — turn start only, no phase banners)
    - Quality tiers: full/reduced = animated slide, minimal = simpler text-only fade (no slide animation)
    - Duration scaled by speedMultiplier
    - Auto-dismiss: call onComplete after full animation cycle (slide in + pause + slide out)

    Create `CardRevealBurst.tsx`:
    - Props: `position: { x: number; y: number }`, `colors: string[]` (WUBRG colors for the card), `speedMultiplier: number`, `onComplete: () => void`, `particleRef: RefObject<ParticleCanvasHandle>`
    - Framer Motion `motion.div` at the card position that scales 0.8 -> 1.0 over ~300ms (per user decision)
    - On mount, emit WUBRG-colored particle burst from center position via particleRef
    - Card color matches the card's color identity (passed as `colors` prop; use first color for single, cycle for multi)
    - Quality: full = scale + particles, reduced = scale + particles (50% count handled by ParticleCanvas), minimal = no burst (return null)
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check</automated>
  </verify>
  <done>Four VFX components created: ScreenShake (CSS transform function), DamageVignette (red radial gradient overlay), TurnBanner (slide-through announcement), CardRevealBurst (scale + particle burst). All respect VFX quality tiers and animation speed multiplier.</done>
</task>

</tasks>

<verification>
- `cd /Users/matt/dev/forge.rs/client && pnpm run type-check` — no type errors
- `cd /Users/matt/dev/forge.rs/client && pnpm test -- --run` — all existing tests still pass
</verification>

<success_criteria>
- ParticleCanvas has glow (shadowBlur, full quality only), gravity, variable radius (2-6px)
- FloatingNumber accepts speedMultiplier prop, has scale-in animation
- applyScreenShake function handles 3 intensity levels with decaying oscillation
- DamageVignette shows red gradient overlay with opacity scaled by damage amount
- TurnBanner slides in/pauses/slides out with correct text format
- CardRevealBurst scales 0.8->1.0 with WUBRG particle burst
- All components gate on VFX quality and apply speed multiplier
</success_criteria>

<output>
After completion, create `.planning/phases/14-animation-pipeline/14-03-SUMMARY.md`
</output>
