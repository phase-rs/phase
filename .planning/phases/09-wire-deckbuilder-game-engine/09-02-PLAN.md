---
phase: 09-wire-deckbuilder-game-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/services/deckParser.ts
  - client/src/data/starterDecks.ts
  - client/src/constants/storage.ts
autonomous: true
requirements: [DECK-01, DECK-03]

must_haves:
  truths:
    - "MTGA text format decks can be parsed into ParsedDeck structures"
    - "Auto-detection distinguishes MTGA text format from Forge .dck format"
    - "Starter decks are available as typed constants with name, colorIdentity, and card entries"
    - "Storage keys are defined as shared constants (not string literals scattered in files)"
  artifacts:
    - path: "client/src/services/deckParser.ts"
      provides: "parseMtgaDeck function, detectAndParseDeck auto-detection function"
      exports: ["parseMtgaDeck", "detectAndParseDeck"]
    - path: "client/src/data/starterDecks.ts"
      provides: "STARTER_DECKS constant array with 4-5 mono/dual-color decks"
      exports: ["STARTER_DECKS", "StarterDeck"]
    - path: "client/src/constants/storage.ts"
      provides: "Shared storage key constants for deck persistence"
      exports: ["STORAGE_KEY_PREFIX", "ACTIVE_DECK_KEY"]
  key_links:
    - from: "client/src/services/deckParser.ts"
      to: "client/src/data/starterDecks.ts"
      via: "shared DeckEntry type"
      pattern: "DeckEntry"
---

<objective>
Add MTGA text format parser, starter deck data, and shared storage key constants. These are client-side utilities that do not depend on the Rust engine changes.

Purpose: Enables deck import from MTGA format and provides first-launch starter decks. Storage constants prevent key mismatch bugs.
Output: Extended deckParser.ts, starterDecks.ts data file, storage.ts constants file.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-wire-deckbuilder-game-engine/09-CONTEXT.md
@.planning/phases/09-wire-deckbuilder-game-engine/09-RESEARCH.md

@client/src/services/deckParser.ts
@client/src/components/deck-builder/DeckBuilder.tsx

<interfaces>
<!-- Existing types from deckParser.ts -->
```typescript
export interface DeckEntry {
  count: number;
  name: string;
}

export interface ParsedDeck {
  main: DeckEntry[];
  sideboard: DeckEntry[];
}

export function parseDeckFile(content: string): ParsedDeck;
export function exportDeckFile(deck: ParsedDeck): string;
```

<!-- Storage keys currently hardcoded in DeckBuilder.tsx -->
```typescript
const STORAGE_KEY_PREFIX = "forge-deck:";  // DeckBuilder.tsx line 10
sessionStorage.setItem("forge-active-deck", ...);  // DeckBuilder.tsx line 132
```

<!-- GamePage reads a different key (BUG) -->
```typescript
sessionStorage.getItem("forge-deck");  // GamePage.tsx line 36
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MTGA text format parser and auto-detection to deckParser.ts</name>
  <files>client/src/services/deckParser.ts</files>
  <behavior>
    - parseMtgaDeck("4 Lightning Bolt (FDN) 123\n2 Counterspell (MKM) 56") returns { main: [{count:4, name:"Lightning Bolt"}, {count:2, name:"Counterspell"}], sideboard: [] }
    - parseMtgaDeck with blank line separator puts subsequent cards in sideboard
    - parseMtgaDeck ignores empty lines at start/end and comment lines starting with #
    - detectAndParseDeck auto-detects MTGA format (lines contain `(SET) NUM` pattern) vs .dck format (lines contain `[Main]`/`[Sideboard]` sections or `Nx CardName`)
    - detectAndParseDeck correctly parses both formats when auto-detected
    - parseMtgaDeck handles "Deck" and "Sideboard" header labels (some MTGA exports include these)
  </behavior>
  <action>
Add to `client/src/services/deckParser.ts`:

1. `parseMtgaDeck(content: string): ParsedDeck` function:
   - Regex for MTGA lines: `/^(\d+)\s+(.+?)\s+\([A-Z0-9]+\)\s+\d+$/`
   - Split on newlines, trim each line
   - Skip empty lines, `#` comments, and header labels like "Deck" or "Companion"
   - Blank line (or "Sideboard" header) switches to sideboard section
   - Extract count and card name (ignore set code and collector number)

2. `detectAndParseDeck(content: string): ParsedDeck` function:
   - Check if any non-empty, non-comment line matches the MTGA pattern `(XXX) NNN`
   - If yes, use `parseMtgaDeck`
   - Otherwise, use existing `parseDeckFile`

Write tests first in a `__tests__/deckParser.test.ts` file (or extend existing). Run tests, see them fail, then implement.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.ts/client && npx vitest run src/services/__tests__/deckParser.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>parseMtgaDeck and detectAndParseDeck exported from deckParser.ts. All parser tests pass including MTGA format parsing, blank-line sideboard detection, and auto-detection between formats.</done>
</task>

<task type="auto">
  <name>Task 2: Create starter decks data and storage key constants</name>
  <files>client/src/data/starterDecks.ts, client/src/constants/storage.ts</files>
  <action>
1. Create `client/src/constants/storage.ts`:
   ```typescript
   /** Prefix for saved deck data in localStorage. Full key: `${STORAGE_KEY_PREFIX}${deckName}` */
   export const STORAGE_KEY_PREFIX = "forge-deck:";
   /** Key for the currently selected/active deck name in localStorage */
   export const ACTIVE_DECK_KEY = "forge-active-deck";
   ```

2. Create `client/src/data/starterDecks.ts`:
   - Export `StarterDeck` interface: `{ name: string; colorIdentity: string[]; cards: DeckEntry[] }`
   - Import `DeckEntry` from `../services/deckParser`
   - Export `STARTER_DECKS: StarterDeck[]` with 4-5 starter decks curated from cards the engine likely supports (mono-color and simple dual-color decks using common creatures and spells):
     - "Red Deck Wins" (R): Lightning Bolt, Goblin Guide, Monastery Swiftspear, Shock, basic Mountains, etc.
     - "White Weenie" (W): Savannah Lions, Elite Vanguard, Raise the Alarm, Plains, etc.
     - "Blue Control" (U): Counterspell, Cancel, Air Elemental, Islands, etc.
     - "Green Stompy" (G): Llanowar Elves, Giant Growth, Grizzly Bears, Forests, etc.
     - "Azorius Flyers" (W, U): Flying creatures, removal, card draw, Plains + Islands
   - Each deck should have exactly 60 cards (summing counts)
   - Use simple, well-known cards that the engine's effect handlers can process (DealDamage, Pump, Draw, Counter, ChangeZone, Token)

Create `client/src/constants` directory if it does not exist.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.ts/client && node -e "const s = require('./src/data/starterDecks.ts'); console.log('ERROR: should use ESM')" 2>&1 || npx tsx -e "import { STARTER_DECKS } from './src/data/starterDecks'; console.log(STARTER_DECKS.length, 'decks'); for (const d of STARTER_DECKS) { const total = d.cards.reduce((s,c) => s+c.count, 0); console.log(d.name, d.colorIdentity, total, 'cards'); }" 2>&1 | tail -10</automated>
  </verify>
  <done>STARTER_DECKS exports 4-5 decks, each with 60 cards. Storage key constants exported from client/src/constants/storage.ts. No hardcoded storage key strings remain for deck operations.</done>
</task>

</tasks>

<verification>
- `cd client && npx vitest run src/services/__tests__/deckParser.test.ts` -- parser tests pass
- Starter decks each sum to 60 cards
- Storage constants file exists with STORAGE_KEY_PREFIX and ACTIVE_DECK_KEY
</verification>

<success_criteria>
- MTGA text format (`4 Card Name (SET) 123`) parses correctly to ParsedDeck
- Auto-detection correctly routes MTGA vs .dck formats
- 4-5 starter decks available as typed constants with valid card entries
- Storage key constants centralized in one file for import by all consumers
</success_criteria>

<output>
After completion, create `.planning/phases/09-wire-deckbuilder-game-engine/09-02-SUMMARY.md`
</output>
