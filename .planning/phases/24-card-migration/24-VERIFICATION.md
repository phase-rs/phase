---
phase: 24-card-migration
verified: 2026-03-10T23:19:14Z
status: passed
score: 11/11 must-haves verified
re_verification: false
---

# Phase 24: Card Migration Verification Report

**Phase Goal:** Migrate all Forge .txt card definitions to ability JSON format with parity validation and dual CI coverage gates
**Verified:** 2026-03-10T23:19:14Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | `cargo run --bin migrate` converts all 32,300 Forge .txt files to ability JSON in data/abilities/ | VERIFIED | 32,275 files present (32,274 cards + schema.json); migration-report.json summary: total=32300, converted=32274, errors=26, warnings=347 |
| 2  | Cost strings like '3 W', 'T', 'AddCounter<2/LOYALTY>', 'SubCounter<1/LOYALTY>', 'Sac<1/CARDNAME>' produce correct AbilityCost variants | VERIFIED | parse_cost() at ability.rs:547 handles all variants; jace_the_mind_sculptor.json shows Loyalty{amount:2}, Loyalty{amount:-1}, Loyalty{amount:-12}; lightning_bolt.json shows Spell with DealDamage effect |
| 3  | Multi-face cards produce JSON with populated faces array, not top-level abilities | VERIFIED | Sampled multi-face cards (a_young_red_dragon.json, assault.json, uninvited_geist.json) all show faces:2, top-level abilities:0 |
| 4  | Migration tool prints summary stats to stdout and writes migration-report.json | VERIFIED | migrate.rs lines 208-245 print stats and write report; migration-report.json exists with summary, errors, warnings keys |
| 5  | `cargo test --test parity` loads all 78 Standard cards via both Forge and JSON paths and compares them structurally | VERIFIED | All 4 parity tests pass: parity_all_standard_cards, parity_lightning_bolt, parity_jace_the_mind_sculptor, parity_lovestruck_beast (10.62s run) |
| 6  | CardDatabase::load_json() successfully loads all 78 Standard cards by name without errors | VERIFIED | JSON coverage gate reports 78/78 (100.0%) coverage; parity tests load all cards without errors |
| 7  | Standard card manifest lists all 78 card names, one per line | VERIFIED | data/standard-cards.txt: 80 total lines, 78 non-comment/non-blank card names |
| 8  | Parity test detects mismatches in abilities, triggers, statics, keywords, P/T, types, and mana costs | VERIFIED | compare_card_rules() in parity.rs covers all these fields; filter_allowed_divergences() only allows basic-land mana and equipment equip synthesis divergences |
| 9  | `cargo run --bin coverage-report -- --json data/ --ci` loads cards via JSON path and validates 100% Standard coverage | VERIFIED | Confirmed live: "CI PASSED: 100% Standard-legal coverage" — 78/78 cards supported |
| 10 | CI workflow runs both Forge and JSON coverage gates | VERIFIED | ci.yml lines 33-37: "Standard coverage gate" (Forge) and "Standard coverage gate (JSON)" both present |
| 11 | JSON coverage gate reads the same Standard card manifest as the Forge gate | VERIFIED | coverage_report.rs line 97 reads standard-cards.txt; Forge gate reads standard-cards/ directory with same 78 cards |

**Score:** 11/11 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/engine/src/bin/migrate.rs` | Migration tool binary | VERIFIED | 306 lines (min_lines: 100); uses parse_card_file, AbilityFile, MigrationStats, card_rules_to_ability_file |
| `crates/engine/src/parser/ability.rs` | Enhanced cost parser | VERIFIED | 1006 lines; parse_cost() at line 547; split_cost_components() at line 447; 17 new unit tests; wired via line 632 |
| `data/abilities/lightning_bolt.json` | Migrated card (overwrites hand-authored) | VERIFIED | Valid AbilityFile with Spell/DealDamage/Any-target; cost:null (no activation cost); generated by migration tool |
| `crates/engine/tests/parity.rs` | Forge vs JSON parity integration tests | VERIFIED | 696 lines (min_lines: 80); 4 tests including parity_all_standard_cards, spot-checks, positive assertions |
| `data/standard-cards.txt` | Standard card manifest (78 card names) | VERIFIED | 80 lines total, 78 card names; min_lines: 78 satisfied |
| `data/mtgjson/test_fixture.json` | MTGJSON entries for all 78 Standard cards | VERIFIED | 85 entries (78 Standard + 7 extras from prior phases) |
| `crates/engine/src/bin/coverage_report.rs` | Coverage report with --json mode | VERIFIED | 271 lines; --json flag at line 14; load_json() call at line 55; manifest filtering at line 97 |
| `.github/workflows/ci.yml` | Dual coverage gate CI step | VERIFIED | Lines 33-37 contain both Forge and JSON coverage gate steps with coverage-report binary |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `crates/engine/src/bin/migrate.rs` | `crates/engine/src/parser/card_parser.rs` | parse_card_file() call | WIRED | Line 5: `use engine::parser::parse_card_file;` + line 151: `parse_card_file(&content)` |
| `crates/engine/src/bin/migrate.rs` | `crates/engine/src/schema/mod.rs` | AbilityFile serialization | WIRED | Line 6: `use engine::schema::{AbilityFile, FaceAbilities};` + used in card_rules_to_ability_file() |
| `crates/engine/tests/parity.rs` | `crates/engine/src/database/card_db.rs` | CardDatabase::load() and CardDatabase::load_json() | WIRED | Lines 468-474: both load paths called; all 4 tests exercise both |
| `crates/engine/tests/parity.rs` | `data/standard-cards.txt` | reads manifest to get card names | WIRED | Line 24: `data_dir().join("standard-cards.txt")` |
| `crates/engine/src/bin/coverage_report.rs` | `crates/engine/src/database/card_db.rs` | CardDatabase::load_json() when --json flag | WIRED | Lines 50-73: json_mode branch calls load_json() |
| `crates/engine/src/bin/coverage_report.rs` | `data/standard-cards.txt` | reads manifest for card list | WIRED | Line 97: `path.join("standard-cards.txt")` |
| `.github/workflows/ci.yml` | `crates/engine/src/bin/coverage_report.rs` | cargo run --bin coverage-report | WIRED | Lines 34, 37: both Forge and JSON invocations |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| MIGR-01 | 24-01, 24-02 | All engine-supported cards converted from Forge .txt to MTGJSON metadata + ability JSON via automated migration | SATISFIED | 32,274 card JSON files in data/abilities/; all 78 Standard cards load via both paths in parity tests |
| MIGR-03 | 24-01 | Automated Forge-to-JSON migration tool converts all 32,300+ Forge .txt card definitions to ability JSON format | SATISFIED | migrate.rs binary runs; 32,274 out of 32,300 converted (26 rejected for missing Name: field — malformed Forge source data) |
| MIGR-05 | 24-03 | CI coverage gate updated to validate against JSON card data; all previously supported cards remain supported | SATISFIED | ci.yml has dual Forge+JSON coverage gates; JSON gate confirms 78/78 (100%) Standard coverage |
| TEST-04 | 24-02 | Per-card behavioral parity tests confirm migrated cards produce identical game outcomes as original Forge format | SATISFIED | parity.rs runs structural comparison across all 78 Standard cards; 4 tests all pass; only 2 allowlisted divergence categories (basic-land mana synthesis, equipment equip synthesis) |

No orphaned requirements — all 4 Phase 24 requirements are claimed by plans and verified in the codebase.

### Anti-Patterns Found

No anti-patterns found. Scan of migrate.rs, coverage_report.rs, parity.rs, and ability.rs returned no TODO/FIXME/PLACEHOLDER markers, no stub implementations, no empty handlers, and no return-null patterns.

One pre-existing clippy warning noted in summaries (if_same_then_else for Tap/Untap branches in ability.rs) was out of scope for this phase and not introduced by it.

### Human Verification Required

None. All goal-critical behaviors are verifiable programmatically:

- Migration tool output: verified by file count (32,275 in data/abilities/) and content inspection (lightning_bolt.json, jace_the_mind_sculptor.json)
- Parity tests: verified by running `cargo test --test parity` — all 4 pass
- Coverage gates: verified by running both `cargo run --bin coverage-report` invocations — both exit 0 with "CI PASSED"
- Full test suite: 802 tests pass (691 unit + 10 json_smoke + 4 parity + 42 rules + 55 AI)

### Gaps Summary

No gaps. All 11 observable truths are verified, all 8 required artifacts exist and are substantive and wired, all 7 key links are confirmed wired, all 4 requirements are satisfied.

---

_Verified: 2026-03-10T23:19:14Z_
_Verifier: Claude (gsd-verifier)_
