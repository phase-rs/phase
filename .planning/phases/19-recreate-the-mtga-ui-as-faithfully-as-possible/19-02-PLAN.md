---
phase: 19-recreate-the-mtga-ui-as-faithfully-as-possible
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - client/src/components/board/PermanentCard.tsx
  - client/src/components/board/BattlefieldRow.tsx
  - client/src/components/board/BattlefieldBackground.tsx
  - client/src/components/card/CardPreview.tsx
autonomous: true
requirements: [ARENA-03, ARENA-05]
must_haves:
  truths:
    - "Tapped permanents rotate 17 degrees clockwise with 0.85 opacity (not 90 degrees)"
    - "Battlefield cards show art_crop images instead of full card images"
    - "Hovering a battlefield card shows instant full-card preview on right side (no animation)"
    - "Battlefield rows have no visible borders or bars"
    - "Battlefield background is a full-bleed image with no gradient overlays"
  artifacts:
    - path: "client/src/components/board/PermanentCard.tsx"
      provides: "Art-crop battlefield cards with 17deg tap rotation"
      contains: "ArtCropCard"
    - path: "client/src/components/card/CardPreview.tsx"
      provides: "Instant card preview without animation"
  key_links:
    - from: "client/src/components/board/PermanentCard.tsx"
      to: "client/src/components/card/ArtCropCard.tsx"
      via: "renders ArtCropCard instead of CardImage"
      pattern: "ArtCropCard"
---

<objective>
Wire ArtCropCard into battlefield rendering, change tap rotation to 17 degrees, remove zone borders, and make card preview instant.

Purpose: Transform battlefield visuals from full-card display to MTGA-style art crops with subtle tap rotation. Critical visual shift that makes the game look like Arena.
Output: Updated PermanentCard, BattlefieldRow, BattlefieldBackground, CardPreview.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-recreate-the-mtga-ui-as-faithfully-as-possible/19-CONTEXT.md
@.planning/phases/19-recreate-the-mtga-ui-as-faithfully-as-possible/19-01-SUMMARY.md

<interfaces>
From client/src/components/card/ArtCropCard.tsx (created in plan 01):
```typescript
export function ArtCropCard({ objectId }: { objectId: number }): JSX.Element;
```

From client/src/components/board/PermanentCard.tsx:
```typescript
// Current: uses CardImage with size="small"
// animate={{ rotate: isAttacking || obj.tapped ? 90 : 0 }}
// Has data-object-id, layoutId, click handlers, glow classes, attachment rendering
```

From client/src/components/board/BattlefieldRow.tsx:
```typescript
export function BattlefieldRow({ groups, rowType }: { groups: GroupedPermanent[]; rowType: string }): JSX.Element;
// Currently has min-h using --card-h — needs to use --art-crop-h
```

From client/src/components/card/CardPreview.tsx:
```typescript
// Currently uses motion.div with initial/animate/exit opacity+scale (0.15s)
// User wants: instant appear, NO slide animation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ArtCropCard into PermanentCard + change tap rotation to 17deg</name>
  <files>client/src/components/board/PermanentCard.tsx, client/src/components/board/BattlefieldRow.tsx</files>
  <action>
**PermanentCard.tsx changes:**

1. Replace `CardImage` import with `ArtCropCard` import from `../card/ArtCropCard.tsx`. Remove the `CardImage` import entirely.

2. Replace the `<CardImage cardName={obj.name} size="small" hasUnimplementedMechanics={obj.has_unimplemented_mechanics} />` with `<ArtCropCard objectId={objectId} />`. Remove the wrapping `<div className="relative z-10">` since ArtCropCard handles its own layout.

3. Remove the P/T box rendering (`{ptDisplay && <PTBox ptDisplay={ptDisplay} />}`) — ArtCropCard already renders P/T.

4. Remove the loyalty shield rendering (`{obj.loyalty != null && ...}`) — ArtCropCard handles this.

5. Remove the counter badges rendering (`{counters.length > 0 && ...}`) — ArtCropCard handles this.

6. Remove the damage overlay rendering for non-creatures (`{!ptDisplay && obj.damage_marked > 0 && ...}`) — ArtCropCard should handle this (add damage overlay for non-creatures in ArtCropCard if not already present).

7. Remove the `computePTDisplay` import and `PTBox` import since they're no longer used directly.

8. Remove the `counters` variable and `COUNTER_COLORS` constant since ArtCropCard handles them.

9. Change tap rotation from 90 to 17 degrees:
```typescript
animate={{
  rotate: isAttacking || obj.tapped ? 17 : 0,
  opacity: obj.tapped && !isAttacking ? 0.85 : 1,
}}
```
Note: Attacking cards should NOT be dimmed (only tapped non-attacking cards get 0.85 opacity).

10. Remove the `tappedStyle` logic from CardImage since it no longer exists.

11. Keep ALL click handlers, glow classes, attachment rendering, data-object-id, layoutId, longPressHandlers — those are interaction logic, unchanged.

**BattlefieldRow.tsx changes:**

1. Change the `min-h` from `calc(var(--card-h)+8px)` to `calc(var(--art-crop-h)+8px)` to match art crop card height.

2. Remove the `border-b border-white/10 bg-black/20` classes from the battlefield section containers — these create visible zone lanes. The rows should have no visible borders or backgrounds. Keep the layout gap and padding.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check</automated>
  </verify>
  <done>PermanentCard renders ArtCropCard instead of CardImage, tapped cards rotate 17deg with 0.85 opacity, P/T/loyalty/counters delegated to ArtCropCard, no visible zone borders on battlefield rows.</done>
</task>

<task type="auto">
  <name>Task 2: Make CardPreview instant + remove battlefield background overlay</name>
  <files>client/src/components/card/CardPreview.tsx, client/src/components/board/BattlefieldBackground.tsx</files>
  <action>
**CardPreview.tsx changes:**

Per user decision: "NO slide animation — instant appear to avoid jarring when scanning multiple cards."

1. Remove the `AnimatePresence` and `motion` imports from framer-motion.
2. Remove the `AnimatePresence` wrapper.
3. Replace the `motion.div` with a regular `div` — remove `initial`, `animate`, `exit`, `transition` props.
4. Keep the `fixed z-[60] pointer-events-none` positioning.
5. Change the image size from `"large"` to `"normal"` (normal is still readable and loads faster since hand cards already cache normal).
6. The component should now render/unmount instantly with no animation when cardName changes.

**BattlefieldBackground.tsx changes:**

Per user decision: "NO gradients, NO vignette, NO post-processing" for battlefield backgrounds.

1. Remove the dark overlay div: `<div className="absolute inset-0 bg-black/40" />`. The background image should display without any darkening overlay.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check</automated>
  </verify>
  <done>CardPreview appears/disappears instantly with no animation transition. Battlefield background shows the full image without darkening overlay.</done>
</task>

</tasks>

<verification>
- `cd client && pnpm run type-check` passes
- PermanentCard imports ArtCropCard, not CardImage
- Tap rotation is 17 (not 90) in PermanentCard animate prop
- CardPreview has no framer-motion animation
- BattlefieldBackground has no `bg-black/40` overlay
</verification>

<success_criteria>
- Battlefield permanents display as art-crop cards with color-coded borders
- Tapped permanents rotate 17deg clockwise with 0.85 opacity
- Hovering battlefield cards shows instant (no animation) full-card preview
- No visible zone borders, bars, or background darkening
</success_criteria>

<output>
After completion, create `.planning/phases/19-recreate-the-mtga-ui-as-faithfully-as-possible/19-02-SUMMARY.md`
</output>
