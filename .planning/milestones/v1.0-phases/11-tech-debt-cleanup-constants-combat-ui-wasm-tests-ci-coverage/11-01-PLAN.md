---
phase: 11-tech-debt-cleanup-constants-combat-ui-wasm-tests-ci-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/constants/game.ts
  - client/src/constants/storage.ts
  - client/src/constants/ui.ts
  - client/src/stores/gameStore.ts
  - client/src/hooks/useKeyboardShortcuts.ts
  - client/src/game/controllers/aiController.ts
  - client/src/pages/GamePage.tsx
  - client/src/components/modal/CardDataMissingModal.tsx
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TD-01, TD-04]

must_haves:
  truths:
    - "No duplicate constant definitions exist in the client codebase"
    - "All game logic constants import from constants/game.ts"
    - "All storage key constants import from constants/storage.ts"
    - "When card-data.json is missing, user sees a blocking modal explaining how to generate it"
    - "User can dismiss the modal with Continue anyway to proceed without card data"
  artifacts:
    - path: "client/src/constants/game.ts"
      provides: "Canonical game logic constants"
      contains: "UNDOABLE_ACTIONS.*MAX_UNDO_HISTORY.*AI_PLAYER_ID.*AI_BASE_DELAY_MS"
    - path: "client/src/constants/ui.ts"
      provides: "UI constants (combat tilt, glow colors, animation timing)"
    - path: "client/src/components/modal/CardDataMissingModal.tsx"
      provides: "Blocking modal for missing card-data.json"
      exports: ["CardDataMissingModal"]
  key_links:
    - from: "client/src/stores/gameStore.ts"
      to: "client/src/constants/game.ts"
      via: "import { UNDOABLE_ACTIONS, MAX_UNDO_HISTORY }"
      pattern: "import.*from.*constants/game"
    - from: "client/src/hooks/useKeyboardShortcuts.ts"
      to: "client/src/constants/game.ts"
      via: "import { UNDOABLE_ACTIONS, MAX_UNDO_HISTORY }"
      pattern: "import.*from.*constants/game"
    - from: "client/src/pages/GamePage.tsx"
      to: "client/src/components/modal/CardDataMissingModal.tsx"
      via: "Renders CardDataMissingModal when card-data.json fetch fails"
      pattern: "CardDataMissingModal"
---

<objective>
Consolidate duplicated constants across the TypeScript client, add missing card-data.json modal, and fix stale documentation.

Purpose: Remove three duplicate definitions of UNDOABLE_ACTIONS/MAX_UNDO_HISTORY, extract magic numbers from aiController, and prevent silent empty-library game starts.
Output: Clean constant imports, CardDataMissingModal component, updated ROADMAP.md and REQUIREMENTS.md.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tech-debt-cleanup-constants-combat-ui-wasm-tests-ci-coverage/11-CONTEXT.md

<interfaces>
<!-- Existing constants files that will be expanded -->

From client/src/constants/game.ts:
```typescript
export const UNDOABLE_ACTIONS = new Set(["PassPriority", "DeclareAttackers", "DeclareBlockers", "ActivateAbility"]);
export const MAX_UNDO_HISTORY = 5;
```

From client/src/constants/storage.ts:
```typescript
export const STORAGE_KEY_PREFIX = "forge-deck:";
export const ACTIVE_DECK_KEY = "forge-active-deck";
```

<!-- Duplicate definitions to remove -->

From client/src/stores/gameStore.ts (lines 6-14):
```typescript
const UNDOABLE_ACTIONS = new Set(["PassPriority", "DeclareAttackers", "DeclareBlockers", "ActivateAbility"]);
const MAX_UNDO_HISTORY = 5;
```

From client/src/hooks/useKeyboardShortcuts.ts (lines 10-18):
```typescript
const UNDOABLE_ACTIONS = new Set(["PassPriority", "DeclareAttackers", "DeclareBlockers", "ActivateAbility"]);
const MAX_UNDO_HISTORY = 5;
```

<!-- Magic numbers to extract from aiController.ts -->
```typescript
const AI_BASE_DELAY = 800;
const AI_DELAY_VARIANCE = 400;
const aiPlayer = 1; // used inline at line 41
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate constants and extract magic numbers</name>
  <files>client/src/constants/game.ts, client/src/constants/ui.ts, client/src/stores/gameStore.ts, client/src/hooks/useKeyboardShortcuts.ts, client/src/game/controllers/aiController.ts</files>
  <action>
1. Expand `client/src/constants/game.ts` with additional constants:
   - `AI_PLAYER_ID = 1` (from aiController.ts inline `aiPlayer = 1`)
   - `AI_BASE_DELAY_MS = 800` (from aiController.ts)
   - `AI_DELAY_VARIANCE_MS = 400` (from aiController.ts)
   Keep UNDOABLE_ACTIONS and MAX_UNDO_HISTORY already there.

2. Create `client/src/constants/ui.ts` with UI-specific constants:
   - `COMBAT_TILT_DEGREES = 15` (for combat overlay, plan 02 will consume)
   - `DEFAULT_ANIMATION_DURATION_MS = 200`
   - Any other UI magic numbers found in components

3. In `client/src/stores/gameStore.ts`: Delete the local `UNDOABLE_ACTIONS` and `MAX_UNDO_HISTORY` definitions (lines 6-14). Add `import { UNDOABLE_ACTIONS, MAX_UNDO_HISTORY } from "../constants/game";` at top.

4. In `client/src/hooks/useKeyboardShortcuts.ts`: Delete the local `UNDOABLE_ACTIONS` and `MAX_UNDO_HISTORY` definitions (lines 10-18) and the re-export at line 109. Add `import { UNDOABLE_ACTIONS, MAX_UNDO_HISTORY } from "../constants/game";` at top.

5. In `client/src/game/controllers/aiController.ts`: Replace local `AI_BASE_DELAY = 800`, `AI_DELAY_VARIANCE = 400`, and inline `aiPlayer = 1` with imports from `constants/game.ts`: `import { AI_PLAYER_ID, AI_BASE_DELAY_MS, AI_DELAY_VARIANCE_MS } from "../../constants/game";`. Update usages accordingly.

Constants files should only depend on primitive types — no imports from stores or adapters.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check && pnpm test -- --run</automated>
  </verify>
  <done>Zero duplicate constant definitions. gameStore.ts, useKeyboardShortcuts.ts, and aiController.ts all import from constants/game.ts. All tests pass. No circular imports.</done>
</task>

<task type="auto">
  <name>Task 2: CardDataMissingModal and documentation fixes</name>
  <files>client/src/components/modal/CardDataMissingModal.tsx, client/src/pages/GamePage.tsx, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
1. Create `client/src/components/modal/CardDataMissingModal.tsx`:
   - Blocking modal (fixed inset-0, z-50) with dark backdrop (bg-black/70)
   - Title: "Card Data Missing"
   - Body: Explains card-data.json was not found and how to generate it
   - Code block: `cargo run --bin card_data_export`
   - "Continue anyway" text link (small, gray, underlined) that calls `onContinue` prop
   - Props: `{ onContinue: () => void }`
   - Follow the existing modal styling patterns from the codebase (rounded-xl, bg-gray-900, ring-1 ring-gray-700)

2. In `client/src/pages/GamePage.tsx`:
   - Modify `buildDeckPayload` to distinguish between "card-data.json missing" (404/fetch failure) and "cards loaded successfully"
   - Add state `showCardDataMissing` (boolean) to GamePage component
   - When `buildDeckPayload` returns null, set `showCardDataMissing = true` and render `CardDataMissingModal`
   - The `onContinue` callback sets `showCardDataMissing = false` and proceeds with the empty game initialization
   - Import CardDataMissingModal at the top of the file

3. Fix ROADMAP.md: No changes needed — Phase 8 already shows "Complete" status in the progress table.

4. Fix REQUIREMENTS.md: PARSE-04 and ABIL-01 already show `[x]` (complete) in the requirements list and "Complete" in the traceability table. Verify no fixes needed — if already correct, skip.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check && pnpm test -- --run</automated>
  </verify>
  <done>CardDataMissingModal renders when card-data.json is unavailable. Modal shows generation command and "Continue anyway" escape hatch. Documentation is accurate.</done>
</task>

</tasks>

<verification>
- `grep -rn "const UNDOABLE_ACTIONS" client/src/` returns exactly 1 result (in constants/game.ts)
- `grep -rn "const MAX_UNDO_HISTORY" client/src/` returns exactly 1 result (in constants/game.ts)
- `grep -rn "= 800" client/src/game/controllers/aiController.ts` returns 0 results (extracted to constants)
- `pnpm run type-check` passes with no errors
- `pnpm test -- --run` passes all existing tests
</verification>

<success_criteria>
- Zero duplicated constant definitions across the client codebase
- AI controller uses named imports from constants/game.ts
- CardDataMissingModal blocks game start when card-data.json is missing
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-tech-debt-cleanup-constants-combat-ui-wasm-tests-ci-coverage/11-01-SUMMARY.md`
</output>
