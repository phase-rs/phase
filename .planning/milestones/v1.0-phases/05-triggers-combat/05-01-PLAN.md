---
phase: 05-triggers-combat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/engine/src/types/keywords.rs
  - crates/engine/src/types/triggers.rs
  - crates/engine/src/types/mod.rs
  - crates/engine/src/game/game_object.rs
  - crates/engine/src/game/keywords.rs
  - crates/engine/src/game/mod.rs
  - crates/engine/src/game/engine.rs
  - crates/engine/src/game/casting.rs
  - crates/engine/src/game/sba.rs
  - crates/engine/src/game/targeting.rs
autonomous: true
requirements: [KWRD-01, KWRD-02]

must_haves:
  truths:
    - "Keyword::from_str parses all 50+ keyword strings into typed enum variants"
    - "Parameterized keywords (Kicker, Cycling, Flashback, Ward, Protection, Equip, etc.) parse with colon-delimited params"
    - "Unrecognized keywords become Keyword::Unknown(String) for forward compatibility"
    - "TriggerMode::from_str parses all 137 trigger mode strings into typed enum variants"
    - "GameObject.keywords is Vec<Keyword> (not Vec<String>) throughout the codebase"
    - "has_keyword() helper on GameObject uses discriminant-based matching"
  artifacts:
    - path: "crates/engine/src/types/keywords.rs"
      provides: "Keyword enum with 50+ variants, ProtectionTarget, FromStr impl"
      contains: "pub enum Keyword"
    - path: "crates/engine/src/types/triggers.rs"
      provides: "TriggerMode enum with 137 variants, FromStr impl"
      contains: "pub enum TriggerMode"
    - path: "crates/engine/src/game/keywords.rs"
      provides: "has_keyword helper, keyword parsing from card data, keyword-related game logic"
      contains: "pub fn has_keyword"
  key_links:
    - from: "crates/engine/src/game/game_object.rs"
      to: "crates/engine/src/types/keywords.rs"
      via: "keywords field type"
      pattern: "keywords: Vec<Keyword>"
    - from: "crates/engine/src/types/mod.rs"
      to: "crates/engine/src/types/keywords.rs"
      via: "pub mod keywords"
      pattern: "pub use keywords::"
---

<objective>
Define the Keyword enum (50+ variants with parameterized associated data), TriggerMode enum (137 variants), and migrate GameObject.keywords from Vec<String> to Vec<Keyword>. This establishes the type foundations that both the trigger system and combat system depend on.

Purpose: Type-safe keyword and trigger mode dispatch replaces stringly-typed matching, enabling compile-time safety for combat validation and trigger matching in subsequent plans.
Output: Two new type files (types/keywords.rs, types/triggers.rs), a game/keywords.rs helper module, and migrated GameObject with Vec<Keyword>.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-triggers-combat/05-CONTEXT.md
@.planning/phases/05-triggers-combat/05-RESEARCH.md

<interfaces>
<!-- Existing types the executor needs -->

From crates/engine/src/game/game_object.rs:
```rust
pub struct GameObject {
    // ...
    pub keywords: Vec<String>,  // MIGRATE to Vec<Keyword>
    pub abilities: Vec<String>,
    pub svars: HashMap<String, String>,
    // ...
    pub entered_battlefield_turn: Option<u32>,
}
```

From crates/engine/src/types/ability.rs:
```rust
pub struct TriggerDefinition {
    pub mode: String,
    pub params: HashMap<String, String>,
}
```

From crates/engine/src/types/mod.rs:
```rust
// Add: pub mod keywords; pub mod triggers;
// Add re-exports for Keyword, TriggerMode
```

From crates/engine/src/game/mod.rs:
```rust
// Add: pub mod keywords;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keyword enum, TriggerMode enum, and type definitions</name>
  <files>crates/engine/src/types/keywords.rs, crates/engine/src/types/triggers.rs, crates/engine/src/types/mod.rs</files>
  <action>
Create `crates/engine/src/types/keywords.rs`:
- Define `ProtectionTarget` enum with variants: `Color(ManaColor)`, `CardType(String)`, `Quality(String)` (for "from everything", "from players", etc.)
- Define `Keyword` enum with ALL 50+ variants. Simple (unit) variants for combat and common keywords: Flying, FirstStrike, DoubleStrike, Trample, Deathtouch, Lifelink, Vigilance, Haste, Reach, Defender, Menace, Indestructible, Hexproof, Shroud, Flash, Fear, Intimidate, Skulk, Shadow, Horsemanship, Wither, Infect, Afflict, Prowess, Undying, Persist, Cascade, Convoke, Delve, Devoid, Exalted, Flanking, Changeling, Phasing, Battlecry, Decayed, Unleash, Riot, Afterlife, Annihilator, Bushido, Dredge, Evolve, Extort, Modular, Ninjutsu, Renown, Soulbond, Tribute, Unearth, Exploit, Explore, Ascend, Reconfigure, Living Weapon, Totem Armor, Bestow, Embalm, Eternalize, Fabricate
- Parameterized variants with associated data: `Protection(ProtectionTarget)`, `Kicker(String)`, `Cycling(String)`, `Flashback(String)`, `Ward(String)`, `Equip(String)`, `Landwalk(String)`, `Rampage(u32)`, `Bushido(u32)` (if parameterized in Forge), `Absorb(u32)`, `Fading(u32)`, `Vanishing(u32)`, `Crew(u32)`, `Partner(Option<String>)` (with or without name), `Companion(String)`
- `Unknown(String)` as final fallback variant
- Derive: Debug, Clone, PartialEq, Eq, Serialize, Deserialize on both enums
- Implement `FromStr` on `Keyword`: split on first colon for parameterized. Match case-insensitively on the keyword name portion. "First Strike" and "Double Strike" handle the space. Unknown keywords -> `Keyword::Unknown(s.to_string())`. Return `Result<Self, Infallible>` (never fails).
- Import ManaColor from super::mana

Create `crates/engine/src/types/triggers.rs`:
- Define `TriggerMode` enum with ALL 137 Forge trigger mode variants. Reference Forge's TriggerType.java enum. Key variants include: ChangesZone, DamageDone, SpellCast, AbilityCast, SpellAbilityCast, Attacks, AttackerBlocked, AttackerBlockedByCreature, AttackerUnblocked, Blocks, BlockedBy, DamageDoneOnce, ChangesZoneAll, Countered, CounterAdded, CounterAddedOnce, CounterRemoved, CounterRemovedAll, Sacrificed, Destroyed, Taps, TapsForMana, Untaps, BecomesTarget, BecomesTargetOnce, Drawn, LifeGained, LifeLost, Discarded, Revealed, Surveilled, Explored, Scried, Cycled, PayLife, PayCumulativeUpkeep, TurnFaceUp, Transformed, Regenerated, Voted, RolledDie, Clash, FlippedCoin, Phase, PhaseIn, PhaseOut, NewTurn, EndStep, BeginCombat, EndCombat, BecomeMonarch, LosesGame, Championed, Exerted, Crewed, TokenCreated, Proliferated, SpellCastFromNonHand, AbilityTriggered, ManaPoolChanged, PowerToughnessChanged, ControllerChanged, LandPlayed, Evolved, Soulbond, EquippedUnequipped, AuraAttached, AuraUnattached, Copied, Foretold, PlaneswalkTo, PlaneswalkAway, Adapt, Amass, Learn, Venture, DungeonCompleted, RollPlanarDie, ClassLevelGained, DayNightChange, Disturb, Meld, Coven, Training, Modified, Connive, Blitz, Casualty, Compleated, Prototype, Enlist, ReadAhead, Ravenous, Offspring, Gift, Impending, Manifest, Ascend, and all remaining Forge TriggerType variants up to 137
- `Unknown(String)` as final fallback
- Derive: Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize
- Implement `FromStr` on `TriggerMode`: case-sensitive match on Forge mode strings. Unknown -> `TriggerMode::Unknown(s)`. Return `Result<Self, Infallible>`.

Update `crates/engine/src/types/mod.rs`:
- Add `pub mod keywords;` and `pub mod triggers;`
- Add re-exports: `pub use keywords::{Keyword, ProtectionTarget};` and `pub use triggers::TriggerMode;`

Reference Forge's source at `../forge` for the complete list of TriggerType and Keyword enum variants. Ensure variant names match Forge's naming (CamelCase). The count need not be exact -- include all meaningful variants and use Unknown for edge cases.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test --lib -p engine types::keywords::tests types::triggers::tests -x 2>&1 | tail -20</automated>
  </verify>
  <done>Keyword enum has 50+ variants with FromStr parsing all standard MTG keywords. TriggerMode enum has 137 variants with FromStr. Both have Unknown fallbacks. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: GameObject keyword migration and helpers</name>
  <files>crates/engine/src/game/keywords.rs, crates/engine/src/game/game_object.rs, crates/engine/src/game/mod.rs, crates/engine/src/game/engine.rs, crates/engine/src/game/casting.rs, crates/engine/src/game/sba.rs, crates/engine/src/game/targeting.rs</files>
  <action>
Create `crates/engine/src/game/keywords.rs`:
- `pub fn has_keyword(obj: &GameObject, keyword: &Keyword) -> bool` -- uses `std::mem::discriminant` comparison for simple keywords, direct match for parameterized
- `pub fn has_flying(obj: &GameObject) -> bool`, `pub fn has_haste(obj: &GameObject) -> bool`, etc. -- convenience functions for the most commonly checked keywords
- `pub fn parse_keywords(keyword_strings: &[String]) -> Vec<Keyword>` -- batch parse Vec<String> to Vec<Keyword> via FromStr, used when creating GameObjects from card data
- Add unit tests: parse known keywords, parse parameterized ("Kicker:1G"), parse unknown, has_keyword discriminant matching

Migrate `crates/engine/src/game/game_object.rs`:
- Change `pub keywords: Vec<String>` to `pub keywords: Vec<Keyword>` (import Keyword from crate::types::keywords)
- Update `GameObject::new()` default to `keywords: Vec::new()` (same, just type changes)
- Add `pub fn has_keyword(&self, keyword: &Keyword) -> bool` method on `GameObject` that delegates to keywords::has_keyword

Update `crates/engine/src/game/mod.rs`:
- Add `pub mod keywords;`
- Add to pub use: `pub use keywords::parse_keywords;`

Fix ALL compilation errors across the codebase caused by the Vec<String> -> Vec<Keyword> migration:
- In `crates/engine/src/game/engine.rs`: any code that pushes String keywords -> push Keyword variants instead
- In `crates/engine/src/game/casting.rs`: update keyword string comparisons to use typed Keyword
- In `crates/engine/src/game/sba.rs`: the indestructible check `keywords.iter().any(|k| k.eq_ignore_ascii_case("indestructible"))` -> `obj.has_keyword(&Keyword::Indestructible)`
- In `crates/engine/src/game/targeting.rs`: hexproof/shroud checks -> use `has_keyword(&Keyword::Hexproof)` etc.
- In any tests that construct GameObjects with `keywords: vec!["Flying".to_string()]` -> `keywords: vec![Keyword::Flying]`
- Search ALL .rs files for string-based keyword usage and convert every instance

Run `cargo test --workspace` to verify no regressions.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test --workspace 2>&1 | tail -20</automated>
  </verify>
  <done>GameObject.keywords is Vec<Keyword> throughout codebase. has_keyword() helper works with discriminant matching. All existing tests pass with typed keywords. No string-based keyword checks remain.</done>
</task>

</tasks>

<verification>
- `cargo test --workspace` passes with zero failures
- `grep -r 'keywords.*Vec<String>' crates/engine/src/` returns no matches (migration complete)
- `grep -r 'eq_ignore_ascii_case.*indestructible\|keywords.*contains.*String' crates/engine/src/` returns no matches
- Keyword::from_str("Flying") == Ok(Keyword::Flying)
- Keyword::from_str("Kicker:1G") == Ok(Keyword::Kicker("1G".to_string()))
- Keyword::from_str("NotARealKeyword") == Ok(Keyword::Unknown("NotARealKeyword".to_string()))
- TriggerMode::from_str("ChangesZone") == Ok(TriggerMode::ChangesZone)
</verification>

<success_criteria>
- Keyword enum has 50+ variants covering all standard MTG keywords
- TriggerMode enum has 137 variants covering all Forge trigger modes
- Both enums have FromStr with Unknown fallback
- GameObject.keywords uses Vec<Keyword> with no Vec<String> remnants
- All workspace tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-triggers-combat/05-01-SUMMARY.md`
</output>
