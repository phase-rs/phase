---
phase: 07-platform-bridges-ui
plan: 05
type: execute
wave: 4
depends_on: [07-03]
files_modified:
  - client/src/components/targeting/TargetingOverlay.tsx
  - client/src/components/targeting/TargetArrow.tsx
  - client/src/components/mana/ManaPaymentUI.tsx
  - client/src/components/mana/ManaBadge.tsx
  - client/src/components/modal/ChoiceModal.tsx
  - client/src/components/modal/ReplacementModal.tsx
  - client/src/pages/GamePage.tsx
autonomous: true
requirements: [UI-06, UI-07, UI-09]

must_haves:
  truths:
    - "Valid targets glow with cyan ring during targeting mode"
    - "Arrow drawn from source to selected target"
    - "Auto-target fires when exactly one legal target"
    - "Mana payment shows auto-tap result with manual override option (click lands to change)"
    - "Modal choices display card large with clickable options below"
    - "Replacement effect ordering shows competing effects as cards with choose-which-first prompt"
  artifacts:
    - path: "client/src/components/targeting/TargetingOverlay.tsx"
      provides: "Targeting mode overlay with valid target highlighting"
      exports: ["TargetingOverlay"]
    - path: "client/src/components/targeting/TargetArrow.tsx"
      provides: "SVG arrow from source to target"
      exports: ["TargetArrow"]
    - path: "client/src/components/mana/ManaPaymentUI.tsx"
      provides: "Mana payment panel with auto-tap and manual override"
      exports: ["ManaPaymentUI"]
    - path: "client/src/components/modal/ChoiceModal.tsx"
      provides: "Modal for choosing among multiple options"
      exports: ["ChoiceModal"]
    - path: "client/src/components/modal/ReplacementModal.tsx"
      provides: "Modal for ordering replacement effects"
      exports: ["ReplacementModal"]
  key_links:
    - from: "client/src/components/targeting/TargetingOverlay.tsx"
      to: "client/src/stores/gameStore.ts"
      via: "waitingFor.TargetSelection"
      pattern: "waitingFor.*TargetSelection"
    - from: "client/src/components/mana/ManaPaymentUI.tsx"
      to: "client/src/stores/gameStore.ts"
      via: "waitingFor.ManaPayment + dispatch TapLandForMana"
      pattern: "waitingFor.*ManaPayment"
    - from: "client/src/components/modal/ReplacementModal.tsx"
      to: "client/src/stores/gameStore.ts"
      via: "waitingFor.ReplacementChoice + dispatch ChooseReplacement"
      pattern: "waitingFor.*ReplacementChoice"
---

<objective>
Build interactive game prompts: targeting UI with valid-target glow rings and source-to-target arrows, mana payment panel with auto-tap and manual land clicking override, choice modal for modal effects, and replacement effect ordering modal.

Purpose: These prompts enable all player decisions during gameplay -- casting spells, paying costs, choosing modes, and ordering replacement effects.
Output: All WaitingFor-driven interactive prompts rendered as overlays/panels.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-platform-bridges-ui/07-CONTEXT.md
@.planning/phases/07-platform-bridges-ui/07-RESEARCH.md
@.planning/phases/07-platform-bridges-ui/07-01-SUMMARY.md
@.planning/phases/07-platform-bridges-ui/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Targeting overlay with glow rings, arrows, and auto-target</name>
  <files>client/src/components/targeting/TargetingOverlay.tsx, client/src/components/targeting/TargetArrow.tsx</files>
  <action>
1. Create `client/src/components/targeting/TargetArrow.tsx`:
   - Props: `{ from: { x: number, y: number }, to: { x: number, y: number } }`.
   - Renders an SVG arrow line from source to target position.
   - Fixed position SVG overlay covering the full viewport (pointer-events: none).
   - Arrow with pointed head, semi-transparent cyan color (#00e5ff), 2-3px stroke width.
   - Framer Motion: animate arrow drawing from source to target.

2. Create `client/src/components/targeting/TargetingOverlay.tsx`:
   - Activates when `gameStore.waitingFor` is `TargetSelection`.
   - Reads pending_cast from WaitingFor::TargetSelection to know the source card.
   - Drives uiStore targeting mode: sets `targetingMode: true`, populates which objects are valid targets.
   - Valid target determination: for now, highlight all objects on the battlefield + all players as potentially valid (the engine validates on submission; full client-side filtering is a refinement). The engine already auto-targets when exactly one legal target.
   - Renders:
     - Semi-transparent dark overlay (pointer-events: none except on valid targets).
     - Cyan glow ring (`box-shadow: 0 0 12px 3px rgba(0,229,255,0.8)`) on valid target PermanentCards (driven via uiStore state that PermanentCard reads).
     - Instruction text: "Choose a target" at top center.
     - Cancel button.
   - When a target is clicked (via PermanentCard click handler checking targeting mode):
     - Dispatches `SelectTargets` action via gameStore.dispatch with the selected target.
   - Renders TargetArrow from source card position to each selected target.
   - If the engine auto-targeted (waitingFor transitions away immediately), this overlay never shows.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm exec tsc --noEmit && pnpm test -- --run 2>&1 | tail -5 && echo "OK"</automated>
  </verify>
  <done>TargetingOverlay shows cyan glow on valid targets, TargetArrow draws SVG arrows from source to target. Auto-target handled by engine (overlay skipped).</done>
</task>

<task type="auto">
  <name>Task 2: Mana payment UI, choice modal, replacement modal, and GamePage wiring</name>
  <files>client/src/components/mana/ManaPaymentUI.tsx, client/src/components/mana/ManaBadge.tsx, client/src/components/modal/ChoiceModal.tsx, client/src/components/modal/ReplacementModal.tsx, client/src/pages/GamePage.tsx</files>
  <action>
1. Create `client/src/components/mana/ManaBadge.tsx`:
   - Props: `{ color: ManaColor | 'colorless', amount: number }`.
   - Renders a small circular badge with mana symbol color (W=gold, U=blue, B=dark, R=red, G=green, C=gray) and amount number.

2. Create `client/src/components/mana/ManaPaymentUI.tsx`:
   - Activates when `gameStore.waitingFor` is `ManaPayment`.
   - Shows the cost being paid (from the pending spell/ability context).
   - Shows current mana pool via ManaBadges.
   - Auto-tap behavior: the engine's greedy algorithm handles mana payment. The UI shows which lands would be tapped (engine's auto-tap picks them).
   - Manual override: player can click untapped lands on the battlefield to tap them manually via `TapLandForMana` action. Highlight untapped lands with white glow.
   - "Confirm" button: submits the mana payment.
   - "Auto Pay" button: lets engine handle it automatically.
   - Renders as a panel/overlay near the bottom of the board.

3. Create `client/src/components/modal/ChoiceModal.tsx`:
   - Generic modal for player choices.
   - Props: `{ title: string, options: { id: string, label: string, description?: string }[], onChoose: (id: string) => void }`.
   - Renders: dark overlay, centered card showing the source card image large (if applicable), options as clickable buttons below.
   - Arena-style: clean, centered modal overlay. Framer Motion fade in/scale up animation.
   - Used for: mulligan decision (Keep / Mulligan buttons), card selection (SelectCards).

4. Create `client/src/components/modal/ReplacementModal.tsx`:
   - Activates when `gameStore.waitingFor` is `ReplacementChoice`.
   - Shows competing replacement effects as cards with "Choose which replacement applies first" prompt.
   - Each candidate rendered with source card image and effect description.
   - On click: dispatches `ChooseReplacement { index }` via gameStore.dispatch.
   - Framer Motion AnimatePresence for modal appear/disappear.

5. Update `client/src/pages/GamePage.tsx`:
   - Add WaitingFor-driven prompt rendering:
     - Switch on `gameStore.waitingFor.type`:
       - "TargetSelection" -> render TargetingOverlay
       - "ManaPayment" -> render ManaPaymentUI
       - "ReplacementChoice" -> render ReplacementModal
       - "MulliganDecision" -> render ChoiceModal with Keep/Mulligan options
       - "MulliganBottomCards" -> render card selection modal
       - "GameOver" -> render game over screen with winner
   - All prompts render as overlays on top of the game board.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm exec tsc --noEmit && pnpm run build && pnpm test -- --run 2>&1 | tail -5 && echo "Build OK"</automated>
  </verify>
  <done>ManaPaymentUI shows auto-tap with manual override. ChoiceModal renders clean Arena-style modal for decisions. ReplacementModal shows competing effects for ordering. GamePage wires all WaitingFor variants to appropriate prompt components.</done>
</task>

</tasks>

<verification>
- `cd client && pnpm exec tsc --noEmit` passes
- `cd client && pnpm run build` succeeds
- `cd client && pnpm test -- --run` passes
- Targeting shows glow rings and arrows
- Mana payment shows auto-tap with manual override
- Choice modal renders centered with card image and options
- Replacement modal shows candidates with ordering prompt
- All WaitingFor variants handled in GamePage
</verification>

<success_criteria>
- Targeting overlay highlights valid targets with cyan glow, draws SVG arrows
- Mana payment shows auto-tap result, manual land clicking override works
- Choice modal centered with clean Arena-style overlay
- Replacement effect ordering shows competing effects as cards
- All 9 WaitingFor variants have corresponding UI prompts
- Auto-target when exactly one legal target (handled by engine, overlay skips)
</success_criteria>

<output>
After completion, create `.planning/phases/07-platform-bridges-ui/07-05-SUMMARY.md`
</output>
