---
phase: 07-platform-bridges-ui
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - client/src/stores/__tests__/gameStore.test.ts
  - client/src/stores/__tests__/uiStore.test.ts
  - client/src/services/__tests__/imageCache.test.ts
  - client/src/services/__tests__/deckParser.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Test stubs exist and can be discovered by vitest"
    - "Running pnpm test completes with pending/skipped test stubs (no failures)"
  artifacts:
    - path: "client/src/stores/__tests__/gameStore.test.ts"
      provides: "Test stubs for game store state management"
    - path: "client/src/stores/__tests__/uiStore.test.ts"
      provides: "Test stubs for UI store selection/targeting/hover state"
    - path: "client/src/services/__tests__/imageCache.test.ts"
      provides: "Test stubs for IndexedDB image caching"
    - path: "client/src/services/__tests__/deckParser.test.ts"
      provides: "Test stubs for .dck/.dec deck file parsing"
  key_links: []
---

<objective>
Create test stub files for all Phase 7 testable modules. These stubs define the test contracts that subsequent plans must satisfy — each stub contains `it.todo()` entries describing expected behaviors.

Purpose: Nyquist compliance — every plan's `<verify>` must be able to run `pnpm test` against real test files.
Output: Four test stub files with todo entries covering stores, services, and parsing.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-platform-bridges-ui/07-CONTEXT.md
@.planning/phases/07-platform-bridges-ui/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stub files for stores and services</name>
  <files>client/src/stores/__tests__/gameStore.test.ts, client/src/stores/__tests__/uiStore.test.ts, client/src/services/__tests__/imageCache.test.ts, client/src/services/__tests__/deckParser.test.ts</files>
  <action>
1. Create `client/src/stores/__tests__/gameStore.test.ts`:
   ```typescript
   import { describe, it } from 'vitest';

   describe('gameStore', () => {
     it.todo('initializes with null gameState');
     it.todo('initGame sets adapter and creates initial game state');
     it.todo('dispatch calls adapter.submitAction and updates state');
     it.todo('dispatch pushes to stateHistory for unrevealed-info actions');
     it.todo('undo restores previous state from stateHistory');
     it.todo('undo is unavailable when stateHistory is empty');
     it.todo('reset clears all state');
   });
   ```

2. Create `client/src/stores/__tests__/uiStore.test.ts`:
   ```typescript
   import { describe, it } from 'vitest';

   describe('uiStore', () => {
     it.todo('selectObject sets selectedObjectId');
     it.todo('hoverObject sets hoveredObjectId');
     it.todo('inspectObject sets inspectedObjectId');
     it.todo('startTargeting enters targeting mode');
     it.todo('addTarget appends to selectedTargets');
     it.todo('clearTargets resets targeting state');
     it.todo('toggleFullControl flips fullControl boolean');
   });
   ```

3. Create `client/src/services/__tests__/imageCache.test.ts`:
   ```typescript
   import { describe, it } from 'vitest';

   describe('imageCache', () => {
     it.todo('getCachedImage returns null on cache miss');
     it.todo('cacheImage stores blob and getCachedImage retrieves it');
     it.todo('getCachedImage returns object URL from cached blob');
     it.todo('revokeImageUrl calls URL.revokeObjectURL');
   });
   ```

4. Create `client/src/services/__tests__/deckParser.test.ts`:
   ```typescript
   import { describe, it } from 'vitest';

   describe('deckParser', () => {
     it.todo('parses simple deck: "4 Lightning Bolt" -> { count: 4, name: "Lightning Bolt" }');
     it.todo('parses "4x Lightning Bolt" format');
     it.todo('parses [Main] and [Sideboard] sections');
     it.todo('skips comment lines starting with #');
     it.todo('skips empty lines');
     it.todo('exportDeckFile produces valid .dck format');
   });
   ```
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>Four test stub files exist with it.todo() entries. `pnpm test -- --run` discovers them and reports pending tests without failures.</done>
</task>

</tasks>

<verification>
- `cd client && pnpm test -- --run` completes without errors
- All four test files are discoverable by vitest
- Test stubs show as "todo" or "skipped" in output
</verification>

<success_criteria>
- Test stubs exist at all four paths
- vitest discovers and reports all stubs
- No test failures (all are todo/skipped)
</success_criteria>

<output>
After completion, create `.planning/phases/07-platform-bridges-ui/07-00-SUMMARY.md`
</output>
