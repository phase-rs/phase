---
phase: 18-select-candidates-to-support-and-implement-stubbed-mechanics
plan: 05
type: execute
wave: 2
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - crates/engine/src/game/coverage.rs
  - crates/engine/src/game/game_object.rs
  - crates/engine/src/game/mod.rs
  - crates/engine-wasm/src/lib.rs
  - client/src/adapter/types.ts
  - client/src/components/card/CardImage.tsx
autonomous: true
requirements: [MECH-09, MECH-10]

must_haves:
  truths:
    - "Coverage report lists percentage of Standard cards with full mechanic support"
    - "Cards with unimplemented mechanics show a visual warning indicator in the UI"
    - "has_unimplemented_mechanics check is engine-side (no keyword list duplication)"
  artifacts:
    - path: "crates/engine/src/game/coverage.rs"
      provides: "Mechanic coverage analysis functions"
      exports: ["coverage_report", "has_unimplemented_mechanics"]
    - path: "client/src/components/card/CardImage.tsx"
      provides: "Visual warning indicator for unsupported mechanics"
      contains: "unimplemented"
  key_links:
    - from: "crates/engine/src/game/game_object.rs"
      to: "crates/engine/src/game/coverage.rs"
      via: "has_unimplemented_mechanics method delegates to coverage module"
      pattern: "has_unimplemented_mechanics"
    - from: "crates/engine-wasm/src/lib.rs"
      to: "crates/engine/src/game/game_object.rs"
      via: "Serializes has_unimplemented_mechanics flag to JS"
      pattern: "has_unimplemented"
---

<objective>
Create mechanic coverage report and card-level UI warning for unimplemented mechanics.

Purpose: The coverage report quantifies progress (user requested metric of Standard cards with support). The UI warning lets players identify cards that may not behave correctly during gameplay.
Output: Coverage analysis module, engine-side unimplemented flag, frontend warning badge
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-select-candidates-to-support-and-implement-stubbed-mechanics/18-CONTEXT.md
@.planning/phases/18-select-candidates-to-support-and-implement-stubbed-mechanics/18-RESEARCH.md
@.planning/phases/18-select-candidates-to-support-and-implement-stubbed-mechanics/18-01-SUMMARY.md
@.planning/phases/18-select-candidates-to-support-and-implement-stubbed-mechanics/18-02-SUMMARY.md
@.planning/phases/18-select-candidates-to-support-and-implement-stubbed-mechanics/18-03-SUMMARY.md

<interfaces>
<!-- From game_object.rs -->
```rust
pub struct GameObject {
    pub id: ObjectId,
    pub card_id: CardId,
    pub owner: PlayerId,
    pub controller: PlayerId,
    pub name: String,
    pub zone: Zone,
    pub card_types: CardTypes,
    pub keywords: Vec<Keyword>,
    pub power: Option<i32>,
    pub toughness: Option<i32>,
    pub abilities: Vec<AbilityDefinition>,
    pub static_definitions: Vec<StaticDefinition>,
    pub trigger_definitions: Vec<TriggerDefinition>,
    // ...
}
```

<!-- From client/src/adapter/types.ts -->
```typescript
export interface GameObject {
  id: number;
  name: string;
  // ... add: hasUnimplementedMechanics?: boolean;
}
```

<!-- Effect registry keys (from build_registry) - these are "implemented" -->
<!-- Keyword game logic locations - these are "implemented keywords" -->
<!-- Static ability stubs array - these are "unimplemented statics" -->
<!-- Trigger unimplemented_modes array - these are "unimplemented triggers" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create coverage report and has_unimplemented_mechanics check</name>
  <files>crates/engine/src/game/coverage.rs, crates/engine/src/game/game_object.rs, crates/engine/src/game/mod.rs</files>
  <action>
1. Update `crates/engine/src/game/coverage.rs` (already exists but may be empty/minimal):
   - Create a `IMPLEMENTED_KEYWORDS` const set listing all keywords that have actual game logic (not just parsed): Flying, Reach, Menace, Vigilance, Defender, Haste, Shadow, FirstStrike, DoubleStrike, Trample, Deathtouch, Lifelink, Indestructible, Flash, Hexproof, Shroud, Fear, Intimidate, Skulk, Horsemanship, Wither, Infect, Prowess
   - Create a `IMPLEMENTED_EFFECTS` const set listing all registered effect api_types from build_registry (the 21+ handlers)
   - `pub fn has_unimplemented_mechanics(obj: &GameObject) -> bool`:
     - Check if any keyword in obj.keywords is NOT in IMPLEMENTED_KEYWORDS
     - Check if any ability's api_type is NOT in IMPLEMENTED_EFFECTS
     - Check if any trigger mode is in the known unimplemented set
     - Return true if ANY unimplemented mechanic found
   - `pub fn coverage_report(db: &CardDatabase) -> CoverageReport`:
     - Iterate all cards in database
     - For each card, check front face keywords, ability api_types, trigger modes
     - Classify as: fully_supported, partially_supported, unsupported
     - Return counts and percentages
   - Add a `#[test] #[ignore]` integration test that loads Forge DB and prints coverage

2. In `game_object.rs`, add:
   ```rust
   pub fn has_unimplemented_mechanics(&self) -> bool {
       coverage::has_unimplemented_mechanics(self)
   }
   ```

3. Ensure `pub mod coverage;` is in `game/mod.rs` (it may already exist).
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine -- coverage --no-fail-fast 2>&1 | tail -20</automated>
  </verify>
  <done>Coverage module reports mechanic support levels, has_unimplemented_mechanics works on GameObjects</done>
</task>

<task type="auto">
  <name>Task 2: Wire unimplemented flag through WASM and add UI warning badge</name>
  <files>crates/engine-wasm/src/lib.rs, client/src/adapter/types.ts, client/src/components/card/CardImage.tsx</files>
  <action>
1. In `crates/engine-wasm/src/lib.rs` (or wherever GameObjects are serialized to JS):
   - When building the serialized game state, include `has_unimplemented_mechanics` as a boolean field on each object
   - This may require adding the field to whatever intermediate struct is serialized via serde-wasm-bindgen
   - Check the existing serialization path: if GameState is serialized directly, the method on GameObject should be called during serialization. If there's an intermediate DTO, add the field there.

2. In `client/src/adapter/types.ts`:
   - Add `hasUnimplementedMechanics?: boolean` to the `GameObject` interface

3. In `client/src/components/card/CardImage.tsx` (or the appropriate card display component):
   - If the card's `hasUnimplementedMechanics` is true, render a small warning icon/badge (e.g., a yellow triangle with "!" or a subtle amber border)
   - Use Tailwind classes for styling
   - Add a tooltip or title attribute: "This card has mechanics not yet fully implemented"
   - Only show on battlefield cards and hand cards (not in deck builder)

4. Keep it minimal: one small badge, no complex UI. The indicator just lets the player know.
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs && cargo test -p engine -- coverage && cd client && pnpm run type-check 2>&1 | tail -20</automated>
  </verify>
  <done>Cards with unimplemented mechanics show a warning badge in the game UI, coverage report runs as ignored test</done>
</task>

</tasks>

<verification>
- `cargo test -p engine -- coverage` passes
- `cargo test -p engine -- --ignored coverage_report` runs when Forge DB available
- `cd client && pnpm run type-check` passes
- `cd client && pnpm lint` passes
</verification>

<success_criteria>
- Coverage report quantifies Standard card support percentage
- has_unimplemented_mechanics correctly identifies cards with unsupported mechanics
- Warning badge visible on cards with unimplemented mechanics in game UI
- Engine-side approach means no keyword list duplication in TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/18-select-candidates-to-support-and-implement-stubbed-mechanics/18-05-SUMMARY.md`
</output>
