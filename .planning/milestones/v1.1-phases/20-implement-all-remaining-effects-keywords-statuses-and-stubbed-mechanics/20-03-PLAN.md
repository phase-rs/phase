---
phase: 20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/engine/src/types/game_state.rs
  - crates/engine/src/game/engine.rs
  - crates/engine/src/game/effects/scry.rs
  - crates/engine/src/game/effects/dig.rs
  - crates/engine/src/game/effects/surveil.rs
  - crates/engine/src/game/effects/mod.rs
  - crates/engine-wasm/src/lib.rs
  - client/src/adapter/types.ts
  - client/src/components/modal/CardChoiceModal.tsx
  - crates/forge-ai/src/lib.rs
autonomous: true
requirements: [ENG-05, ENG-06, ENG-07]

must_haves:
  truths:
    - "Scry emits WaitingFor::ScryChoice and player chooses top/bottom for each card"
    - "Dig emits WaitingFor::DigChoice and player selects cards to keep"
    - "Surveil emits WaitingFor::SurveilChoice and player sends cards to graveyard or keeps on top"
    - "AI makes proper decisions for Scry/Dig/Surveil choices using evaluation"
    - "Frontend shows a card selection modal for all three choice types"
  artifacts:
    - path: "crates/engine/src/game/effects/surveil.rs"
      provides: "Surveil effect handler"
      exports: ["resolve"]
    - path: "client/src/components/modal/CardChoiceModal.tsx"
      provides: "Generic card choice UI for Scry/Dig/Surveil"
  key_links:
    - from: "crates/engine/src/game/effects/scry.rs"
      to: "crates/engine/src/types/game_state.rs"
      via: "Sets WaitingFor::ScryChoice instead of auto-resolving"
      pattern: "WaitingFor::ScryChoice"
    - from: "client/src/components/modal/CardChoiceModal.tsx"
      to: "client/src/adapter/types.ts"
      via: "Renders based on WaitingFor type"
      pattern: "ScryChoice|DigChoice|SurveilChoice"
---

<objective>
Replace simplified Scry/Dig implementations with proper WaitingFor interactive choices, add Surveil effect handler, create a generic card choice modal for the frontend, and wire AI evaluation for all three choice types.

Purpose: The current Scry sends all cards to bottom and Dig takes first N cards. Per CONTEXT.md, these must emit WaitingFor choices so players (and AI) make proper decisions. Scry UI should use per-card top/bottom buttons (MTGA-style).

Output: Three new WaitingFor variants, updated Scry/Dig effect handlers, new Surveil handler, CardChoiceModal component, AI choice evaluation.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-CONTEXT.md
@.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-RESEARCH.md

@crates/engine/src/game/effects/scry.rs
@crates/engine/src/game/effects/dig.rs
@crates/engine/src/game/effects/mod.rs
@crates/engine/src/game/engine.rs
@crates/engine/src/types/game_state.rs
@crates/engine/src/types/actions.rs
@crates/engine-wasm/src/lib.rs
@client/src/adapter/types.ts
@crates/forge-ai/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WaitingFor variants and update Scry/Dig/Surveil engine handlers</name>
  <files>crates/engine/src/types/game_state.rs, crates/engine/src/game/engine.rs, crates/engine/src/game/effects/scry.rs, crates/engine/src/game/effects/dig.rs, crates/engine/src/game/effects/surveil.rs, crates/engine/src/game/effects/mod.rs</files>
  <behavior>
    - Test: Scry 2 sets WaitingFor::ScryChoice with 2 card ObjectIds from top of library
    - Test: SelectCards with top_cards for ScryChoice puts selected on top, rest on bottom
    - Test: Dig 5 (keep 2) sets WaitingFor::DigChoice with 5 cards, player selects 2 to hand, rest to graveyard
    - Test: Surveil 2 sets WaitingFor::SurveilChoice with 2 cards, player picks which go to graveyard vs stay on top
    - Test: Scry 1 with single card still requires choice (top or bottom)
  </behavior>
  <action>
1. Add three WaitingFor variants to `types/game_state.rs`:
   ```
   ScryChoice { player: PlayerId, cards: Vec<ObjectId> }
   DigChoice { player: PlayerId, cards: Vec<ObjectId>, keep_count: usize }
   SurveilChoice { player: PlayerId, cards: Vec<ObjectId> }
   ```
2. Update `effects/scry.rs` resolve(): Instead of moving all to bottom, peek at top N cards from library, store their ObjectIds, then set `state.waiting_for = WaitingFor::ScryChoice { player, cards }` and return Ok. The actual reordering happens when the engine processes the SelectCards response.
3. Update `effects/dig.rs` resolve(): Instead of taking first N to hand, peek at top N cards, set `state.waiting_for = WaitingFor::DigChoice { player, cards, keep_count }`.
4. Create `effects/surveil.rs` with resolve() that peeks at top N cards and sets `state.waiting_for = WaitingFor::SurveilChoice { player, cards }`.
5. Register "Surveil" in effects/mod.rs: `registry.insert("Surveil".to_string(), surveil::resolve);`
6. In `engine.rs`, add three match arms:
   - `(WaitingFor::ScryChoice { player }, GameAction::SelectCards { cards })` -- cards = ObjectIds to put on TOP (ordered), rest go to bottom. Reorder library accordingly. Return to Priority.
   - `(WaitingFor::DigChoice { player, cards: all_cards, keep_count }, GameAction::SelectCards { cards })` -- validate cards.len() == keep_count. Selected go to hand, rest go to graveyard. Return to Priority.
   - `(WaitingFor::SurveilChoice { player }, GameAction::SelectCards { cards })` -- cards = ObjectIds to put in GRAVEYARD. Rest stay on top of library in original order. Return to Priority.
7. Add `pub mod surveil;` to effects/mod.rs.
8. Write 4-5 inline tests in scry.rs, dig.rs, surveil.rs, and engine.rs for the behaviors above.</action>
  <verify>
    <automated>cargo test -p engine -- test_scry -x && cargo test -p engine -- test_dig -x && cargo test -p engine -- test_surveil -x</automated>
  </verify>
  <done>Scry/Dig/Surveil emit proper WaitingFor variants. SelectCards response processes choices correctly. Library/hand/graveyard state updated accurately.</done>
</task>

<task type="auto">
  <name>Task 2: WASM bridge, TS types, CardChoiceModal, and AI evaluation</name>
  <files>crates/engine-wasm/src/lib.rs, client/src/adapter/types.ts, client/src/components/modal/CardChoiceModal.tsx, crates/forge-ai/src/lib.rs</files>
  <action>
1. Update `client/src/adapter/types.ts`:
   - Add `| { type: "ScryChoice"; data: { player: PlayerId; cards: ObjectId[] } }` to WaitingFor union.
   - Add `| { type: "DigChoice"; data: { player: PlayerId; cards: ObjectId[]; keep_count: number } }` to WaitingFor union.
   - Add `| { type: "SurveilChoice"; data: { player: PlayerId; cards: ObjectId[] } }` to WaitingFor union.

2. Create `client/src/components/modal/CardChoiceModal.tsx`:
   - A generic card selection modal that renders based on the WaitingFor type.
   - For ScryChoice: show each card with "Top" / "Bottom" toggle buttons (MTGA-style per user decision). Confirm button sends SelectCards with top-chosen card IDs.
   - For DigChoice: show all cards, player clicks to select `keep_count` cards to keep. Confirm sends SelectCards with selected card IDs.
   - For SurveilChoice: show each card with "Keep" / "Graveyard" toggle. Confirm sends SelectCards with graveyard-chosen card IDs.
   - Use existing card image components (CardImage/ArtCropCard) to display card art.
   - Style with Tailwind, use Framer Motion for card transitions.

3. Wire CardChoiceModal into GamePage: detect ScryChoice/DigChoice/SurveilChoice in waiting_for and render the modal overlay.

4. Update AI in `crates/forge-ai/src/lib.rs`:
   - In the action selection function, handle new WaitingFor variants.
   - For ScryChoice: evaluate each card's value (use existing card hint evaluation), put higher-value cards on top.
   - For DigChoice: select the `keep_count` highest-value cards.
   - For SurveilChoice: send lowest-value cards to graveyard (unless graveyard synergy detected via card hints).
   - Return appropriate GameAction::SelectCards with the chosen ObjectIds.</action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check && cd /Users/matt/dev/forge.rs && cargo test --all</automated>
  </verify>
  <done>TypeScript types match Rust WaitingFor variants. CardChoiceModal renders for all three choice types with MTGA-style Scry UI. AI makes evaluation-based decisions for all choices. Full test suite green.</done>
</task>

</tasks>

<verification>
- `cargo test -p engine -- scry` passes
- `cargo test -p engine -- dig` passes
- `cargo test -p engine -- surveil` passes
- `cargo test --all` passes
- `cd client && pnpm run type-check` passes
- `cargo clippy --all-targets -- -D warnings` clean
</verification>

<success_criteria>
- Scry/Dig/Surveil emit WaitingFor choices instead of auto-resolving
- Frontend CardChoiceModal renders correct UI for each choice type
- Scry uses per-card top/bottom buttons (MTGA-style per user decision)
- AI makes proper evaluation-based decisions for all three
- All existing tests pass (no regressions from changing Scry/Dig behavior)
</success_criteria>

<output>
After completion, create `.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-03-SUMMARY.md`
</output>
