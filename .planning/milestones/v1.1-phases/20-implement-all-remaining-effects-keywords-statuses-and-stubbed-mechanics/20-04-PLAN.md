---
phase: 20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics
plan: 04
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - crates/engine/src/game/planeswalker.rs
  - crates/engine/src/game/engine.rs
  - crates/engine/src/game/sba.rs
  - crates/engine/src/game/combat_damage.rs
  - crates/engine/src/game/effects/deal_damage.rs
  - crates/engine/src/game/game_object.rs
  - crates/engine/src/game/mod.rs
  - crates/engine/src/types/game_state.rs
  - client/src/adapter/types.ts
autonomous: true
requirements: [ENG-08, ENG-09]

must_haves:
  truths:
    - "Player can activate one loyalty ability per turn on a planeswalker they control"
    - "Loyalty abilities cost or add loyalty counters (not mana)"
    - "Planeswalker with 0 loyalty is put into graveyard by SBA"
    - "Combat damage to a player can be redirected to a planeswalker they control"
    - "Second loyalty ability activation in same turn is rejected"
  artifacts:
    - path: "crates/engine/src/game/planeswalker.rs"
      provides: "Planeswalker loyalty ability activation and tracking"
      exports: ["handle_activate_loyalty", "can_activate_loyalty"]
  key_links:
    - from: "crates/engine/src/game/engine.rs"
      to: "crates/engine/src/game/planeswalker.rs"
      via: "ActivateAbility checks if source is planeswalker"
      pattern: "planeswalker::can_activate_loyalty"
    - from: "crates/engine/src/game/sba.rs"
      to: "crates/engine/src/game/game_object.rs"
      via: "704.5i check for 0 loyalty"
      pattern: "check_zero_loyalty"
---

<objective>
Implement planeswalker loyalty ability activation, once-per-turn tracking, SBA for 0 loyalty, and combat damage-to-planeswalker redirection.

Purpose: Planeswalkers are a core card type in Standard. The loyalty field exists on GameObject but no activation mechanic, turn tracking, or damage redirection is implemented. This blocks all Planeswalker cards.

Output: New planeswalker module, loyalty activation flow, SBA extension, damage redirect.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-CONTEXT.md

@crates/engine/src/game/engine.rs
@crates/engine/src/game/sba.rs
@crates/engine/src/game/combat_damage.rs
@crates/engine/src/game/effects/deal_damage.rs
@crates/engine/src/game/game_object.rs
@crates/engine/src/types/game_state.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Planeswalker loyalty activation with once-per-turn tracking</name>
  <files>crates/engine/src/game/planeswalker.rs, crates/engine/src/game/engine.rs, crates/engine/src/game/game_object.rs, crates/engine/src/game/mod.rs, crates/engine/src/types/game_state.rs</files>
  <behavior>
    - Test: Activating a +1 loyalty ability adds 1 loyalty counter and puts ability on stack
    - Test: Activating a -3 loyalty ability removes 3 loyalty counters (must have >= 3)
    - Test: Second loyalty activation on same planeswalker in same turn is rejected
    - Test: Loyalty activation resets at start of controller's turn (can activate again next turn)
    - Test: Planeswalker can only activate loyalty at sorcery speed (main phase, empty stack, active player)
  </behavior>
  <action>
1. Add `loyalty_activated_this_turn: bool` field to `GameObject` (default false). Reset it in `turns.rs` at turn start for all permanents controlled by active player.
2. Create `crates/engine/src/game/planeswalker.rs`:
   - `can_activate_loyalty(state: &GameState, planeswalker_id: ObjectId, player: PlayerId) -> bool` -- checks: is a planeswalker, controlled by player, !loyalty_activated_this_turn, sorcery-speed timing (main phase, empty stack, active player).
   - `handle_activate_loyalty(state: &mut GameState, player: PlayerId, pw_id: ObjectId, ability_index: usize, events: &mut Vec<GameEvent>) -> Result<WaitingFor, EngineError>` -- parse the ability at ability_index, determine loyalty cost (look for "PW_Cost$" param or parse from ability definition), validate sufficient loyalty for minus abilities, adjust loyalty counter, set loyalty_activated_this_turn = true, then push the ability onto the stack. Return Priority.
3. In `engine.rs`, modify the ActivateAbility handler: after mana ability check (from Plan 01), check if source object is a planeswalker (has CoreType::Planeswalker or loyalty.is_some()). If so, route to `planeswalker::handle_activate_loyalty()`.
4. Add `pub mod planeswalker;` to mod.rs.
5. Write 4-5 inline tests in planeswalker.rs.</action>
  <verify>
    <automated>cargo test -p engine -- planeswalker -x</automated>
  </verify>
  <done>Loyalty abilities activate with correct counter changes, once-per-turn enforced, sorcery-speed enforced, ability goes on stack.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SBA for 0 loyalty and damage-to-planeswalker redirection</name>
  <files>crates/engine/src/game/sba.rs, crates/engine/src/game/combat_damage.rs, crates/engine/src/game/effects/deal_damage.rs, client/src/adapter/types.ts</files>
  <behavior>
    - Test: Planeswalker with 0 loyalty goes to graveyard via SBA (704.5i)
    - Test: Planeswalker with 1+ loyalty stays on battlefield
    - Test: Noncombat damage dealt to a player can be redirected to their planeswalker (optional -- for now, direct damage to planeswalker removes loyalty)
    - Test: Combat damage from unblocked attacker that "attacks planeswalker" reduces loyalty
  </behavior>
  <action>
1. In `sba.rs`, add `check_zero_loyalty()` function:
   - Scan battlefield for objects with `loyalty == Some(0)` that are Planeswalkers
   - Move them to graveyard
   - Call this from the fixpoint loop in `check_state_based_actions` after existing checks
2. In `combat_damage.rs` and/or `deal_damage.rs`:
   - When dealing damage to a planeswalker (TargetRef::Object where target is a planeswalker), remove that many loyalty counters instead of marking damage.
   - If loyalty goes to 0 or below, SBA will handle cleanup.
3. Update `client/src/adapter/types.ts`: no new WaitingFor variants needed here, but ensure GameState serialization includes loyalty_activated_this_turn if it needs to be visible (likely skip_deserializing like has_summoning_sickness).
4. Write 3-4 inline tests in sba.rs and deal_damage.rs.</action>
  <verify>
    <automated>cargo test -p engine -- planeswalker -x && cargo test -p engine -- sba -x && cargo test --all</automated>
  </verify>
  <done>Planeswalkers with 0 loyalty die to SBA. Damage to planeswalkers removes loyalty. Combat and noncombat damage both work correctly against planeswalkers.</done>
</task>

</tasks>

<verification>
- `cargo test -p engine -- planeswalker` passes
- `cargo test -p engine -- sba` passes
- `cargo test --all` passes
- `cargo clippy --all-targets -- -D warnings` clean
</verification>

<success_criteria>
- Planeswalker loyalty abilities activate and adjust counters
- Once-per-turn restriction enforced
- Sorcery-speed timing enforced
- SBA destroys 0-loyalty planeswalkers
- Damage to planeswalkers removes loyalty counters
</success_criteria>

<output>
After completion, create `.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-04-SUMMARY.md`
</output>
