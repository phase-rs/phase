---
phase: 20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics
plan: 10
type: execute
wave: 5
depends_on: [20-06, 20-07, 20-08, 20-09]
files_modified:
  - crates/engine/src/bin/coverage_report.rs
  - crates/engine/src/game/coverage.rs
  - data/standard-cards/
  - .github/workflows/ci.yml
autonomous: true
requirements: [ENG-18, ENG-19]

must_haves:
  truths:
    - "Coverage report runs against Standard-legal card subset and shows 100% coverage"
    - "CI pipeline fails if any Standard-legal card has unsupported mechanics"
    - "Standard-legal card data is checked into the repository"
  artifacts:
    - path: "data/standard-cards/"
      provides: "Standard-legal Forge card definition files for CI"
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with coverage gate step"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "crates/engine/src/bin/coverage_report.rs"
      via: "CI runs coverage_report binary against data/standard-cards/"
      pattern: "cargo run --bin coverage_report"
---

<objective>
Curate Standard-legal card data subset, run final coverage report to validate 100% Standard coverage, and add CI gate that fails on any unsupported Standard cards.

Purpose: This is the validation gate for the entire phase. Per CONTEXT.md, 100% Standard-legal coverage is the hard target. The CI gate prevents regressions. Must run after all mechanic implementations (Plans 01-09) are complete.

Output: Standard card data directory, updated coverage_report binary with exit code, CI workflow step.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-CONTEXT.md

@crates/engine/src/bin/coverage_report.rs
@crates/engine/src/game/coverage.rs
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Curate Standard-legal card subset and update coverage report for CI gating</name>
  <files>crates/engine/src/bin/coverage_report.rs, crates/engine/src/game/coverage.rs, data/standard-cards/</files>
  <action>
1. Create `data/standard-cards/` directory.
2. Write a script or use the existing Forge card data to extract Standard-legal cards. The approach:
   - Read all Forge .txt card files from the card data directory (the path used in tests via FORGE_CARDS_PATH)
   - Filter to only Standard-legal sets. Standard-legal sets as of 2026 include the most recent sets. Since the exact set list depends on rotation, use a reasonable approximation: include cards from recent Standard-legal sets (e.g., the last 2-3 years of sets).
   - Alternative (simpler, per CONTEXT): create a curated list of Standard-legal card names and copy their .txt files to `data/standard-cards/`.
   - The exact curation can use Forge's edition data or a hardcoded list of Standard set codes.
3. Update `coverage_report.rs`:
   - Add `--ci` flag: when present, exit with code 1 if any cards are unsupported.
   - Print summary to stderr, JSON to stdout.
   - Add `--standard-only` flag to filter only the data/standard-cards/ subset.
   - Show the unsupported card names and their missing handlers on failure.
4. Update `coverage.rs` if needed: ensure the analysis properly reports which specific handlers are missing per card.
5. Run the report against `data/standard-cards/` and verify 100% coverage. If there are remaining unsupported cards, document them (they should be addressed by earlier plans in this phase).
6. Write 1-2 tests for the CI flag behavior.</action>
  <verify>
    <automated>cargo run --bin coverage_report -- data/standard-cards/ --ci</automated>
  </verify>
  <done>Standard-legal card data curated and checked in. Coverage report with --ci flag exits 0 for 100% coverage, exits 1 for any gaps.</done>
</task>

<task type="auto">
  <name>Task 2: Add coverage gate to CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
1. In `.github/workflows/ci.yml`, add a new step to the Rust job after the existing test step:
   ```yaml
   - name: Standard coverage gate
     run: cargo run --bin coverage_report -- data/standard-cards/ --ci
   ```
2. This step runs after `cargo test --all` and before the WASM build step.
3. If the coverage report finds any unsupported Standard cards, it exits with code 1 and the CI job fails.
4. Ensure the `data/standard-cards/` directory is committed to the repo so CI can access it.
5. Run `cargo test --all` to verify no regressions from all Phase 20 changes.</action>
  <verify>
    <automated>cargo test --all && cargo run --bin coverage_report -- data/standard-cards/ --ci</automated>
  </verify>
  <done>CI workflow includes coverage gate step. Full test suite passes. Coverage report confirms 100% Standard coverage.</done>
</task>

</tasks>

<verification>
- `cargo test --all` passes (full regression suite)
- `cargo run --bin coverage_report -- data/standard-cards/ --ci` exits 0
- `cargo clippy --all-targets -- -D warnings` clean
- `cd client && pnpm run type-check` passes
</verification>

<success_criteria>
- Standard-legal card data subset exists in data/standard-cards/
- Coverage report with --ci flag validates 100% Standard coverage
- CI workflow fails on any coverage regression
- All engine tests pass
- All frontend type checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-10-SUMMARY.md`
</output>
