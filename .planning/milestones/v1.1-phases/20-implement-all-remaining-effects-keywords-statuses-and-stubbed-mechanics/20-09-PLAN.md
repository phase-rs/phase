---
phase: 20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics
plan: 09
type: execute
wave: 4
depends_on: [20-05, 20-06, 20-07]
files_modified:
  - crates/engine/src/game/morph.rs
  - crates/engine/src/game/engine.rs
  - crates/engine/src/game/game_object.rs
  - crates/engine/src/game/mod.rs
  - crates/engine/src/types/actions.rs
  - crates/engine/src/types/events.rs
  - crates/engine/src/game/triggers.rs
  - client/src/adapter/types.ts
autonomous: true
requirements: [ENG-17]

must_haves:
  truths:
    - "Face-down creatures are 2/2 with no name, types, or abilities"
    - "Morph creatures can be turned face up by paying their morph cost"
    - "Hidden information: opponents cannot see the identity of face-down cards"
    - "Manifest puts top card of library face down as 2/2 creature"
  artifacts:
    - path: "crates/engine/src/game/morph.rs"
      provides: "Morph/manifest/disguise face-down mechanics"
      exports: ["play_face_down", "turn_face_up", "manifest"]
  key_links:
    - from: "crates/engine/src/game/morph.rs"
      to: "crates/engine/src/game/game_object.rs"
      via: "face_down field controls characteristics visibility"
      pattern: "face_down"
    - from: "crates/engine/src/game/morph.rs"
      to: "crates/engine/src/game/transform.rs"
      via: "Turn face up uses characteristic restoration similar to transform"
      pattern: "face_down = false"
---

<objective>
Implement Morph/Manifest/Disguise face-down mechanics -- playing creatures face down as 2/2, turning face up by paying costs, and manifest from library top.

Purpose: Morph/Manifest/Disguise creates face-down creatures that can be revealed. This is a distinct subsystem from Day/Night and can be implemented independently. Depends on Plan 05 (transform for characteristic swapping patterns), Plan 06 (trigger matchers for TurnFaceUp), and Plan 07 (effect handlers that may trigger manifest).

Output: Morph module with face-down play, face-up actions, manifest, hidden information support, TypeScript types.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-CONTEXT.md
@.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-05-SUMMARY.md
@.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-06-SUMMARY.md
@.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-07-SUMMARY.md

@crates/engine/src/game/engine.rs
@crates/engine/src/game/game_object.rs
@crates/engine/src/types/actions.rs
@crates/engine/src/types/events.rs
@crates/engine/src/game/triggers.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Face-down play and turn-face-up mechanics</name>
  <files>crates/engine/src/game/morph.rs, crates/engine/src/game/game_object.rs, crates/engine/src/game/mod.rs, crates/engine/src/types/events.rs</files>
  <behavior>
    - Test: Playing a morph creature face down creates a 2/2 with no name/types/abilities
    - Test: Turning face up by paying morph cost reveals the card and restores characteristics
    - Test: TurnFaceUp trigger fires when morph is turned face up
    - Test: Face-down card in multiplayer has hidden identity from opponents (server-side check)
  </behavior>
  <action>
1. Create `crates/engine/src/game/morph.rs`:
   - `play_face_down(state: &mut GameState, player: PlayerId, card_id: CardId, events: &mut Vec<GameEvent>) -> Result<WaitingFor, EngineError>`:
     - Move card from hand to battlefield face_down = true
     - Override characteristics: name = "" (empty), power = 2, toughness = 2, card_types = Creature only, keywords = empty, abilities = empty
     - Store original characteristics so they can be restored on turn face up
   - `turn_face_up(state: &mut GameState, player: PlayerId, object_id: ObjectId, events: &mut Vec<GameEvent>) -> Result<WaitingFor, EngineError>`:
     - Validate player controls the face-down permanent
     - Check morph cost can be paid (for now, check if the card has a morph/disguise keyword)
     - Set face_down = false, restore original characteristics
     - Emit `GameEvent::TurnedFaceUp { object_id }`
2. Add `GameEvent::TurnedFaceUp { object_id: ObjectId }` to events.rs.
3. Add `pub mod morph;` to mod.rs.
4. Write 3-4 inline tests.</action>
  <verify>
    <automated>cargo test -p engine -- morph -x && cargo test -p engine -- face_down -x</automated>
  </verify>
  <done>Face-down creatures are 2/2 with hidden characteristics. Turn face up restores real card. TurnFaceUp event emitted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Manifest, engine wiring, trigger promotion, and TS types</name>
  <files>crates/engine/src/game/morph.rs, crates/engine/src/game/engine.rs, crates/engine/src/types/actions.rs, crates/engine/src/game/triggers.rs, client/src/adapter/types.ts</files>
  <behavior>
    - Test: Manifest puts top card of library face down as 2/2 creature
    - Test: Manifested creature card can be turned face up by paying its mana cost
    - Test: Manifested noncreature card cannot be turned face up (no morph cost)
    - Test: TurnFaceUp trigger matcher fires on TurnedFaceUp event
  </behavior>
  <action>
1. Add to `crates/engine/src/game/morph.rs`:
   - `manifest(state: &mut GameState, player: PlayerId, events: &mut Vec<GameEvent>) -> Result<(), EngineError>`:
     - Take top card of library
     - Put it onto battlefield face down as 2/2 creature
     - If it's a creature card, it can be turned face up by paying its mana cost
2. Add `GameAction::PlayFaceDown { card_id: CardId }` and `GameAction::TurnFaceUp { object_id: ObjectId }` to actions.rs.
3. In engine.rs, add match arms for PlayFaceDown and TurnFaceUp during Priority.
4. In triggers.rs, promote `TurnFaceUp` from unimplemented to real matcher that checks for TurnedFaceUp event.
5. Update `client/src/adapter/types.ts` with new action/event types:
   - Add `| { type: "PlayFaceDown"; data: { card_id: CardId } }` to GameAction union.
   - Add `| { type: "TurnFaceUp"; data: { object_id: ObjectId } }` to GameAction union.
   - Add `| { type: "TurnedFaceUp"; data: { object_id: ObjectId } }` to GameEvent union.
6. Write 3-4 inline tests for manifest and trigger promotion.</action>
  <verify>
    <automated>cargo test -p engine -- morph -x && cargo test -p engine -- manifest -x && cd /Users/matt/dev/forge.rs/client && pnpm run type-check && cd /Users/matt/dev/forge.rs && cargo test --all</automated>
  </verify>
  <done>Manifest works. Engine wiring for PlayFaceDown and TurnFaceUp complete. TurnFaceUp trigger fires. TypeScript types updated. All tests pass.</done>
</task>

</tasks>

<verification>
- `cargo test -p engine -- morph` passes
- `cargo test -p engine -- manifest` passes
- `cargo test --all` passes
- `cd client && pnpm run type-check` passes
- `cargo clippy --all-targets -- -D warnings` clean
</verification>

<success_criteria>
- Face-down creatures are 2/2 with no visible characteristics
- Morph turn-face-up works with cost payment
- Manifest puts library top face down
- TurnFaceUp trigger fires correctly
- TypeScript types updated for WASM boundary
</success_criteria>

<output>
After completion, create `.planning/phases/20-implement-all-remaining-effects-keywords-statuses-and-stubbed-mechanics/20-09-SUMMARY.md`
</output>
