---
phase: 19-recreate-the-mtga-ui-as-faithfully-as-possible
plan: 06
type: execute
wave: 3
depends_on: ["19-03"]
files_modified:
  - client/src/pages/GamePage.tsx
autonomous: true
requirements: [ARENA-09]
must_haves:
  truths:
    - "Mulligan screen is full-screen with large full-card images centered"
    - "Mulligan has prominent Keep Hand and Mulligan buttons"
    - "Game over shows dramatic full-screen VICTORY (gold + particles) or DEFEAT (red/dark) overlay"
    - "Game over shows final life totals and menu + rematch buttons"
  artifacts:
    - path: "client/src/pages/GamePage.tsx"
      provides: "Full-screen mulligan and dramatic game over screens"
      contains: "VICTORY"
  key_links:
    - from: "client/src/pages/GamePage.tsx"
      to: "client/src/stores/gameStore.ts"
      via: "reads waitingFor for MulliganDecision and GameOver"
      pattern: "waitingFor.*MulliganDecision|GameOver"
---

<objective>
Upgrade the mulligan screen and game over screen to full-screen MTGA-style dramatic presentations.

Purpose: These screens bookend the game experience — first (mulligan) and last (game over) impressions. Making them dramatic and polished creates the MTGA feel.
Output: Redesigned MulliganDecisionPrompt and GameOverScreen components within GamePage.tsx.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-recreate-the-mtga-ui-as-faithfully-as-possible/19-CONTEXT.md
@.planning/phases/19-recreate-the-mtga-ui-as-faithfully-as-possible/19-03-SUMMARY.md

<interfaces>
From client/src/pages/GamePage.tsx (current mulligan):
```typescript
// MulliganDecisionPrompt: modal with small card images (130x182px) in a flex-wrap grid
// MulliganBottomCardsPrompt: similar layout for selecting cards to bottom
// GameOverScreen: simple modal with "Game Over" text, winner info, and Return to Menu button
```

From client/src/components/card/CardImage.tsx:
```typescript
export function CardImage({ cardName, size, className, tapped, hasUnimplementedMechanics }: CardImageProps): JSX.Element;
// Use size="normal" for large readable cards in mulligan
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-screen MTGA-style mulligan screen</name>
  <files>client/src/pages/GamePage.tsx</files>
  <action>
Rewrite `MulliganDecisionPrompt` to be a full-screen cinematic mulligan experience:

1. **Full-screen backdrop**: Dark gradient background with slight radial light at center. No modal box — the entire screen IS the mulligan.

2. **Title area**: "Opening Hand" in large text at top center with subtle text glow. Below it: "Mulligan {mulliganCount}" if mulliganCount > 0, showing how many cards will be drawn.

3. **Card display**: Large, centered card images using `size="normal"` for readable card text. Display cards in a horizontal row, evenly spaced:
   - Size: ~160px wide x ~224px tall (larger than current 130x182)
   - Add hover effect: slight scale up (1.05) and elevated shadow
   - Cards should fan slightly (rotate -6deg, -3deg, 0, 3deg, 6deg etc. based on position)
   - Use Framer Motion for card entrance animation (stagger in from bottom)

4. **Button area**: Two prominent buttons at bottom center:
   - "Keep Hand" — large, emerald green, bold. Wide (px-12 py-4 text-xl)
   - "Mulligan (Draw {nextHandSize} cards)" — outline style, gray/white border
   - Buttons animate in with slight delay after cards appear

5. Keep the `onChoose` callback unchanged. Same handler as before.

Also rewrite `MulliganBottomCardsPrompt` similarly:
- Full-screen presentation
- "Put {count} cards on bottom" title
- Same large card display with click-to-select (selected cards get cyan ring + slight transparency)
- "Confirm" button at bottom
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check</automated>
  </verify>
  <done>Mulligan screen is full-screen with large readable card images, fan layout, staggered entrance animation, and prominent Keep/Mulligan buttons. Bottom cards prompt uses same full-screen style.</done>
</task>

<task type="auto">
  <name>Task 2: Dramatic VICTORY/DEFEAT game over screen</name>
  <files>client/src/pages/GamePage.tsx</files>
  <action>
Rewrite `GameOverScreen` to be a dramatic full-screen overlay:

**Victory (winner === 0, player wins):**
1. Full-screen dark backdrop with golden radial gradient at center
2. Large "VICTORY" text (text-6xl font-black tracking-widest) with golden color (#C9B037) and triple text-shadow glow (similar to TurnBanner glow treatment):
   - `text-shadow: 0 0 20px rgba(201,176,55,0.8), 0 0 40px rgba(201,176,55,0.5), 0 0 80px rgba(201,176,55,0.3)`
3. Text animates in: scale from 0.5 to 1.0 with overshoot spring
4. Below the text: show final life totals — "You: {playerLife} / Opponent: {opponentLife}" in white
5. Simple particle effect: golden particles floating upward (can reuse ParticleCanvas burst pattern or just CSS-animated dots)
6. Buttons: "Return to Menu" (gold bg) and "Rematch" (outline) — both large and centered

**Defeat (winner === 1, opponent wins):**
1. Full-screen dark backdrop with dark red radial gradient
2. Large "DEFEAT" text in dark red (#991B1B) with red glow text-shadow
3. Same scale-in animation
4. Final life totals below
5. No particles (somber mood)
6. Buttons: "Return to Menu" (gray bg) and "Rematch" (outline)

**Draw (winner === null):**
1. Neutral dark backdrop
2. "DRAW" text in silver with subtle glow
3. Life totals and buttons

Read life totals from `useGameStore((s) => s.gameState?.players)`. Player 0 and Player 1 life values.

For the "Rematch" button: navigate to the same game URL with a new UUID — `navigate(/game/${crypto.randomUUID()}?mode=${mode}&difficulty=${difficulty})`. The mode and difficulty can be read from the URL search params already available in the parent component (GamePageContent receives `mode`).

Use Framer Motion for the text scale-in animation. The `onComplete` of animate triggers the buttons to appear (stagger).
  </action>
  <verify>
    <automated>cd /Users/matt/dev/forge.rs/client && pnpm run type-check</automated>
  </verify>
  <done>Game over shows dramatic full-screen VICTORY (gold glow + particles) or DEFEAT (red glow) or DRAW (silver) with final life totals, animated text entrance, and Return to Menu + Rematch buttons.</done>
</task>

</tasks>

<verification>
- `cd client && pnpm run type-check` passes
- MulliganDecisionPrompt renders full-screen with large card images
- GameOverScreen renders VICTORY/DEFEAT with dramatic text effects
- Both screens use Framer Motion for entrance animations
</verification>

<success_criteria>
- Mulligan is full-screen MTGA-style with large readable cards, fan layout, and prominent buttons
- Game over shows dramatic VICTORY (gold+particles) / DEFEAT (red/dark) / DRAW (silver) overlays
- Final life totals displayed on game over screen
- Rematch button starts a new game with same settings
</success_criteria>

<output>
After completion, create `.planning/phases/19-recreate-the-mtga-ui-as-faithfully-as-possible/19-06-SUMMARY.md`
</output>
